MASK AI MARKET INTELLIGENCE & SELECTION SYSTEM
MASTER PROGRESS TRACKER
========================================================================
Last updated: 2026-09-11 19:25 PKT (UTC+05:00)
Tracker version: 1
Project root: outputs/mask-market-intelligence/ (relative to workspace)
Canonical tracker: progress.txt in this project root
Source of requirements: supplied MASK AI Complete Development Guide, the
Autonomous Market Intelligence Engine Build Specification v1, and docs/

1. CURRENT POSITION
------------------------------------------------------------------------
Current phase: Autonomous functional research build A01-A20 plus A17.5, with the established P01-P03 infrastructure/evidence foundations preserved.
Current implementation status: IN_PROGRESS - Supabase PostgreSQL/pgvector is live;
A01-A05, A10-A17 are complete, A06 enforces source holds plus source-family/
persona normalization for free collectors, and A07/A09 have offline foundations.
Census, BEA, SAM.gov, keyless BLS, and SEC each passed one bounded live smoke;
Gemini and YouTube Data API credentials are now locally configured (owner-
supplied 2026-09-10) and live wiring for both is in progress under A17.5.
Current task: A17.5 in progress. Five live-capable sources now exist: a
Gemini analysis provider (mirroring openai_responses.py's exact shape;
extraction/classification only, never scores/confidence/gates/vetoes), a
YouTube Data API adapter (mask_api.modules.youtube_data;
search.list/commentThreads.list for M2/M3/M5, with its own per-endpoint
quota-unit ledger and a distinct quota-exceeded error path for
SOURCE_UNAVAILABLE_QUOTA handling), a sixth official_data source,
USAspending award search (M4/M5, keyless), an O*NET database bootstrap/
cache/import pipeline for M3 (mask_api.modules.onet_data), and a targeted
Common Crawl retriever (mask_api.modules.common_crawl_data; one CDX
lookup plus one ranged WARC-record fetch, reusing the existing static-
HTML parse/normalize pipeline unchanged). Live status: Common Crawl's
gated smoke test passed against a real server; the original 5
official_data live smokes still pass; USAspending's equivalent live
smoke exists but has not been run; O*NET's live download was attempted
twice -- the first attempt found and fixed a real silent-truncation bug,
the second (post-fix) correctly rejected a genuine mid-transfer
connection drop as a clean retryable error instead of corrupting the
cache, but neither completed end to end against this environment's very
slow connection to onetcenter.org (see docs/ONET_DATA.md); Gemini/YouTube
remain offline-tested only, disabled until explicit policy approval. The
youtube source profile entry is now operational_status: available (local
MASK_youtube_API_KEY configured), matching how census_cbp/sam_gov are
available despite also requiring a credential. Next: the provider-
independent source/evidence fallback registry, then the first real
MethodExecutor wiring. A07 and A09 remain partial only because their
live provider access is held/being wired; Google Ads, Reddit, Google
Places, Meta, Brave, and firmographic providers remain skipped/optional.
Paid collection/model use remains disabled; Gemini/YouTube use their free
tiers/quotas only, targeting $0 external paid-data cost.
Local authentication remains application-owned and disabled at the HTTP boundary;
Supabase Auth is not added. The runtime uses the IPv4 session pooler with TLS and
the migration role uses the direct TLS endpoint.

Completed checkpoints: 31/172
Remaining checkpoints: 141/172
Checkpoint-count progress: 18.0%
Completed phases: 1/25 (P00 baseline approved for infrastructure work).
Phase 0 drafting/initial verification checkpoints complete: 10/10.
Phase 0 approval checkpoint remaining: 0; numerical rubric approvals deferred.
Implementation/operational checkpoints complete: 7/140 (Phases 1-24).
Autonomous functional-build checkpoints complete: 13/21 (A01-A20 plus A17.5).
Phase 1: IN_PROGRESS (6/10 verified). Phase 2: IN_PROGRESS (1/7 verified).
Phase 3: IN_PROGRESS (0/8 verified). Phases 4-24: NOT_STARTED.

IMPORTANT: Checkpoints are not equal effort. This percentage measures
checklist completion, NOT engineering hours, budget, delivery time, or
percentage of working software. No reliable delivery-date estimate exists
yet. Recalculate counts when the baseline changes; never inflate progress
by marking documentation as implemented functionality.

Next checkpoint: continue A17.5 (the provider-independent source/evidence
fallback registry with Tier A-E quality classes, then the first real
MethodExecutor wiring), per the owner's explicit 2026-09-10 free-first
research directive. Gemini, YouTube, USAspending, O*NET, and Common Crawl
are all now implemented and offline-tested; wiring Gemini/YouTube through
a real transport and a composition root (with explicit policy approval)
is still pending, running USAspending's/YouTube's gated live smoke tests
is still pending, and completing O*NET's live smoke end to end remains
pending a more reliable connection to onetcenter.org.
Paid/model network use stays disabled except Gemini/YouTube's free tiers; A07
and the other held providers remain at their live edges.
User explicitly requires Windows-only, no Docker, requirements-only scope, modularity, and lean market research/model usage under D13.
The A01-A20 functional track may proceed with remaining P01/P02 acceptance open;
this is a sequencing change, not acceptance of missing live-service/CI or security checks.
No production deployment, paid services, outreach, or research is authorized.

Resume prerequisites and sequence:
1. Implement A01-A17 as the CLI-first functional vertical slice. Keep unsupported
   inputs UNKNOWN and keep every score deterministic and formula-versioned.
2. Implement A02 LocalArtifactStore through the same typed persistence ports;
   it is an explicit CLI target, never a silent API/worker database fallback.
3. Technical lead: from this project root, use pinned Node/pnpm/Python/uv;
   pnpm install --frozen-lockfile; uv sync --frozen; pnpm run setup.
   setup preserves the existing ignored .env; privately configure actual service
   URLs. It does not provision database roles or start services.
4. On the isolated configured Windows environment:
   pnpm migrate; separate terminals for pnpm dev:worker, pnpm dev:api, and pnpm dev.
   pnpm check:services; pnpm test; pnpm lint; pnpm typecheck; pnpm schemas:check.
   Inspect worker logs/shutdown and operator service-restart recovery separately;
   resolve real-service findings before closing P01-05/P01-06/P01-07/P01-10.
5. Extend the existing Phase 3 source-policy/collector core through A04-A10. Do
   not dispatch live collection until a source and current policy version are
   registered and approved.
6. After the Supabase migration/auth persistence checks pass, apply Alembic 0004,
   run bootstrap/auth/session/CSRF acceptance, and register only the auth routes;
   market routes still require P02-03 resource authorization and audit behavior.
7. Run the complete local Windows release suite. Hosted CI/branch protection is
   deferred until an owner-approved remote exists and is not a private-MVP blocker.
8. Update this tracker after every attempt. The latest user request activates
   A01-A20; Phases 1-3 cannot be signed off until
   their dependencies/acceptance pass. Do not claim mocks as live integration.

Current verification: Supabase PostgreSQL 17 and pgvector 0.8.2 are live at
Alembic 0006 with 12 private mask tables. The restricted mask_app runtime role,
TLS, search path, grants, Data API isolation, schema reflection, tenant checks,
and local-auth persistence passed 18 focused live tests. Full offline regression:
696 Python tests PASS (was 628; +68 A17.5 O*NET and Common Crawl focused
tests: O*NET CSV parsing against real-verified column headers/blank-value
behavior, local cache round-trip/corruption/path-traversal detection,
bootstrap download-once/skip-when-cached/redownload-when-changed logic,
and the new incomplete-download detection added after the live bug found
below; Common Crawl CDX request/NDJSON-response parsing including the
real "no captures" 404 shape, WARC record parsing against a real-verified
structure (including the trailing-terminator-truncation fix), the
retriever's enabled/policy/source-policy-scope/budget gates and short-
range/content-type/status rejection, and an integration suite proving
reuse of the existing StaticHtmlCollector.parse()/normalize_document()
unchanged). Ruff/format over 349 files, strict mypy over 187 source files
(apps/api/src plus workers), architecture/import-cycle, and research-
configuration/OpenAPI schema drift PASS (no new schema needed); local
secret guard and current Gitleaks source scan PASS (confirmed the Gemini/
YouTube local credential names remain unleaked, and no fixture API key
pattern was flagged). Node.js/pnpm confirm the full web suite (11 tests),
web lint, web format:check, web typecheck, `pnpm schemas:check`, and
`pnpm check:progress` (31/172 completed, 141 remaining, matching this
entry's hand-recalculated totals) all PASS.
P01-08 remains open for its broader historical/CI
acceptance rather than a current finding. Auth and collection routes remain
disabled. The original A05's 5 bounded official-source live tests PASS,
plus a new live Common Crawl smoke (real CDX lookup + ranged WARC fetch
against example.com, PASS). USAspending's live smoke has not yet been
run; O*NET's live download was attempted twice and did not complete end
to end (see docs/ONET_DATA.md) -- an honest environment limitation, not a
known code defect; the fix it produced is offline-verified. No web/API/worker
runtime, broad collection, or paid research ran.

2. STATUS AND COMPLETION RULES
------------------------------------------------------------------------
[x] DONE             Work and its acceptance checks are completed.
[~] IN_PROGRESS      Started; incomplete or verification still running.
[?] REVIEW_PENDING   Waiting for a specific human review/approval.
[!] BLOCKED          Cannot proceed; record exact cause, owner, and next step.
[ ] NOT_STARTED      No checkpoint implementation has begun.

A phase is DONE only when every checkpoint and its exit condition are met.
Partial code, a drafted plan, skipped tests, unavailable credentials, or an
unapproved numerical rubric are not completion.
Dependencies not yet reached are NOT_STARTED, not automatically BLOCKED.

For each applicable implementation checkpoint, completion includes:
- Implementation and safe error handling/logging.
- Unit tests and relevant integration/end-to-end or parser/evaluation tests.
- Lint and type checks.
- Migration for schema changes.
- Documentation and evidence of acceptance.
- No secrets, access-control bypass, or unapproved methodology changes.
If a check is not applicable, say why. If not run, say NOT RUN and why.
A checkpoint may only be closed with an explicit documented acceptance of
any permitted limitation; mandatory safety/methodology controls cannot be waived.

3. UPDATE PROTOCOL - AFTER EVERY CHECKPOINT
------------------------------------------------------------------------
1. Read this file and AGENTS.md before starting project work.
2. Identify the checkpoint ID and verify dependencies/current files.
3. On starting work, mark it IN_PROGRESS and set the current/next checkpoint.
4. After each checkpoint attempt, update its status immediately; do not
   wait until the end of a whole phase or a long work session.
5. Append a history entry with date/time, ID, work done, changed files,
   verification commands/results, limitations, and next action.
6. If interrupted or blocked, record partial work, exact blocker, responsible
   person/action, and resumable next step. Do not mark it DONE.
7. Recalculate completed/remaining totals, checkpoint-count percentage,
   phase rollup, current status, pending decisions, and next checkpoint.
8. Keep README status and any detailed phase checklist consistent.
9. Preserve stable IDs and prior history. New scope gets new IDs; explain
   denominator changes. Do not silently delete unfinished work.
10. Before ending a work session, save this file even if no checkpoint closed.

Canonical requirements stay in docs/; this file tracks execution and does
not replace specifications. The detailed Phase 1 checklist expands the
P01 checkpoints; its checkboxes are not counted a second time.
The Phase 0 ZIP is a static historical snapshot, not the live tracker.
No scheduled/background automation is needed: the agent updates this file
while doing authorized project work. Future project instructions enforce it.

4. ROADMAP AND NUMBERING
------------------------------------------------------------------------
Numbering follows the original guide's PHASE 0 through PHASE 24 headings.
The guide's later dependency list decomposes some of these into smaller
steps; authentication, embeddings, initial AI evaluation, and confidence
are included here as checkpoints rather than duplicate phases.
Follow checkpoint dependencies, not just ascending phase numbers.

Security, audit, evaluation, and critical-veto controls begin in the modules
that need them. Phases 18-21 consolidate and harden them; they are not
permission to leave earlier modules unsafe or unaudited.
M10 is the last analytical module. Gate 3/4 behavior remains not_ready until
its rubric and evidence are approved. Fixtures must not masquerade as real
market research.

ID  | Status         | Done  | Phase
P00 | DONE           | 11/11 | Freeze the Research Specification
P01 | IN_PROGRESS    |  6/10 | Repository & Development Environment
P02 | IN_PROGRESS    |  1/ 7 | Authentication, Market Registry & Research Workspace
P03 | IN_PROGRESS    |  0/ 8 | Evidence & Source Engine
P04 | NOT_STARTED    |  0/ 9 | Sentiment & Commercial Pain Intelligence
P05 | NOT_STARTED    |  0/ 5 | Quantitative Market Intelligence (M1)
P06 | NOT_STARTED    |  0/ 5 | Economic Pain Engine (M4)
P07 | NOT_STARTED    |  0/ 5 | Competitive Intelligence (M5)
P08 | NOT_STARTED    |  0/ 5 | Search & Buying Intent (M6)
P09 | NOT_STARTED    |  0/ 5 | Buyer Accessibility & Channel Fit (M7)
P10 | NOT_STARTED    |  0/ 5 | Workflow Intelligence (M3)
P11 | NOT_STARTED    |  0/ 5 | Primary Customer Research (M8)
P12 | NOT_STARTED    |  0/ 4 | MASK AI & Productization Fit (M9)
P13 | NOT_STARTED    |  0/ 7 | Deterministic Scoring & Confidence Engine
P14 | NOT_STARTED    |  0/ 4 | Evidence Gap Detection
P15 | NOT_STARTED    |  0/ 6 | Stage-Gate Engine
P16 | NOT_STARTED    |  0/ 7 | Market Comparison Dashboard
P17 | NOT_STARTED    |  0/ 5 | Real-World Market Validation (M10)
P18 | NOT_STARTED    |  0/ 4 | Red Flag & Veto Engine
P19 | NOT_STARTED    |  0/ 5 | Audit Trail
P20 | NOT_STARTED    |  0/ 5 | Quality & AI Evaluation Hardening
P21 | NOT_STARTED    |  0/ 7 | Security, Operations & Production Readiness
P22 | NOT_STARTED    |  0/ 5 | First Real System Test: 3-5 Markets
P23 | NOT_STARTED    |  0/ 5 | Calibration Run: Approximately 10 Markets
P24 | NOT_STARTED    |  0/ 7 | Full Market Discovery, Validation & Final Selection

Autonomous functional build track (new scope from the 2026-09-09 specification;
A17.5 added 2026-09-10 under explicit owner request, see its checkpoint entry):
A   | IN_PROGRESS    | 13/21 | CLI-first Autonomous Research Build

A01-A20 (plus A17.5) are counted in the overall checkpoint total but are not an
additional original guide phase. This denominator change adds 20 source-defined
functional tasks, plus one more (A17.5) added 2026-09-10, without deleting,
renumbering, or silently accepting any P00-P24 work.

AUTONOMOUS FUNCTIONAL BUILD CHECKPOINTS
------------------------------------------------------------------------
[x] A01 Autonomous specification/configuration: add the execution-mode document,
formula configuration v1, strict market YAML schema, method source profiles,
version/hash loaders, and deterministic shared math tests.

[x] A02 Implement a LocalArtifactStore adapter for raw, normalized, evidence,
metrics, report, and lineage outputs without duplicating business logic.

[x] A03 Implement the one-command ResearchRunner CLI with method selection,
resume/idempotency, manifests, budgets, and fail-soft partial completion.

[x] A04 Add deterministic source-discovery queries and an optional Brave Search
adapter with explicit policy, key, budget, and provenance controls.

[x] A05 Add fixture-backed Census CBP, BLS, BEA, SEC, and SAM.gov adapters plus
credential-gated live smoke tests.

[~] A06 Add permitted YouTube, Google Places, approved Reddit, RSS/Atom, and
static-HTML adapters with source-family dedupe and persona-aware normalization.

[~] A07 Add a Google Ads Keyword Planner adapter, cache, historical metrics,
CPC/competition handling, and the versioned intent taxonomy.

[ ] A08 Add a licensed firmographic/contact provider port and adapter profile for
target-account, buyer-identification, and contact-coverage metrics.

[~] A09 Add an LLM provider port and OpenAI Responses API Structured Outputs
adapter; keep model choices and all business scoring outside the adapter.

[x] A10 Add exact-span and numeric-grounding validation, high-impact second-pass
verification, contradiction preservation, and prompt-injection controls.

[x] A11 Build the M2 pain extraction, embeddings, near-dedupe, clustering,
cluster calculation, persona separation, and report section.

[x] A12 Build exact M1/M4/M5/M6/M7 source-to-metric transformations and v1
formula calculators with golden/UNKNOWN tests.

[x] A13 Build evidence-grounded workflow reconstruction, bottleneck metrics,
and the exact M3 formula.

[x] A14 Build capability-registry ingestion, integration discovery, complexity,
and the exact deterministic M9 fit formula.

[x] A15 Build confidence/completeness, minimum samples, method statuses, UNKNOWN
rules, contradiction effects, and automatic veto detection.

[x] A16 Build approved overall weighting, fixed gate profiles, immutable score
snapshots, deterministic recommendations, and compatibility checks.

[x] A17 Generate report.json, HTML, PDF, CSV source manifest, score/confidence
breakdowns, unknowns, raw/normalized/evidence/metrics outputs, and formula appendix.
PDF explicitly deferred (see 2026-09-10 AUTONOMOUS-026 entry): it needs a new,
unapproved rendering dependency; HTML is print-to-PDF ready in any browser today.

[~] A17.5 Free-Source Real Market Integration. Added 2026-09-10 by explicit
owner request after Gemini and YouTube Data API credentials were supplied
locally; denominator change recorded below (171 -> 172 checkpoints; A track
20 -> 21). Wire real free/zero-cost sources into the existing calculators
and produce the first genuine source-backed market report at $0 paid-data
cost: a live Gemini analysis provider (relevance/persona/pain/workflow/
competitor/intent/evidence classification and cluster naming only, never
scores/confidence/ROI/rankings/gates/vetoes), Gemini Search-grounded
discovery (discovery-only until the original source is fetched/verified --
Gemini prose is never itself evidence), a live YouTube Data API collector
for M2/M3/M5, a keyless USAspending adapter for M4/M5 (or NOT_RELEVANT,
never a penalty), an O*NET downloadable-dataset bootstrap/importer for M3
occupation/task/workflow mapping, a targeted Common Crawl retriever
(indexed-record range fetch only, never full WARC/archive downloads) as a
live-fetch fallback, extending the existing RSS/Atom/static-HTML crawler
under the same source-policy gate, a provider-independent source/evidence
fallback registry, Tier A-E deterministic source-quality classes (Tier E
never directly scores), M6 free-mode search-intent evidence, and the
RiskAdjustedMethodScore secondary display metric. M8/M10 stay UNOBSERVED
until real interviews/campaigns exist. First real run targets one
calibration market, not a 30-market batch. Preserves every OBSERVED/
DERIVED/ESTIMATED_BY_APPROVED_RULE/UNKNOWN/CONFLICTED/UNOBSERVED/
NOT_RELEVANT/SOURCE_UNAVAILABLE/SOURCE_UNAVAILABLE_QUOTA distinction; no
synthetic fixture may enter a real market report.

[ ] A18 Build budget- and compliance-gated automated M8 research invitations,
interviewer/landing flow, respondent qualification, and exact M8 calculation.

[ ] A19 Build explicitly budget-enabled M10 experiment adapters, landing/CRM
telemetry, hard spending stops, and exact M10 calculation.

[ ] A20 Run three materially different markets through M1-M7/M9; enable M8/M10
only for finalists with separately configured credentials, consent, and budget.

5. PHASE CHECKPOINTS
========================================================================

P00 - FREEZE THE RESEARCH SPECIFICATION
------------------------------------------------------------------------
Status: DONE
Completed checkpoints: 11/11
Dependencies: Supplied development guide; owner review of the drafted specifications.
Exit condition: Specifications, evidence lineage, responsibilities, and architecture are reviewed; unresolved numerical rules are explicitly deferred with owners.

[x] P00-01 Draft product purpose, users, workflow, stages, MVP boundary, and acceptance invariants. Evidence: docs/PRODUCT.md.

[x] P00-02 Document all ten methodologies, automation/human responsibilities, and their approved overall weights. Evidence: docs/RESEARCH_METHODOLOGY.md.

[x] P00-03 Draft component boundaries, monorepo, asynchronous processing, and deployment architecture. Evidence: docs/ARCHITECTURE.md.

[x] P00-04 Draft logical data model, versioning, tenant boundaries, and score-to-source evidence lineage. Evidence: docs/DATA_MODEL.md.

[x] P00-05 Document scoring, confidence, gaps, gate profiles, vetoes, and human overrides; flag unapproved numerical details. Evidence: docs/SCORING.md.

[x] P00-06 Document permitted collection, source registration, rate limits, uploads, deduplication, and retention. Evidence: docs/SCRAPING_POLICY.md.

[x] P00-07 Define AI responsibilities, extraction contracts, prompt/model versioning, validation, and review. Evidence: docs/AI_PIPELINE.md.

[x] P00-08 Define labelled evaluation, test layers, provisional quality gates, security, and privacy controls. Evidence: docs/EVALUATION.md and docs/SECURITY.md.

[x] P00-09 Create project instructions, documentation index, and detailed Phase 1 checklist. Evidence: AGENTS.md, README.md, docs/phases/PHASE_1_CHECKLIST.md.

[x] P00-10 Validate initial document coverage, ten methods, 100% overall weights, and local links; package documentation-only Phase 0 handoff. Evidence: prior verification and outputs/mask-market-intelligence-phase-0.zip (relative to workspace).

[x] P00-11 Baseline accepted for Phase 1 by the user's instruction to move onwards and start the new phase with AGENTS.md/progress.txt. Numerical engineering proposals/rubric activation remain explicitly deferred under D02-D06, not implicitly approved.


P01 - REPOSITORY & DEVELOPMENT ENVIRONMENT
------------------------------------------------------------------------
Status: IN_PROGRESS
Completed checkpoints: 5/10
Dependencies: P00-11; detailed checklist: docs/phases/PHASE_1_CHECKLIST.md.
Exit condition: A clean Windows environment starts web/API/PostgreSQL/pgvector/native worker; migrations, durable jobs, tests, lint, and type checks pass.

[x] P01-01 Record and pin runtime, package manager, queue, authentication approach, storage abstraction, and test-tool choices. Evidence: docs/decisions/0001-phase-1-baseline.md and dependency manifests; official registry checks passed.

[x] P01-02 Initialize Git and monorepo directories; add ignore rules, lockfiles, environment examples, and root task commands. Evidence: frozen pnpm/uv installs, generated .env ignored, root quality commands and explicit integration preflight verified. No remote or branch protection configured.

[x] P01-03 Scaffold Next.js App Router/TypeScript web skeleton and typed API boundary with a minimal health/status page. Evidence: 11 web tests, lint/types/contracts, production build, and local HTTP unavailable-dependency rendering passed. Shared format automation is tracked under P01-08.

[x] P01-04 Scaffold FastAPI settings, safe errors, structured logs, OpenAPI, liveness/readiness, and test setup. Evidence: API/contract unit tests, Ruff, mypy pass; real PostgreSQL/worker integration is separately pending P01-05/P01-06/P01-10.

[x] P01-05 Configure PostgreSQL/pgvector, SQLAlchemy, Alembic, initial infrastructure migration, and vector round-trip test. Supabase PostgreSQL 17 is live under D14 at Alembic 0006 with pgvector 0.8.2; restricted runtime/migration identities, TLS, bounded pooling, private schema, vector operation, grants, and safe hosted acceptance pass.

[~] P01-06 Configure the PostgreSQL-backed native Windows worker, typed jobs, status reporting, leases/heartbeats, idempotency, bounded retries, recovery, and graceful shutdown. Generic queue schema/adapter, Alembic 0003, native concurrency-1 runner, safe retry/logging behavior, and offline/unit contracts pass. Real PostgreSQL concurrent-claim, recovery, round-trip, and operator shutdown/restart acceptance remain D10.

[~] P01-07 Provide native Windows web/API/worker commands, PostgreSQL/worker readiness checks, and safe startup/shutdown instructions. All commands and PostgreSQL/worker preflight are implemented and unit-tested; full clean Windows startup/shutdown acceptance remains D10.

[~] P01-08 Configure unit/integration tests, lint, type checks, contract checks, and secret scanning. Windows quality/integration workflows and pinned checksum-verified PowerShell Gitleaks script are implemented; 141 local tests and static gates pass. Live scanner execution, clean frozen install, build/audits, hosted run, and branch protection remain unverified/deferred as applicable.

[x] P01-09 Document reproducible setup, commands, migrations, troubleshooting, service ports, and safe shutdown. Evidence: docs/DEVELOPMENT.md, ADR 0001, README links verified, detailed checklist updated; full clean setup execution remains P01-10.

[!] P01-10 Verify clean Windows startup, migration, web-to-API connectivity, worker round-trip, failure states, and all quality commands. Historical web/API unavailable-state HTTP smoke passes; queue replacement and complete native PostgreSQL/startup/operator acceptance remain.


P02 - AUTHENTICATION, MARKET REGISTRY & RESEARCH WORKSPACE
------------------------------------------------------------------------
Status: IN_PROGRESS
Completed checkpoints: 0/7
Dependencies: Phase 1; approved organization and role model.
Exit condition: An authenticated researcher can create a market and see an empty ten-method workspace without tenant leakage.

[x] P02-01 Add organization, user, role, market, definition-version, hypothesis, and research-plan migrations. Alembic 0001-0006 is applied to Supabase; hosted reflection, grants, private-schema isolation, append-only definitions, and tenant constraints pass. No market business endpoint is enabled.

[~] P02-02 Implement authentication/session handling and server-enforced organization/role permissions. Existing local Argon2id credentials, hashed PostgreSQL sessions, login/rotation/revocation, lockout, CSRF, and bootstrap remain. An explicit auth-route toggle, current-session endpoint, persisted-CSRF mutation guard, tenant-derived recent-auth role administration, and Alembic 0006 role-change audit are partially implemented and focused-tested. Documentation, repository/live tests, Supabase migration/bootstrap, browser acceptance, and final route activation remain; market routes are still absent.

[ ] P02-03 Implement market create/read/update/archive APIs and validated definitions, owner, reviewer, and hypotheses.

[ ] P02-04 Build registry and creation UI for market, submarket, geography, company band, buyer, owner, and reviewer.

[ ] P02-05 Create empty ten-method scorecard and overview for stage, status, completeness, gaps, and next action.

[ ] P02-06 Add explicit stage/status interfaces and audit events; do not allow uncontrolled direct stage transitions.

[ ] P02-07 Verify market creation, edits/versioning, authorization, tenant isolation, validation, and empty states.


P03 - EVIDENCE & SOURCE ENGINE
------------------------------------------------------------------------
Status: IN_PROGRESS
Completed checkpoints: 0/8
Dependencies: Phase 2; collection/security policies.
Exit condition: At least two offline test collectors feed the same lineage-preserving evidence pipeline with duplicate-safe persistence.

[ ] P03-01 Add source/policy-version, collector-run, job, upload, raw-document, normalization, duplicate-link, and evidence migrations.

[~] P03-02 Implement source-policy registration/approval and block disallowed, expired, or unreviewed collection.

[~] P03-03 Implement common discover/fetch/parse/normalize/persist contracts and typed normalized-document schema.

[ ] P03-04 Implement safe analyst upload/manual capture with provenance, checksums, file validation, and access/retention classes.

[~] P03-05 Implement versioned normalization, exact deduplication, duplicate provenance, and idempotent persistence.

[~] P03-06 Implement per-source limits, capped backoff, circuit breakers, cancellation, partial results, and job monitoring.

[ ] P03-07 Implement evidence browse/detail APIs and raw-source drill-down with organization/access filtering.

[~] P03-08 Verify two fixture-backed test collectors, parsers, lineage, retry behavior, duplicate counts, upload failure handling, and migrations.


P04 - SENTIMENT & COMMERCIAL PAIN INTELLIGENCE
------------------------------------------------------------------------
Status: NOT_STARTED
Completed checkpoints: 0/9
Dependencies: Phase 3; approved source access and AI processing policy.
Exit condition: Reviewed pain intelligence is traceable, clustered, evaluated, and not used for scoring until its release gates pass.

[ ] P04-01 Select and approve two or three initial permitted sources; implement production collector adapters with fixtures.

[ ] P04-02 Implement versioned relevance classification and routing for relevant, possible, and irrelevant documents.

[ ] P04-03 Implement persona classification without mixing operator, customer, employee, and vendor evidence.

[ ] P04-04 Implement strict commercial pain extraction contracts, evidence-span validation, bounded repair, and quarantine.

[ ] P04-05 Add analysis/pain/review persistence with model/prompt/schema/taxonomy versions and immutable history.

[ ] P04-06 Implement versioned embeddings, near-duplicate detection, semantic clustering, memberships, and outlier handling.

[ ] P04-07 Build pain review/cluster UI showing sources, samples, severity, urgency, economics, intent, contradictions, and proposed names.

[ ] P04-08 Create a human-labelled 100-200-record gold set, reproducible make eval-ai, metrics, slices, and reviewed release decision.

[ ] P04-09 Verify ingestion-to-reviewed-cluster flow, rejected/duplicate exclusion, privacy, failed jobs, evaluation thresholds, and no AI scoring.


P05 - QUANTITATIVE MARKET INTELLIGENCE (M1)
------------------------------------------------------------------------
Status: NOT_STARTED
Completed checkpoints: 0/5
Dependencies: Phases 3-4; M1 normalization approval is required before computed M1 scoring.o the next phase 

Exit condition: Comparable, sourced quantitative records and serviceable-buyer estimates can be reviewed without invented values.

[ ] P05-01 Approve M1 units, source periods, market boundaries, normalization rules, and proposed subweight configuration or keep computed scoring disabled.

[ ] P05-02 Add quantitative metric/schema/migration support for counts, company sizes, revenue, growth, geography, and fragmentation.

[ ] P05-03 Implement validated CSV/Excel import and permitted API adapters only where practical.

[ ] P05-04 Build quantitative views with serviceable-business estimates, provenance, assumptions, ranges, and contradictions.

[ ] P05-05 Verify import validation, units/populations, duplicate records, unknowns, evidence links, and approved deterministic transformations.


P06 - ECONOMIC PAIN ENGINE (M4)
------------------------------------------------------------------------
Status: NOT_STARTED
Completed checkpoints: 0/5
Dependencies: Evidence engine and market definitions; approved ROI input/denominator conventions.
Exit condition: Every economic value is sourced or explicitly estimated/unknown; ROI calculations are reproducible and reviewed.

[ ] P06-01 Define economic evidence categories, currencies/periods, ranges, estimation labels, and versioned ROI calculation conventions.

[ ] P06-02 Add economic-evidence and ROI-scenario persistence/APIs with explicit source/interview links.

[ ] P06-03 Implement observed/estimated/interview-confirmed/unknown input capture and assumption approval.

[ ] P06-04 Implement deterministic potential value, value-to-price ratio, payback, annual net benefit, and sensitivity views.

[ ] P06-05 Verify units, non-overlapping cost categories, missing values, zero denominators, boundary cases, evidence, and review.


P07 - COMPETITIVE INTELLIGENCE (M5)
------------------------------------------------------------------------
Status: NOT_STARTED
Completed checkpoints: 0/5
Dependencies: Evidence engine; permitted competitor/review sources.
Exit condition: Researchers can compare evidenced alternatives, pricing labels, gaps, and switching friction.

[ ] P07-01 Add competitor and finding schemas/migrations for AI vendors, vertical SaaS, agencies, consultancies, BPO, and DIY.

[ ] P07-02 Implement manual/import collection for target buyers, problems, features, integrations, positioning, and case studies.

[ ] P07-03 Implement source-grounded extraction/summaries with exact/starting-at/estimated/unknown pricing labels.

[ ] P07-04 Build competitor landscape/detail views with strengths, complaints, weaknesses, substitutes, and evidence review.

[ ] P07-05 Verify unsupported pricing is rejected, findings preserve contradiction/provenance, and tenant/review rules hold.


P08 - SEARCH & BUYING INTENT (M6)
------------------------------------------------------------------------
Status: NOT_STARTED
Completed checkpoints: 0/5
Dependencies: Evidence engine; licensed/export/API access where applicable.
Exit condition: Search demand is segmented by intent, source, geography, and time with auditable numerical metrics.

[ ] P08-01 Add keyword schemas/migrations for volume, trend, CPC, competition, country, date, and source.

[ ] P08-02 Implement manual/CSV imports and approved API adapters with units, period, and data-source validation.

[ ] P08-03 Implement/evaluate intent taxonomy: problem, informational, solution, commercial, comparison, transactional, and switching.

[ ] P08-04 Build demand/trend views with deterministic aggregation and explicit incomplete/source-overlap caveats.

[ ] P08-05 Verify intent boundary cases, seasonality/period handling, duplicate keywords, absent metrics, and source drill-down.


P09 - BUYER ACCESSIBILITY & CHANNEL FIT (M7)
------------------------------------------------------------------------
Status: NOT_STARTED
Completed checkpoints: 0/5
Dependencies: Evidence engine and market registry; no outreach is authorized merely by this roadmap.
Exit condition: Reachable economic buyers, viable channels, and procurement friction are evidenced rather than assumed.

[ ] P09-01 Add buyer persona/stakeholder and channel schemas/migrations.

[ ] P09-02 Implement economic buyer, champion, technical, and procurement stakeholder mapping.

[ ] P09-03 Capture account reach, contact-data availability, sales cycle, procurement, channel diversity, and Meta suitability evidence.

[ ] P09-04 Build channel comparison and deterministic coverage views with unknowns, access restrictions, and cost assumptions.

[ ] P09-05 Verify counts, buyer-role evidence, privacy, inaccessible-buyer flags, review, and channel metric calculations.


P10 - WORKFLOW INTELLIGENCE (M3)
------------------------------------------------------------------------
Status: NOT_STARTED
Completed checkpoints: 0/5
Dependencies: Evidence engine and economic module; interview validation is completed through Phase 11.
Exit condition: Versioned workflows expose evidenced bottlenecks; AI suggestions cannot become validated workflows by themselves.

[ ] P10-01 Add versioned workflow-map/step schemas, evidence links, ownership, and review state.

[ ] P10-02 Build human workflow editor for roles, systems, inputs/outputs, frequency, handling/wait times, failures, and consequences.

[ ] P10-03 Implement optional source-grounded AI workflow proposals without automatic approval.

[ ] P10-04 Implement deterministic bottleneck/value calculations and primary-research validation state.

[ ] P10-05 Verify sequence/versioning, units, assumptions, evidence, unvalidated-state warnings, and researcher/reviewer boundaries.


P11 - PRIMARY CUSTOMER RESEARCH (M8)
------------------------------------------------------------------------
Status: NOT_STARTED
Completed checkpoints: 0/5
Dependencies: Evidence engine, private-data controls, and an approved interview process.
Exit condition: Qualified, consented interviews can confirm or contradict desk research with inspectable transcript context.

[ ] P11-01 Add interview/finding schemas, participant-fit metadata, consent/authority, private storage, and retention support.

[ ] P11-02 Build interview creation, transcript upload, notes, role tagging, and restricted access.

[ ] P11-03 Implement/evaluate extraction of confirmation/rejection, urgency, spend, workflow, authority, objections, and willingness.

[ ] P11-04 Build desk-research versus interview comparison with explicit denominators and disagreement flags.

[ ] P11-05 Verify consent/access, span fidelity, participant qualification, correction history, contradictions, and deletion inheritance.


P12 - MASK AI & PRODUCTIZATION FIT (M9)
------------------------------------------------------------------------
Status: NOT_STARTED
Completed checkpoints: 0/4
Dependencies: Evidence engine, economic/workflow/competitive evidence, and technical/business reviewers.
Exit condition: Light and full fit assessments are leadership-reviewed and high ratings have written supporting evidence.

[ ] P12-01 Add light/full assessment schema and criteria for technical/integration fit, complexity, standardization, recurring revenue, proof, and expansion.

[ ] P12-02 Build leadership assessment UI with written rationale, evidence attachments, and unknown/assumption states.

[ ] P12-03 Implement technical/business review, version history, and completeness checks for high ratings.

[ ] P12-04 Verify permissions, missing rationale, light-versus-full profile separation, evidence links, and scoring-input eligibility.


P13 - DETERMINISTIC SCORING & CONFIDENCE ENGINE
------------------------------------------------------------------------
Status: NOT_STARTED
Completed checkpoints: 0/7
Dependencies: Structured method inputs and approvals D02-D06 below; M10 scoring remains unavailable until Phase 17 evidence exists.
Exit condition: Scores, confidence, overrides, and immutable snapshots are reproducible; unknown rubrics cannot produce invented scores.

[ ] P13-01 Approve and version applicable method rubrics, normalizers, subweights, minimums, manual-review rules, and confidence dimensions.

[ ] P13-02 Add scoring configuration/rubric, method-score, evidence-join, override, confidence, and market-snapshot migrations.

[ ] P13-03 Implement pure backend method calculations and fixed overall weights M1-M10 totaling 100%; store full breakdowns.

[ ] P13-04 Implement computed versus reviewed score workflows with explicit overrides, reviewer/rationale/evidence/timestamp.

[ ] P13-05 Implement deterministic confidence indices/labels, manual confidence challenges, completeness, and duplicate/source-diversity controls.

[ ] P13-06 Implement complete overall scores, fixed-scope gate scores, incompatible-snapshot warnings, and deterministic rankings.

[ ] P13-07 Verify all formulas, weight sums, boundaries, nulls, overrides, reproducibility, tenant isolation, immutable history, and no LLM calls.


P14 - EVIDENCE GAP DETECTION
------------------------------------------------------------------------
Status: NOT_STARTED
Completed checkpoints: 0/4
Dependencies: Method requirements, review states, scoring configuration, and confidence/completeness conventions.
Exit condition: Every market shows proven, weak, and missing evidence with a deterministic next research action.

[ ] P14-01 Version evidence requirement rules by methodology and gate, with sample/quality/recency/source checks.

[ ] P14-02 Implement deterministic proven/weak/missing/not-applicable gap calculation and result persistence.

[ ] P14-03 Build prioritized next actions and gap views; AI may explain but must not select actions.

[ ] P14-04 Verify all gap states, approved exceptions, recalculation, gate/veto priorities, counts, and evidence traceability.


P15 - STAGE-GATE ENGINE
------------------------------------------------------------------------
Status: NOT_STARTED
Completed checkpoints: 0/6
Dependencies: Phases 13-14; minimum veto/audit controls are required here; full Gate 3/4 acceptance awaits Phase 17.
Exit condition: Gate recommendations explain every condition; only an authorized human decision changes a stage.

[ ] P15-01 Version four gate profiles, fixed method sets, thresholds, confidence rules, completeness checks, and veto requirements.

[ ] P15-02 Implement Gate 1: 6.0 indicative score, required desk-research methods/light M9, and no open confirmed critical veto.

[ ] P15-03 Implement Gate 2: 7.0+, at least Medium confidence, full workflow/interview/product-fit evidence.

[ ] P15-04 Implement Gate 3: 7.5+ and approved measurable behavior requirements; return not_ready without sufficient M10 evidence.

[ ] P15-05 Implement Gate 4: preferred 8.0, High confidence, required final evidence, founder selection/exception records.

[ ] P15-06 Verify not_ready/blocked/eligible/fail/founder-review results, score boundaries, hold/reject/advance/select transitions, and audit.


P16 - MARKET COMPARISON DASHBOARD
------------------------------------------------------------------------
Status: NOT_STARTED
Completed checkpoints: 0/7
Dependencies: Registry, evidence, method modules, scoring, confidence, gaps, and gate recommendation APIs.
Exit condition: Users can inspect a numeric comparison and navigate every conclusion back to its evidence.

[ ] P16-01 Build leaderboard with reviewed scores, confidence, stage, completion, veto state, and compatible-comparison rules.

[ ] P16-02 Build market-detail navigation for all method tabs, including clear unimplemented/unknown/empty states.

[ ] P16-03 Build evidence explorer with source, persona, date, pain, method, sentiment, confidence, and access filters.

[ ] P16-04 Build pain intelligence visualizations with frequency, economics, intent, source mix, trends, and supporting context.

[ ] P16-05 Build side-by-side ten-method comparison with actual numbers; charts are supplemental.

[ ] P16-06 Build score calculation/approval drill-down, gaps, next action, red flags, and stage-decision surfaces.

[ ] P16-07 Verify end-to-end UI flows, accessibility, responsive layouts, loading/errors, numeric fidelity, evidence links, and tenant scope.


P17 - REAL-WORLD MARKET VALIDATION (M10)
------------------------------------------------------------------------
Status: NOT_STARTED
Completed checkpoints: 0/5
Dependencies: Scoring/gates and experiment approval; external spend, publication, or outreach needs explicit authorization.
Exit condition: Manual experiment inputs yield reliable funnel/unit-economic metrics and traceable behavioral evidence.

[ ] P17-01 Approve outcome definitions, attribution windows, denominator conventions, M10 rubric, and strong-behavior thresholds.

[ ] P17-02 Add experiment/metric schemas and manual entry/import for offer, message, channel, dates, spend, funnel, revenue, and gross profit.

[ ] P17-03 Implement deterministic CTR, CPL, CPQL, cost/meeting, opportunity/close rates, CAC, value ratios, and sales-cycle calculations.

[ ] P17-04 Build experiment review/comparison and paid-pilot/pricing/retention/expansion evidence capture.

[ ] P17-05 Verify source totals, zero denominators, cohort/currency consistency, approved M10 scoring, and Gate 3/4 end-to-end eligibility.


P18 - RED FLAG & VETO ENGINE
------------------------------------------------------------------------
Status: NOT_STARTED
Completed checkpoints: 0/4
Dependencies: Minimum blocking controls already exist before gate use; this phase completes dedicated lifecycle/UI coverage.
Exit condition: A high score cannot conceal an unresolved critical veto or an undocumented founder exception.

[ ] P18-01 Complete veto taxonomy and suspected/confirmed/resolved/accepted-exception lifecycle with severity, evidence, and owner.

[ ] P18-02 Build investigation/resolution UI and deterministic founder-review/blocking rules.

[ ] P18-03 Implement founder exceptions with rationale, evidence, gate scope, review/expiry date, and visible retained veto.

[ ] P18-04 Verify lifecycle transitions, expired exceptions, suspected-versus-confirmed handling, review priorities, and gate integration.


P19 - AUDIT TRAIL
------------------------------------------------------------------------
Status: NOT_STARTED
Completed checkpoints: 0/5
Dependencies: Audit events are added with every earlier mutation; this phase completes query, coverage, and tamper-resistance checks.
Exit condition: The team can reconstruct why a market or score changed without exposing secrets or losing historical evidence.

[ ] P19-01 Complete append-only audit schema, event taxonomy, correlation IDs, actors/roles, and safe before/after references.

[ ] P19-02 Audit score/confidence/rubric/prompt/source/review/stage/experiment/veto/founder changes and evidence retention actions.

[ ] P19-03 Build authorized market/record history and reviewable decision reports with exact version/evidence references.

[ ] P19-04 Implement audit export/access controls, retention, immutability safeguards, and sensitive-payload redaction.

[ ] P19-05 Verify event coverage, ordered history, failed/unauthorized actions, source changes, and rejection/selection reconstruction.


P20 - QUALITY & AI EVALUATION HARDENING
------------------------------------------------------------------------
Status: NOT_STARTED
Completed checkpoints: 0/5
Dependencies: Initial labelled evaluation already blocks unsafe Phase 4 use; this phase extends coverage across the platform.
Exit condition: Every production analysis version has reproducible evidence of quality and an approved release decision.

[ ] P20-01 Expand versioned gold/challenge datasets across methods, sources, personas, markets, and sensitive-data classes.

[ ] P20-02 Implement complete precision/recall/agreement/MAE/kappa/span metrics, slices, uncertainty, and regression reports.

[ ] P20-03 Evaluate clustering coherence, duplicate dominance, outliers, stability, and name fidelity.

[ ] P20-04 Enforce prompt/model/taxonomy/schema-change regression gates and critical-error human review.

[ ] P20-05 Verify release/rollback/reanalysis procedures, drift/correction monitoring, make eval-ai reproducibility, and reporting.


P21 - SECURITY, OPERATIONS & PRODUCTION READINESS
------------------------------------------------------------------------
Status: NOT_STARTED
Completed checkpoints: 0/7
Dependencies: Security is incremental from Phase 1; production provider/account choices and deployment require approval.
Exit condition: An approved deployment is authenticated, tenant-isolated, observable, recoverable, and has no unresolved critical findings.

[ ] P21-01 Complete threat model, role matrix, service identities, authentication hardening, and tenant-isolation verification.

[ ] P21-02 Harden uploads, HTML rendering, SSRF/collector egress, rate limiting, prompt injection, and secret handling.

[ ] P21-03 Verify private transcript/object access, retention/deletion propagation, consent, and provider processing controls.

[ ] P21-04 Configure encrypted production services, migrations, least-privilege credentials, and environment separation.

[ ] P21-05 Configure backups, restore drill, health/metrics/alerts, queue monitoring, and incident owners/runbooks.

[ ] P21-06 Run dependency/host-runtime/security testing, log/audit redaction checks, performance/reliability tests, and remediation. Packaging requirement updated by D12; hardening is not waived.

[ ] P21-07 With explicit approval, deploy a controlled pilot environment and verify rollback, migrations, access, and end-to-end smoke tests.


P22 - FIRST REAL SYSTEM TEST: 3-5 MARKETS
------------------------------------------------------------------------
Status: NOT_STARTED
Completed checkpoints: 0/5
Dependencies: Gate 1 MVP acceptance plus security/audit/evaluation baseline; approved research sources and market list.
Exit condition: A documented real-data pilot proves collection, interpretation, scoring fairness, and workflow usability before scale.

[ ] P22-01 Select 3-5 materially different markets and freeze a common definition/profile/configuration and research plan.

[ ] P22-02 Run M1, M2, M4, M5, M6, M7, and light M9 with the same collection/review rules.

[ ] P22-03 Measure collector reliability, source access, relevance/extraction accuracy, clusters, evidence gaps, and research effort.

[ ] P22-04 Compare scored evidence and reviewer judgement; inspect misleading rankings, missing fields, and dashboard usability.

[ ] P22-05 Fix/retest pilot defects and record a human go/no-go for calibration with acceptance evidence.


P23 - CALIBRATION RUN: APPROXIMATELY 10 MARKETS
------------------------------------------------------------------------
Status: NOT_STARTED
Completed checkpoints: 0/5
Dependencies: Successful technical pilot and approved calibration protocol.
Exit condition: A reviewed scoring configuration supports fair cross-market comparison without favoring a preferred industry.

[ ] P23-01 Select approximately 10 diverse markets and run the identical frozen research methodology.

[ ] P23-02 Collect independent researcher/reviewer assessments and compare with computed scores/confidence.

[ ] P23-03 Analyze systematic severity, search-demand, source/sample, economic, and accessibility bias.

[ ] P23-04 Approve justified normalization/rubric updates, version them, and recalculate/backtest all comparable markets.

[ ] P23-05 Record calibration findings, regression results, unresolved risks, and approval to scale.


P24 - FULL MARKET DISCOVERY, VALIDATION & FINAL SELECTION
------------------------------------------------------------------------
Status: NOT_STARTED
Completed checkpoints: 0/7
Dependencies: Approved calibration; explicit authorization and budgets for any live outreach, publication, or paid tests.
Exit condition: One primary market is selected by an authorized human using a fully traceable final report and buyer behavior.

[ ] P24-01 Load 30-50 candidate markets and freeze the common research plan, scoring configuration, and evidence policy.

[ ] P24-02 Run/review Gate 1 and document advancement of approximately 10-15 markets plus every hold/rejection.

[ ] P24-03 Complete deep research/interviews/workflows/economics and Gate 2 to narrow to approximately 5.

[ ] P24-04 Conduct authorized outbound, landing-page, message, pricing, demo, and small paid tests; evaluate Gate 3 to reach 2-3 finalists.

[ ] P24-05 Gather paid-pilot, pricing, CAC, sales-friction, margin, productization, retention, and expansion evidence for finalists.

[ ] P24-06 Produce the final comparative report with all ten scores, confidence, evidence, contradictions, gaps, vetoes, and next experiments.

[ ] P24-07 Record founder/reviewer Gate 4 decision and primary-market selection; preserve rejected-market history and operational handoff.

6. RELEASE MILESTONES (ROLLUPS, NOT EXTRA COUNTED CHECKPOINTS)
------------------------------------------------------------------------
R0 - Specification baseline reviewed: P00.
R1 - Local development environment works: P01.
R2 - Secure market/evidence workspace works: P02-P03.
R3 - Reviewed/evaluated pain intelligence works: P04.
R4 - Gate 1 MVP ready: authentication, evidence, 2-3 collectors, evaluated
     pain/embeddings/clustering, quantitative/competitor/search inputs,
     economic/buyer evidence, light product fit, manual reviewed scoring,
     deterministic weighted scoring/confidence, gaps, gate controls,
     leaderboard, evidence drill-down, and baseline security/audit.
     Missing advanced method automation does not imply a full-platform
     release. Any earlier MVP cut must explicitly mark its partial scope.
R5 - Full research and validation platform ready: required P05-P21 work.
R6 - Technical pilot accepted: P22.
R7 - Calibration accepted: P23.
R8 - Evidence-backed primary market selected: P24.

7. OPEN DECISIONS / DEPENDENCIES
------------------------------------------------------------------------
These are pending decisions, not claims that a current implementation failed.
The guide refers to two original MASK AI research documents; only the
development guide was supplied. If the originals define missing rubrics,
use those instead of inventing policy.

D01 | Owner: MASK AI project owner
    | Needed by: P00-11
    | CLOSED FOR PHASE 1: user explicitly authorized starting the next phase
      using the existing instructions/tracker. Architecture baseline adopted.
      Numerical proposals remain pending under D02-D06 before activation.

D02 | Owner: Research lead + reviewer
    | Needed by: computed M1/M2 activation, P05-01/P13-01
    | Approve M1 normalizers, M2 frequency denominator, duplicate handling,
      cluster-to-market aggregation, and minimum samples/subweights.

D03 | Owner: Methodology/business lead + reviewer
    | Needed by: P13-01 and method-specific computed scoring
    | Supply/approve deterministic M3-M10 rubrics. No AI-invented scores.

D04 | Owner: Research lead + reviewer
    | Needed by: P13-01/P13-05 and automated completeness/gate use
    | Approve confidence dimension transforms, minimum evidence, recency,
      source-family grouping, and any allowed exception rules.

D05 | Owner: Founder + sales/research leads
    | Needed by: P17-01 and Gate 3/4 readiness
    | Define strong behavioral signals, qualified outcomes, attribution,
      pricing/pilot/unit-economics requirements, and M10 scoring thresholds.

D06 | Owner: Research/technical leads
    | Needed by: scoring/AI release activation
    | Approve provisional engineering gates, fixed-profile gate-score
      conventions, override threshold, and release-risk acceptance process.

D07 | Owner: Technical lead
    | Needed by: P01-01 and production preparation
    | PHASE 1 BASELINE ADOPTED then superseded in part by D13: runtime/tool
      pins remain; PostgreSQL queue and local session directions replace Celery/OIDC.
      The S3-compatible storage boundary remains optional behind a local adapter.
      Provider accounts and production deployment target remain pending.
      Infrastructure choices do not permit changing research policy.

D08 | Owner: Data/source policy owner
    | Needed by: live collection and AI provider use
    | Approve each source, rights, credentials, rate/retention limits,
      restricted-data processing, and first 2-3 production collectors.

D09 | Owner: MASK AI project owner/founder
    | Needed by: P21 deployment and P22-P24 research/experiments
    | Supply approved markets, participants, provider accounts, budgets,
      and explicit authorization for publication, outreach, or paid tests.
      This tracker alone grants no external-action authority.

D10 | Owner: Project owner/system administrator; technical lead for verification
    | Needed by: P01-05/P01-06/P01-07/P01-08/P01-10
    | Updated by D13: provide one isolated native Windows PostgreSQL/pgvector
      instance with separate restricted application and migration roles; record
      actual versions/readiness. Redis/Linux/WSL/VM/containers are not required.
      Hosted CI is deferred; do not publish code without separate approval.
      Superseded for database hosting by D14; retained as historical context.

D11 | Owner: Project owner + technical lead | APPROVED 2026-09-03
    | Needed by: P02-02 authentication and authenticated market API/UI
    | D13 selects local application accounts with maintained Argon2id password
      hashing and PostgreSQL-backed hashed server sessions for the private Windows
      MVP. Explicit local owner bootstrap, cookies/CSRF, expiry/revocation/logout,
      audit, rate limiting, and live acceptance remain implementation work.
      Optional OIDC/email/social login is deferred; no account was created.

D12 | Owner: Project owner | APPROVED 2026-09-03
    | User explicitly requested removing Docker; it must no longer be required
      by development commands, CI, tests, or current setup instructions.
      D13 retains PostgreSQL/pgvector, authentication, and test safety while
      superseding the interim Redis/Celery/Linux requirement with a Windows worker.
      P01-07 is rescoped in place; all 151 checkpoint IDs/counts are preserved.
      Do not uninstall system software, erase runtime data, or alter Windows PATH.

D13 | Owner: Project owner | APPROVED 2026-09-03
    | The system must run natively on Windows, remain container-free, satisfy the
      supplied development guide without speculative scope, and keep market
      research/model usage lean. Architecture direction is adopted: retain
      PostgreSQL/pgvector, replace Redis/Celery/Linux with a durable PostgreSQL-
      backed Python worker, use local server-side authentication for the initial
      private installation, and enforce explicit per-run collection/model budgets.
      This decision changes infrastructure, not methodology weights, evidence
      lineage, deterministic scoring, authorization, or audit requirements.
      Target architecture/docs are aligned; replacement code and live acceptance
      remain under P01-06/P01-07/P01-08/P01-10 and P02-02.

D14 | Owner: Project owner | APPROVED 2026-09-08
    | The user selected Supabase as the database. Use its managed PostgreSQL and
      pgvector service through TLS from the native Windows API/worker; Docker,
      WSL, Redis, Celery, and a Linux runtime remain excluded. Keep Alembic,
      SQLAlchemy, the durable PostgreSQL queue, tenant-scoped repositories, and
      application-owned local authentication. Do not add Supabase Auth, browser
      database access, public service-role credentials, or a second persistence
      path without a separate requirement. Use distinct runtime and migration
      credentials where the selected Supabase plan permits; keep secrets local.
      Hosted destructive database-cycle tests must target a dedicated disposable
      project/database and are not authorized against shared or production data.

8. DEFERRED / OPTIONAL BACKLOG (NOT IN BASELINE COUNTS)
------------------------------------------------------------------------
- Additional social scrapers and every possible market-data API.
- Meta/Google Ads/CRM/outbound/analytics integrations after manual imports.
- Optional n8n scheduling/notifications/integration workflows.
- Automated outreach or interviews, only after a separately approved scope.
- Fully autonomous workflow mapping and predictive market-success ML.
- Complex multi-agent architecture, custom warehouse, and Kubernetes.
When an item is approved, add stable checkpoints and record the count/scope
change. Core scoring must remain in the application, not orchestration.

9. CHECKPOINT HISTORY (APPEND; DO NOT REWRITE PAST RESULTS)
------------------------------------------------------------------------
2026-09-02 | BASELINE-001 | P00-01 through P00-10 | DONE
Work:
  Prepared 12 Markdown artifacts: AGENTS.md, README.md, eight required
  specification documents, SECURITY.md, and PHASE_1_CHECKLIST.md.
  Packaged the original documentation-only Phase 0 ZIP.
Evidence:
  Files are in this project root and docs/.
Verification:
  Required-doc presence: PASS (8/8).
  Method sections: PASS (10).
  Approved overall weights: PASS (100%).
  README relative document links: PASS (0 broken).
  Initial package implementation files: NONE.
  Application tests/lint/types/migrations: NOT RUN - no application exists.
Limitations:
  Drafting completion is not owner sign-off or scoring-rubric approval.
Next:
  P00-11; then P01-01 after approval.

2026-09-02 23:03 PKT (UTC+05:00) | TRACK-001 | Tracking setup | IN_PROGRESS
Work:
  Added 25-phase roadmap, 151 stable checkpoints, honest baseline counts,
  dependencies, release milestones, decisions, and append-only history.
  Project instruction/index updates and tracker verification are pending.
Verification:
  NOT RUN yet for this tracker.
Limitations:
  Documentation/tracking only; no application implementation authorized.
Next:
  Verify IDs/counts, phase rollup, links, and update instructions; close
  TRACK-001 without counting administrative tracking as product progress.

2026-09-02 23:10 PKT (UTC+05:00) | TRACK-002 | Close TRACK-001 | DONE
Work:
  Completed the canonical tracker and required after-every-checkpoint rule.
  Linked it from README and corrected the unrecorded approval claim there.
Files:
  progress.txt (new), AGENTS.md (updated), README.md (updated).
Verification:
  PowerShell Get-Content + regex checkpoint scan: PASS, 151 unique stable
  IDs, 10 DONE, 141 remaining, 25 phase sections, 25 matching phase rows.
  Sequential-ID and per-phase completed/total comparisons: PASS.
  Summary completed/remaining counts and phase rollups: PASS.
  Get-Content AGENTS.md update-protocol presence check: PASS.
  README relative links checked with Test-Path: PASS, 0 broken.
  Initial validator falsely rejected padded table columns; its whitespace
  pattern was corrected and the full validation rerun passed (exit 0).
  Application tests/lint/types/migrations: NOT RUN - no application exists.
Limitations:
  Tracking setup is not counted as additional product completion.
  No architecture sign-off, scoring-rule approval, or Phase 1 implementation
  is inferred from this tracking request. The original ZIP remains static.
Next:
  P00-11 - record baseline review; then P01-01 after approval.
  On future authorized work, update this file at each checkpoint attempt.

2026-09-02 | PHASE1-START | P00-11 DONE; P01-01 IN_PROGRESS
Work:
  Read project instructions, tracker, architecture, Phase 1 requirements,
  security, evaluation, and infrastructure-related data model.
  Recorded user's authorization to start Phase 1 as baseline acceptance.
Verification:
  Preflight found bundled Node 24.19.0 and Python 3.12.13; Git available.
  Docker CLI/Desktop absent from PATH and standard installation paths.
  Network permission granted for dependency downloads this turn.
Limitations:
  Numerical scoring approvals remain pending. Docker-backed acceptance
  cannot run until a Docker Linux-container environment is available.
Next:
  P01-01 runtime decisions; build/test local scaffold without claiming
  missing Docker integration checks passed.

2026-09-02 | PHASE1-001 | P01-01 DONE; P01-02 IN_PROGRESS
Work:
  Adopted ADR 0001 and pinned Node/Python/pnpm/uv, web/API packages,
  queue, auth/storage boundaries, and test tooling. Initialized Git main.
Files:
  docs/decisions/0001-phase-1-baseline.md; package.json; pyproject.toml;
  apps/api/pyproject.toml; workspace/runtime/environment/ignore files.
Verification:
  Official npm/PyPI registry version checks: PASS. Runtime versions: PASS.
  git init -b main: PASS. uv 0.12.9 installed in workspace tooling only.
Limitations:
  Docker unavailable; dependency locks and infrastructure verification pending.
Next:
  P01-02 dependency installation/locks and cross-platform commands.

2026-09-02 | PHASE1-002 | P01-02/P01-04/P01-05/P01-06 | IN_PROGRESS
Work:
  Added API settings/contracts/health/error handling and protected local
  smoke endpoints, SQLAlchemy infrastructure job model, Celery worker,
  Alembic 0001, and initial unit tests. Starting the web scaffold.
Verification:
  uv sync resolved 60 packages and built mask-api; installation finishing.
  Initial resolver caught Celery incompatibility with redis-py 8; pinned
  redis-py 6.4.0 per Kombu's supported range and resolution succeeded.
Limitations:
  Code is not yet verified; migration/real worker checks require Docker.
Next:
  Web/status and root commands; run unit, lint, types, and integration preflight.

2026-09-02 | PHASE1-003 | P01-03 through P01-08 | IN_PROGRESS
Work:
  Added local Next.js status UI, runtime-validated typed client, component
  tests, Compose/Dockerfiles, restricted DB role initialization, and
  database/worker/failure/recovery integration test specifications.
Verification:
  Initial API suite: 11 PASS. Contract export caught callable dependency
  parameters; fixed via a zero-argument readiness wrapper and added regression.
  Four Docker image tags/digests verified against official registry: PASS.
  pnpm installed packages but refused unapproved unrs-resolver build script;
  inspected its package/postinstall and explicitly approved that dependency.
Limitations:
  Full checks/contract generation rerun pending. Docker tests still blocked.
Next:
  Complete lockfiles, run expanded tests/lint/types/build, and prepare CI/docs.

2026-09-03 | PHASE1-004 | P01-03/P01-04/P01-08/P01-09 | IN_PROGRESS
Work:
  Expanded contract/idempotency tests, generated OpenAPI/TS declarations,
  configured CI and local secret guard, and wrote local development guide.
Verification:
  Backend tests: 19 PASS, 5 real integration tests excluded (not passed).
  Web tests: 11 PASS. TS route/type generation: PASS on first run.
  ESLint 10 failed with React plugin API incompatibility; reverted to the
  previously pinned compatible ESLint 9 and recorded the decision in ADR.
  Python typing gaps corrected; final lint/type/build rerun in progress.
  Corrected setup documentation to pnpm run setup (pnpm setup is a built-in).
Limitations:
  No Docker/CI execution yet; cannot close the phase on offline tests alone.
Next:
  Finish complete local checks and live unavailable-dependency smoke check.

2026-09-03 | PHASE1-005 | P01-04 | DONE
Work:
  Completed typed FastAPI scaffold, startup validation/redaction, structured
  logs, safe readiness/error responses, dev-only authorization, and OpenAPI.
Verification:
  uv run pytest -m "not integration": 20 PASS, 5 integration deselected.
  uv run ruff check .: PASS. uv run mypy apps/api/src workers: PASS (15 files).
  uv run python scripts/check-secrets.py: PASS (narrow local guard only).
Limitations:
  TestClient emits an upstream httpx deprecation warning; tests pass.
  Docker, Podman, and WSL Linux installation are unavailable. Real DB,
  migration, queue/worker, and container tests remain NOT RUN.
Next:
  Finish web lint/build and repository locks; then status-page HTTP smoke.

2026-09-03 00:12 PKT (UTC+05:00) | PHASE1-006 | P01-02/P01-03/P01-07/P01-08 | IN_PROGRESS
Work:
  Completed Next.js production compilation, type generation, and build traces.
  Generated ignored local secrets without printing values or overwriting files.
  Downloaded official standalone Compose v2.40.3 into workspace tooling and
  verified its published SHA256 before execution; no system installation.
Verification:
  pnpm --filter @mask/web build: PASS (/, /api/health, /_not-found).
  docker-compose.exe config --quiet: PASS (configuration syntax only).
  git check-ignore .env: PASS. Tracker: PASS (13/151).
  pnpm schemas:check: FAILED because pnpm forwarded an extra -- to argparse.
  Corrected the root command to invoke the checker directly; rerun pending.
Limitations:
  Standalone Compose is not a Docker engine. Actual container startup,
  migrations, vector round-trip, worker, and recovery remain NOT RUN.
Next:
  Rerun quality commands; smoke-test local production web and API responses.

2026-09-03 00:20 PKT (UTC+05:00) | PHASE1-007 | P01-02 DONE; P01-03/P01-04/P01-08 | IN_PROGRESS
Work:
  Verified repository setup/locks and local production web/API connectivity.
  Final hardening disables inherited Redis health retries, catches unexpected
  API errors before server traceback logging, and adds regression tests.
  Removed premature standalone packaging to align the local next start
  command with its build; production packaging remains a Phase 21 concern.
Verification:
  pnpm install --frozen-lockfile --offline and uv sync --frozen: PASS.
  pnpm schemas:check: PASS after fixing argument forwarding.
  pnpm test:unit: PASS (20 Python + 11 web; 5 integration deselected).
  pnpm lint; pnpm typecheck; pnpm check:secrets: PASS.
  pnpm audit --prod --audit-level high: PASS, no known JS production findings.
  Local HTTP smoke: PASS, API live 200/ready 503, correlation preserved,
  unauthorized smoke POST 401, web 200 with real unavailable state/CSS 200.
  pnpm test:integration: BLOCKED at preflight (exit 1), Docker engine absent.
Limitations:
  Newly added hardening regressions and adjusted web build need rerun.
  Python/container vulnerability audits and hosted CI have not run.
Next:
  Rerun regressions/build, finish documentation, then record remaining blockers.

2026-09-03 00:21 PKT (UTC+05:00) | PHASE1-008 | P01-03/P01-09 | DONE
Work:
  Completed status-page scaffold and development documentation, environment
  ownership table, startup dependency map, extension procedures, limitations.
  Updated the detailed Phase 1 checklist without treating configured services
  as runtime-tested or hosted CI as executed.
Verification:
  Final hardening tests: 22 Python + 11 web PASS; 5 integration deselected.
  pnpm lint/typecheck/schemas:check: PASS. Adjusted pnpm build: PASS.
  README relative links: PASS. Git main exists, no commits or remote created.
  Previous local HTTP smoke demonstrated real web-to-API unavailable state.
Limitations:
  Docker acceptance remains outstanding. Checklist review also identified
  explicit job request-key/retry metadata and web-format/report configuration
  as remaining local details; these are not marked complete.
Next:
  P01-06 metadata alignment and P01-08 format/report automation; then record
  exact Docker and hosted-CI prerequisites for resuming the five open checkpoints.

2026-09-03 00:28 PKT (UTC+05:00) | PHASE1-009 | P01-06/P01-08 | LOCAL CHECKS PASS; ACCEPTANCE OPEN
Work:
  Added explicit request-key and attempt metadata, validated stored identity,
  and aligned two retries with three total attempts. Added unit regressions.
  Added pinned Prettier, non-mutating format gate, ignored JUnit reports,
  and safe CI report retention. Verified/pinned the CI Gitleaks image digest.
  Updated development guide/checklist and D07/D10 without changing scope counts.
Verification:
  pnpm test:unit:ci: PASS (24 Python + 11 web; 5 integration deselected).
  JUnit output: PASS (reports/python-unit.xml and reports/web-unit.xml).
  pnpm lint (Ruff, ESLint, Prettier, tracker): PASS.
  pnpm typecheck and pnpm schemas:check: PASS.
  Gitleaks image tag/digest existence verified; scanner itself NOT RUN.
Limitations:
  Real database/vector/migration, queue delivery/retry/shutdown, full
  five-service startup, recovery, and hosted CI remain unverified.
  Python/container vulnerability audits are not claimed; plan documented.
Next:
  Final frozen-lock/build/secret checks and local HTTP recheck; record
  the remaining five checkpoints as blocked on real infrastructure/CI.

2026-09-03 00:31 PKT (UTC+05:00) | PHASE1-010 | SESSION HANDOFF | PHASE 1 BLOCKED, 5/10 DONE
Work:
  Finished all current scaffold edits and final local checks. Added Linux
  init-script line-ending assertion; verified setup preserves existing .env.
  Saved exact resume steps and dependencies for the five open checkpoints.
Verification:
  pnpm test:unit:ci: 24 Python + 11 web PASS; 5 integration deselected.
  Latest contracts-only rerun: 4 PASS (including shell LF assertion).
  Final pnpm build: PASS; next start launched without standalone warning.
  Final frozen pnpm install: PASS. Earlier frozen uv sync: PASS.
  Ruff/ESLint/Prettier, mypy/TypeScript, OpenAPI/TS contracts: PASS.
  Compose v2.40.3 config --quiet: PASS; no Docker engine involved.
  Final HTTP smoke: API live 200, ready 503 with both dependencies down,
  correlation retained, protected route 401, web/CSS 200 and truthful state.
  Setup re-run preserved .env; .env and JUnit report ignore checks: PASS.
  Narrow secret guard: PASS. JS production audit: no known findings.
  API/web verification servers stopped; no deployment, commit, or remote.
Limitations/blockers:
  D10: host has no Docker/Podman or installed WSL Linux. Five real integration
  tests, image builds, migration/vector/worker/recovery/shutdown acceptance
  remain NOT RUN, not passed. No hosted CI/source scan/branch protection.
  Python/container vulnerability audits remain outstanding before production.
  HTTP/component verification is not a browser visual review. TestClient
  still emits the documented upstream httpx deprecation warning.
Next:
  Owner supplies running Linux-container Docker; technical lead follows
  the resume sequence above. Request an approved remote before hosted CI.
  Counts: 16/151 DONE, 135 remaining, 10.6% checkpoint-count progress;
  1/25 phases complete. No Phase 2 implementation or scoring approval inferred.

2026-09-03 02:40 PKT (UTC+05:00) | MOD-001 / PHASE2-START | IN_PROGRESS
Work:
  Read instructions, tracker, architecture, identity/market data model,
  product roles, and security requirements. User requested highly modular
  code, persistent instruction rules, and starting the next phase.
  Added root/scoped modularity instructions and MODULARITY.md.
Verification:
  Docker still absent from PATH and the standard Desktop CLI path.
  Refactor/schema implementation checks have not yet run.
Limitations:
  Early P02 work does not close P01 or waive live integration/authentication.
  Identity-provider choice remains pending; no secrets/accounts requested
  beyond the non-blocking provider question. No market HTTP routes enabled.
Next:
  Extract composition/transport/smoke service boundaries and regression-test;
  then implement P02-01 tenant-constrained models/migration without live activation.

2026-09-03 02:50 PKT (UTC+05:00) | MOD-002 | Refactor written; initial verification
Work:
  Split main.py into composition, shared transport, feature routers, typed
  smoke ports/service/repository/wiring, and feature-owned contracts/model.
  Workers now call the same smoke service, not ORM/session code. Compatibility
  exports preserve existing imports. Added boundary/cycle and service tests.
Verification:
  Initial root checks DID NOT RUN: existing uv executable is inaccessible
  this session, including after a scoped read permission grant.
  Used the existing accessible venv Python 3.12.13 directly instead:
  24 Python tests PASS, 5 integration deselected; mypy 32 files PASS.
  New architecture/service tests were added afterward and await their run.
Limitations:
  uv-based root aliases cannot be reported as passing this session.
  Existing pytest cache is access-denied; subsequent runs disable that cache.
  No security permissions/ACLs were changed or bypassed.
Next:
  Verify expanded modularity suite and frozen API contract; implement P02-01.

2026-09-03 02:52 PKT (UTC+05:00) | MOD-003 | Modular refactor verification | DONE
Work:
  Composition root is now wiring-only; feature ownership, pure service ports,
  repositories, safe transport, and shared worker service are separated.
  Root/scoped instructions and executable architecture checks preserve this.
Verification:
  Existing venv Python: 38 unit/architecture/service/contract tests PASS;
  5 integration tests deselected. Web suite: 11 PASS.
  Architecture boundary/cycle check: PASS. Mypy: 32 source files PASS.
  OpenAPI snapshot unchanged: PASS. Ruff initially found long lines; formatter
  corrected them and the subsequent Ruff check passed.
Limitations:
  No new roadmap checkpoint counted for refactoring; totals remain 16/151.
  uv runner unavailable in this session; direct venv checks used instead.
Next:
  P02-01 feature-owned identity/market models and frozen Alembic 0002.

2026-09-03 03:02 PKT (UTC+05:00) | PHASE2-001 | P02-01 | IN_PROGRESS
Work:
  Added feature-owned identity/market vocabulary and seven ORM tables,
  shared persistence primitives/registry, schema revision 0002, and frozen
  PostgreSQL migration SQL. No live schema was changed.
  Added composite tenant/market foreign keys, immutable-definition app grants,
  version fields, approval-pair checks, offline consistency and live PG cases.
Verification:
  SQLAlchemy PostgreSQL mock compilation produced all seven table definitions.
  Initial lint caught formatting and a test lambda; corrected without
  disabling rules. Full model/type/test rerun is pending.
  Prior refactor OpenAPI/TypeScript schema checks and web lint/types: PASS.
Limitations:
  PostgreSQL constraints/grants/migrations remain NOT RUN without Docker.
  No login provider chosen; no business HTTP routes or UI enabled.
Next:
  Run expanded schema/architecture suite, update physical-model documentation,
  and preserve the actual migration/tenant acceptance blockers.

2026-09-03 03:12 PKT (UTC+05:00) | PHASE2-002 | P02-01 | LOCAL PASS; LIVE ACCEPTANCE BLOCKED
Work:
  Implemented seven models, tenant/market composite references, snapshot
  application grants, centralized metadata/head, and frozen Alembic 0002.
  Added Phase 2 checklist and physical-model documentation. Extracted smoke
  identity/state decisions into a pure domain function. No live DB changed.
Verification:
  Existing venv Python: 52 tests PASS, 14 integration deselected.
  Mypy: 43 source files PASS. Ruff lint/format: PASS.
  Architecture boundary/cycle check and unchanged OpenAPI: PASS.
  Alembic head 0002 and offline upgrade/downgrade/model DDL consistency: PASS.
  Refactored API live HTTP: 200 liveness/correlation, 503 readiness,
  unregistered business route 404, protected lookup 401. Server stopped.
  Narrow local secret guard: PASS. Earlier web unit/lint/types/contracts: PASS.
  Test collection initially hit duplicate basenames; switched pytest to
  importlib mode without deleting caches. Full rerun then passed.
  A documentation patch failed context validation; confirmed it applied
  nothing, corrected the context, and reapplied successfully.
Limitations:
  Docker-backed 14-case acceptance NOT RUN. No schema checkpoint closed,
  no tenant authorization claimed, and no private data or accounts seeded.
  Two additional lookup error-contract regressions added afterward await
  final run; historical lookup outage status remains 500 with sanitized JSON.
  D11 provider choice and D10 infrastructure/CI remain unresolved.
Next:
  Final combined checks and tracker validation; resume with working Docker
  for P02-01 and an owner-approved OIDC configuration for P02-02.

2026-09-03 03:17 PKT (UTC+05:00) | PHASE2-003 | SESSION HANDOFF | MODULARITY DONE; P02 PARTIAL
Work:
  Persistent modularity rules are in root AGENTS.md, apps/api/AGENTS.md,
  apps/web/AGENTS.md, workers/AGENTS.md, and docs/MODULARITY.md.
  Modular API/worker refactor is implemented, including pure smoke decisions,
  typed ports/use cases, private persistence, and composition-only main.py.
  Phase 2's seven models and frozen 0002 migration are written and checked
  offline. Added its detailed checklist and kept routes disabled before auth.
Verification:
  Existing venv Python -m pytest -p no:cacheprovider -m "not integration":
  54 PASS, 14 deselected. pnpm --filter @mask/web test: 11 PASS.
  Python Ruff lint/format and mypy (43 source files): PASS.
  Web ESLint/Prettier/TypeScript and generated TS contract check: PASS.
  check_architecture.py, export-openapi.py --check, check-secrets.py: PASS.
  Lookup 404/500 error-contract regressions and safe correlation headers: PASS.
  README/instruction/development/architecture local links: 22 PASS.
  check-progress.mjs: PASS (16/151 DONE, 135 remaining; denominator unchanged).
  check-docker.mjs: blocked exit 1; integration explicitly NOT RUN.
Limitations/blockers:
  P02-01 remains BLOCKED on actual PostgreSQL migration/grant/constraint/
  reflection/rollback checks; P01's five acceptance checkpoints remain open.
  D11 OIDC provider/configuration is needed for the next authentication work.
  Restore a working approved uv runner for standard root/clean setup commands.
  No live schema changes, account/seed creation, private research, deployment,
  remote creation, or commits. No Phase 2 completion or scoring approval claimed.
Next:
  With Docker available, run the 14-case integration suite and resolve P01/P02-01
  findings. Confirm an OIDC provider before P02-02; implement authenticated
  tenant-scoped market services/API/UI only after that boundary is in place.

2026-09-03 18:14 PKT (UTC+05:00) | PHASE2-004 | P02-02 | IN_PROGRESS
Work:
  Read canonical/scoped instructions, tracker, security/product/data model,
  modularity contract, and Phase 2 checklist for the next unfinished checkpoint.
  Continue Phase 2 rather than claim it complete or skip its security dependency.
  Implement provider-independent session-reader/membership interfaces, fresh
  server-side tenant/role checks, and an explicit deny-by-default permission map.
Verification:
  Read-only tool discovery still finds no docker or uv on PATH.
  New implementation checks are pending. No acceptance checkpoint closed.
Limitations/blockers:
  D10 live PostgreSQL/containers/CI and D11 OIDC provider choice remain open.
  No provider account, credentials, auth route, or business route will be created
  by this preparatory work. The session reader will have no fake runtime fallback.
Next:
  Implement/test the identity core and tenant-scoped read adapter; update this
  checkpoint immediately with results and the remaining integration boundary.

2026-09-03 18:20 PKT (UTC+05:00) | PHASE2-005 | P02-02 | CORE IMPLEMENTED; ACCEPTANCE OPEN
Work:
  Added identity-owned contracts/errors/ports/service/read-only repository,
  explicit Phase 2 permission map, fresh tenant membership/status/role lookup,
  session expiry/revocation checks, acting roles, and recent-auth admin guard.
  No OIDC adapter, session persistence, login, bootstrap, or market routes added.
Files:
  apps/api/src/mask_api/modules/identity/{domain,contracts,errors,ports,services,repository}.py
  apps/api/tests/identity/; tests/integration/test_identity_membership.py.
Verification:
  Focused identity tests: 39 PASS. Ruff initially found import/line formatting;
  sorted/formatted and reran: lint PASS, format PASS (92 files).
  Mypy: 48 source files PASS. Architecture/OpenAPI/narrow secret guard: PASS.
  Expanded suite collects 111 Python cases, including 18 live integration cases.
  Docker preflight fails explicitly: integration NOT RUN. Full unit run in progress.
Limitations/blockers:
  The session reader is only a port; no insecure runtime fallback exists.
  Unit SQL assertions/mocks do not prove actual PostgreSQL read isolation.
  Four new membership integration cases await D10, plus the previous fourteen.
  Provider D11, secure session storage/CSRF/logout, bootstrap/audit, and E2E remain.
  Permission grants are preliminary role gates, not resource-level authorization.
Next:
  Record detailed identity boundaries and permissions, finish regression checks,
  and keep P02-02 incomplete until the real authentication boundary is verified.

2026-09-03 18:26 PKT (UTC+05:00) | PHASE2-006 | P02-02 | CORE VERIFIED; INTEGRATION BLOCKED
Work:
  Completed the provider-independent identity slice and documented its exact
  public boundaries and preliminary seven-permission/six-role matrix.
  Preserved backend-only enforcement, modular service/port/repository ownership,
  explicit acting roles, no admin wildcard, and no fake authentication fallback.
  Added four real membership repository acceptance cases, not yet executed.
Files:
  Identity module and unit/integration tests listed in PHASE2-005.
  docs/AUTHORIZATION.md, docs/SECURITY.md, docs/MODULARITY.md,
  docs/DEVELOPMENT.md, docs/phases/PHASE_2_CHECKLIST.md, apps/api/AGENTS.md,
  README.md, and this tracker. No dependency, migration, or runtime route change.
Verification:
  Existing venv: python -m pytest -p no:cacheprovider -m "not integration" -q:
  93 PASS, 18 deselected. Existing upstream TestClient deprecation warning remains.
  python -m ruff check . and python -m ruff format --check .: PASS.
  python -m mypy apps/api/src workers: PASS (48 source files).
  check_architecture.py; export-openapi.py --check; check-secrets.py: PASS.
  pnpm --filter @mask/web test: 11 PASS; web lint/format:check/typecheck: PASS.
  pnpm --filter @mask/schemas check: PASS. Local document link check: 25 PASS.
  node scripts/check-progress.mjs: PASS, 16/151 done, 135 remaining, 10.6%.
  node scripts/check-docker.mjs: exit 1, integration explicitly NOT RUN.
Limitations/blockers:
  P02-02 and Phase 2 marked BLOCKED after this safe independent slice.
  D11 owner must choose OIDC provider; implementation then needs verified
  issuer/subject linkage, real server session storage, secure cookies/CSRF,
  expiry/logout/rotation, explicit bootstrap, audited roles, safe HTTP errors,
  security events, and actual login/tenant/permission acceptance.
  D10 owner/admin must supply Docker Linux containers for eighteen real tests;
  approved remote/hosted CI and a usable standard uv runner remain outstanding.
  Live schema constraints and membership-query isolation are not proven by mocks.
  The new grants are internal role gates, not serialized credentials or complete
  object/workflow authorization. No business endpoints, accounts, or real data.
  Build/audits/frozen installs retain earlier results; they were not rerun here.
  No server left running, provider account, deployment, remote, or commit created.
Next:
  Confirm D11 provider and implement its validated adapter and secure session
  lifecycle behind the existing ports. Run D10 acceptance with working Docker.
  P02-03 market APIs and Phase 3 remain unstarted until their dependencies allow.

2026-09-03 18:40 PKT (UTC+05:00) | DOCKER-DIAG-001 | D10 / P02-01 | STILL BLOCKED
Work:
  User requested the exact Docker blocker and reproducible terminal checks.
  Performed read-only local runtime/install/process diagnostics; no fix applied.
Verification:
  Get-Command docker: not found on PATH.
  Docker Desktop and docker.exe absent from the standard all-user and current-
  user install paths. No Docker installed-app entries found in HKLM/HKCU.
  No com.docker.service or Docker Desktop/backend/dockerd process found.
  wsl.exe exists, but both --status and --list --verbose report:
  "The Windows Subsystem for Linux is not installed." Both exit nonzero.
  Docker Compose YAML and API/web Dockerfiles exist in the project.
  Bundled Node running scripts/check-docker.mjs exits 1 before container work.
Limitations/blockers:
  This is an unavailable host runtime, not an observed image-build, container,
  migration, or application error. Eighteen live tests remain NOT RUN.
  No software installed, Windows features changed, or services started.
  Prior unit/lint/type/contract results remain historical, not rerun here.
  Counts unchanged: 16/151 complete, 135 remaining; D11 also remains open.
Next:
  Owner can reproduce with docker version, docker compose version, install-path
  checks, and wsl --status. Install/start an approved Docker Desktop Linux-
  container environment (with WSL 2 if using that backend), reopen the terminal,
  then verify client/server connectivity before attempting project startup.

2026-09-03 19:04 PKT (UTC+05:00) | DOCKER-DIAG-002 | D10 / P02-01 | COMPOSE AVAILABLE; ENGINE BLOCKED
Work:
  User supplied the existing work/docker-compose.exe and reported adding PATH.
  Verified that exact executable and checked current/saved PATH without changing
  persistent environment settings or starting containers.
Verification:
  Absolute-path docker-compose.exe version: v2.40.3, exit 0.
  Absolute-path Compose -f docker-compose.yml config --quiet: exit 0.
  Saved machine PATH contains the executable path itself, not its work directory.
  Current process resolves neither docker nor docker-compose as a command.
  Read-only Compose ps forced to the local Windows docker_engine endpoint fails:
  open //./pipe/docker_engine: The system cannot find the file specified; exit 1.
  The generic error mentions elevation, but the underlying result is missing
  endpoint, not a demonstrated access-denied error.
Limitations/blockers:
  Standalone Compose is available and configuration validation passes. It does
  not provide Docker Engine or the docker CLI/compose plugin our scripts invoke.
  No engine integration demonstrated; all eighteen integration cases NOT RUN.
  No installer, PATH edit, container startup, or application change performed.
  Checkpoint totals remain 16/151 complete, 135 remaining.
Next:
  Correct the saved PATH entry to the containing work directory if retaining
  standalone Compose. Supply/start an approved Linux-container Docker Engine
  (normally Docker Desktop here), then verify docker version and compose version.

2026-09-03 21:24 PKT (UTC+05:00) | NATIVE-001 | P01-07/P01-08 / D12 | IN_PROGRESS
Work:
  User explicitly rejected Docker. Inventory found project Compose/Dockerfiles,
  startup/preflight/init scripts, CI commands, contract tests, two service-stop
  integration cases, environment comments, and active documentation to replace.
  Plan direct web/API commands and Linux worker entrypoint, native service
  preflight, safe application-level dependency-recovery acceptance, and native CI.
Verification:
  Read root/scoped modularity rules and applicable development/runtime docs.
  No code change verified yet. Existing tests/services/data are preserved.
Limitations:
  Docker removal does not install PostgreSQL/Redis, approve hosted services,
  make Celery supported on Windows, waive live tests, or resolve provider D11.
  System software and standalone executable outside project are not removal targets.
Next:
  Implement replacements, remove only obsolete project-owned files, then run
  regression checks and update current instructions/decisions without erasing history.

2026-09-03 21:35 PKT (UTC+05:00) | NATIVE-002 | P01-07/P01-08 | PARTIAL VERIFICATION
Work:
  Removed six obsolete project runtime files with recovery text copies outside
  the project under work/docker-removal-backup-2026-09-03. Added direct web/API
  commands, native Linux worker entrypoint, service-based integration preflight,
  isolated application connectivity-failure tests, and native CI/source scanning.
Verification:
  Focused tooling/contract tests: 25 PASS. Ruff lint/format PASS after formatting.
  Extended mypy found two narrow dictionary-key type errors in the new preflight;
  widened the report dictionary annotation explicitly; rerun pending.
  Actual native preflight reports PostgreSQL/Redis/API/worker down and exits 1.
  No integration test claimed as run; no services were started or stopped.
  A documentation patch failed context validation and applied nothing; checked
  the target files, corrected context, and reapplied. Documentation still underway.
Next:
  Finish native setup/checklists, rerun complete quality checks, and record precise
  remaining service/CI/identity blockers without any Docker prerequisite.

2026-09-03 21:46 PKT (UTC+05:00) | NATIVE-003 | P01-07/P01-08 | REGRESSION PASS; FINAL CHECK
Work:
  Updated native setup/CI/security docs, root/API/worker instructions, runtime
  architecture, phase checklists, and D10/D12. Preserved old ADR/history as
  superseded evidence. PostgreSQL/Redis/Celery and identity boundaries unchanged.
Verification:
  Full unit suite: 114 Python PASS, 18 live cases deselected; web 11 PASS.
  Extended mypy: 50 source files PASS after replacing the narrow report mapping
  conversion with a typed comprehension; no ignored type errors added.
  Web lint/format/types/generated contracts and backend architecture/OpenAPI/
  narrow secret guard PASS. Setup leaves existing .env byte-identical (hash check).
  Native Windows worker guard exits 2 as designed; service preflight reports
  all four services down and exits 1 without running integration.
  A pnpm exec next command failed to resolve its Windows shim. Changed dev:web
  to invoke the existing web package dev script; exact CLI rerun is pending.
Limitations:
  No full native service startup, live schema/worker/fault tests, native Linux
  Gitleaks execution, or hosted CI has run. No Bash executable was found here.
  No active server, system installation, PATH change, database mutation, or
  dependency version change. Standard uv aliases still need an approved uv tool.
Next:
  Verify the final launcher command, rerun changed checks, validate documentation
  links/tracker, and hand off removal with native-service acceptance still open.

2026-09-03 21:47 PKT (UTC+05:00) | NATIVE-004 | P01-07/P01-08 / D12 | CONVERSION VERIFIED; ACCEPTANCE BLOCKED
Work:
  Completed the user-requested container-free conversion. Removed active
  Compose, ignore, image, preflight, and bootstrap files; recovery copies exist
  outside the project. Added direct loopback web/API commands, guarded native
  Linux worker, safe service preflight, isolated connection-fault tests, native
  CI/security scan, ADR 0002, current setup docs, checklists, and instructions.
Verification:
  Final non-integration suite: 114 Python PASS, 18 live cases deselected;
  web suite: 11 PASS. Total local unit/component/contract/tooling: 125 PASS.
  Ruff lint/format: PASS (98 files). Extended mypy: 50 source files PASS.
  Web ESLint/Prettier/TypeScript, generated schemas, API OpenAPI snapshot,
  architecture boundaries, narrow secret guard, tracker, and 30 doc links: PASS.
  pnpm run setup left existing .env byte-identical; no values/hash printed.
  Exact web launcher now resolves Next CLI and displays help; API Uvicorn CLI
  resolves. Worker guard exits 2 on Windows before credentials; unit-tested
  Linux argv uses existing app/concurrency. No actual worker was run here.
  Native service preflight safely reports postgres/redis/api/worker down, exits
  1, and does NOT run integration. Six obsolete files absent and six recovery
  copies present. No active workflow has a container/service job declaration.
Limitations/blockers:
  P01-07 remains incomplete because no clean provisioned native environment ran.
  All eighteen real cases, operator service restart/recovery, worker logs and
  graceful shutdown, native Gitleaks execution, hosted CI, isolated Linux runner,
  full audits, and branch protection remain NOT RUN/unconfigured under D10.
  No service/account/provider/runtime was provisioned, no database/data changed,
  no Windows PATH/system software/standalone Compose executable altered, and no
  server left running. Standard uv aliases remain unverified on this host.
  D11 OIDC provider choice remains separate; roadmap is still 16/151 complete.
  Production build/audit/frozen-install results are historical, not rerun here.
Next:
  Provision isolated native PostgreSQL/pgvector and Redis plus a supported Linux
  worker environment, privately configure URLs/roles, migrate, start direct
  processes, run check:services and eighteen live cases, then hosted acceptance.
  Independently confirm the OIDC provider before authenticated market routes.

2026-09-03 21:55 PKT (UTC+05:00) | NATIVE-005 | D13 / P01-06/P01-07/P01-08 | IN_PROGRESS
Work:
  User established Windows-only, no-Docker, private-use, requirements-only, and
  cost-conscious constraints. Began replacing the remaining Linux/Redis/Celery
  and paid-provider assumptions with a lean Windows-native operating model.
Verification:
  Confirmed the supplied guide permits an equivalent Python queue, explicitly
  discourages expensive AI on every item, limits the first pilot to 3-5 markets,
  and defers non-MVP automation. Documentation/code alignment checks are pending.
Limitations/blockers:
  The existing worker still rejects Windows and still depends on Redis/Celery;
  no queue/auth implementation, service install, migration, or research run has
  occurred. No checkpoint is closed and counts remain 16/151.
Next:
  Adopt a Windows-first ADR and cost-control contract; update active instructions,
  architecture, setup, checklists, README, and tracker. Queue/auth code follows as
  separately verified checkpoint work.

2026-09-03 22:06 PKT (UTC+05:00) | NATIVE-006 | D13 / P01-06/P01-07/P01-08/P02-02 | ARCHITECTURE ADOPTED; IMPLEMENTATION OPEN
Work:
  Adopted ADR 0003 and the mandatory lean research contract. The target is one
  private Windows installation using Next.js, FastAPI, PostgreSQL/pgvector, a
  PostgreSQL-backed native Python worker, and local accounts/server sessions.
  Paid AI defaults off; every enabled run has hard request/token/document/time/
  currency limits, deterministic filtering/dedup first, and version-aware reuse.
  Rescoped current instructions, architecture, authorization, phase checklists,
  security/evaluation/AI/product docs, README, D07/D10-D13, and resume sequence.
Files:
  AGENTS.md; workers/AGENTS.md; apps/api/AGENTS.md; README.md;
  docs/{ARCHITECTURE,AUTHORIZATION,COST_CONTROL,DATA_MODEL,DEVELOPMENT,
  EVALUATION,MODULARITY,PRODUCT,SECURITY,AI_PIPELINE}.md;
  docs/decisions/0001-phase-1-baseline.md,
  docs/decisions/0002-container-free-development.md,
  docs/decisions/0003-windows-first-lean-runtime.md;
  docs/phases/PHASE_1_CHECKLIST.md, docs/phases/PHASE_2_CHECKLIST.md;
  progress.txt. No runtime code, dependency manifest, workflow, or schema changed.
Verification:
  check-progress.mjs: PASS, 16/151 complete and 135 remaining.
  Local markdown links: PASS across 20 instruction/document files.
  Existing venv non-integration Python suite: 114 PASS, 18 deselected, with the
  existing single upstream Starlette/httpx deprecation warning. Web 11 PASS is
  retained from NATIVE-004 and was NOT rerun because no frontend code changed.
  Initial link-check command used an over-broad recursion and hit unreadable
  .pytest_cache; it made no change. Exact AGENTS/README/docs rerun then passed.
Limitations/blockers:
  Architecture is settled, but the checked-in implementation still contains
  Redis/Celery, a Windows worker guard, Redis readiness/config/tests, and a
  Linux-oriented workflow/scanner. Do not provision those transitional services.
  PostgreSQL jobs/leases/worker and local credential/session migrations are not
  implemented. No PostgreSQL service, account, model call, research, deployment,
  paid resource, or runtime process was created or run. No checkpoint closed.
Next:
  Implement P01-06 as one modular migration: durable PostgreSQL jobs, native
  Windows worker, service preflight/tests, dependency cleanup, and queue acceptance.
  Then implement P02-02 local credential/session/bootstrap/CSRF boundaries.

2026-09-03 22:13 PKT (UTC+05:00) | P01Q-001 | P01-06 | IMPLEMENTATION STARTED
Work:
  User explicitly authorized moving from architecture into real implementation.
  Re-read root/API/worker instructions, tracker, modularity contract, ADR 0003,
  and inventoried the queue/worker/test surface. P01-06 remains the active task.
Verification:
  No implementation check has run yet. The first read-only inspection command
  contained a malformed timeout token and did not execute; corrected inspection
  completed successfully. No files or runtime state changed by that failure.
Limitations/blockers:
  Existing code still uses Redis/Celery and rejects Windows. Live PostgreSQL is
  not provisioned, so implementation can be verified offline/unit-first but the
  real queue concurrency/recovery acceptance cannot yet be claimed.
Next:
  Define the durable job domain/ports and frozen Alembic revision, implement the
  PostgreSQL adapter/Windows runner, replace service checks, then remove old
  dependencies only after focused tests pass.

2026-09-03 22:28 PKT (UTC+05:00) | P01Q-002 | P01-06 | CORE WRITTEN; TEST MIGRATION PENDING
Work:
  Added modular job_queue domain/contracts/ports/models/repository/wiring. The
  adapter transactionally enqueues, claims with FOR UPDATE SKIP LOCKED, leases,
  heartbeats, retries with capped jittered backoff, cancels, reclaims, records
  safe outcomes, and tracks worker liveness. Added a concurrency-1 native Python
  worker runtime with lease maintenance and bounded signal-driven shutdown.
  Smoke API now uses the durable queue row directly; removed broker publication,
  the Celery task/app, and single-purpose smoke ORM/domain implementation.
  Added frozen Alembic 0003 with preservation of existing synthetic smoke rows.
  Removed Redis from settings/readiness/example/preflight target code.
Verification:
  Focused Ruff initially reported unused/import formatting and one long line;
  corrected them. Mypy initially reported retry return typing, SQL rowcount, and
  output mapping variance; corrected with explicit types/RETURNING/JsonValue.
  Focused rerun: Ruff PASS; strict mypy PASS across 56 source files.
  An earlier atomic refactor patch was rejected because one file was both deleted
  and added; it applied nothing. The in-place replacement then succeeded.
Limitations/blockers:
  Old Redis/Celery tests, dependencies/lock entries, frontend readiness display,
  integration expectations, workflow/scanner, and documentation remain to migrate.
  Alembic 0003 has not yet passed offline DDL/reversibility checks. No live
  PostgreSQL exists, so real atomic claim/lease/recovery remains NOT RUN.
Next:
  Replace unit/integration/tooling contracts, verify frozen migration/model DDL,
  regenerate OpenAPI/TypeScript, then remove manifests/lock/workflow references.

2026-09-03 23:09 PKT (UTC+05:00) | P01Q-003 | P01-06/P01-07/P01-08 | OFFLINE IMPLEMENTATION VERIFIED; LIVE ACCEPTANCE OPEN
Work:
  Completed the PostgreSQL queue replacement across runtime, API smoke adapter,
  readiness/preflight, migration, generated contracts, dependencies, workflow,
  Windows source scanning, instructions, architecture, and setup documentation.
  Redis/Celery and their transitive broker packages are absent from active
  manifests/lock/runtime paths. The generic queue layers and feature handlers are
  explicitly separated in root/API/worker modularity instructions.
Files:
  apps/api/src/mask_api/job_queue/*; workers/{runtime,wiring,smoke_handler,
  __main__}.py; apps/api/migrations/versions/0003_postgres_job_queue.py;
  queue/smoke/health/config/preflight tests and adapters; .github/workflows/ci.yml;
  scripts/scan-secrets.ps1; uv.lock; generated OpenAPI/TypeScript; current docs,
  scoped AGENTS.md files, phase checklist, README.md, and progress.txt.
Verification:
  Final Python non-integration suite: 130 PASS, 21 real cases deselected; one
  upstream Starlette/httpx deprecation and one restricted pytest-cache warning.
  Web: 11 PASS; ESLint, Prettier, TypeScript/Next route generation PASS.
  Ruff/format PASS (112 files); strict mypy PASS (56 source files); architecture
  and backend OpenAPI snapshot PASS. Focused Windows runtime/workflow tests: 4 PASS.
  uv 0.12.9 regenerated a 44-package lock; lock check PASS and obsolete broker
  dependency search returned no matches. A default uv state path first failed
  access; redirecting cache/managed-Python state to task-specific temp paths fixed
  it without changing user/system configuration.
  One focused pytest command referenced nonexistent test_ci_contract.py and ran no
  tests; the corrected target passed. Initial direct pnpm exec Prettier resolution
  failed, and the package-level formatter initially did not persist one file's
  expected layout; direct checked-in Prettier binary formatting fixed it and the
  complete web gate then passed. Neither failed attempt changed runtime behavior.
Limitations/blockers:
  Counts remain 16/151 and P01-06 stays IN_PROGRESS because all 21 real
  PostgreSQL/service tests, concurrent-worker ownership, process Ctrl+C behavior,
  operator PostgreSQL restart/persistence recovery, and clean provisioned Windows
  startup are NOT RUN until D10 provides an isolated PostgreSQL/pgvector instance.
  Windows Gitleaks download/execution, clean uv sync/root aliases, production
  build, current dependency audits, hosted CI, remote, and branch protection are
  also not newly accepted. No service, database, account, paid model, deployment,
  or market research run was created.
Next:
  Owner/admin provisions D10's isolated native PostgreSQL/pgvector and roles;
  technical lead applies 0003, starts worker/API/web, runs preflight and all 21
  live cases, then records operator restart/shutdown evidence. P02-02 local
  credential/session implementation may proceed independently as authorized.

2026-09-03 23:16 PKT (UTC+05:00) | P02A-001 | P02-02 | IMPLEMENTATION STARTED
Work:
  User directed implementation to continue while deferring live database setup.
  Selected the next independent slice: local credentials, hashed server sessions,
  CSRF/session lifecycle, one-time owner bootstrap, and identity security events.
  Re-read root/API instructions, modularity, authorization, Phase 2 checklist,
  and current identity code. Routes remain unregistered until real persistence is
  present and the authentication boundary passes its offline checks.
Verification:
  Confirmed maintained argon2-cffi 25.1.0 and Windows/Python 3.12 support from
  official project/PyPI documentation. Implementation tests have not run yet.
Limitations/blockers:
  Live PostgreSQL remains deliberately deferred by the user. This permits code,
  frozen migration, and offline/unit verification but not checkpoint completion,
  real bootstrap/login, or enabling authenticated market routes. Counts stay
  16/151 and P02-02 is IN_PROGRESS.
Next:
  Add narrow auth contracts/ports/security adapters and persistence models,
  freeze Alembic 0004, then implement/test bootstrap and session lifecycle before
  deciding whether the HTTP adapter is safe to register.

2026-09-03 23:29 PKT (UTC+05:00) | P02A-002 | P02-02 | AUTH PERSISTENCE CORE WRITTEN
Work:
  Added narrow local-auth contracts/ports, Argon2id password manager, 256-bit
  opaque session/CSRF token manager, login/session/bootstrap services, and
  transactional SQLAlchemy authentication/bootstrap stores. Added identity-owned
  credential, hashed session, and append-only security-event models plus frozen
  Alembic 0004 and registry/head updates. Login session creation, password rehash,
  lockout reset, rotation/revocation, and audit writes share transactions in the
  concrete adapter. Market/auth routes remain unregistered.
Verification:
  Official PyPI/project metadata confirmed argon2-cffi 25.1.0 with Windows and
  Python 3.12 support. The venv has no pip module, so a direct pip install failed
  without changing it; pinned task uv then installed argon2-cffi and bindings,
  regenerated a 48-package lock, and uv lock --check passed.
  Focused Ruff/format PASS; strict mypy PASS for 14 identity source files.
  Auth/Phase 2 model-to-frozen-DDL, offline upgrade/downgrade, grants, migration
  head, route-absence, and prior schema contracts: 10 PASS. The first combined
  test command returned progress dots without a final summary; exact rerun passed
  10 with the existing upstream TestClient deprecation warning.
Limitations/blockers:
  Auth behavior/service/repository unit tests, bootstrap CLI, HTTP cookie/CSRF
  adapter, integration fixtures, full regression, and documentation are still
  pending. Alembic 0004 has not executed on PostgreSQL. Counts remain 16/151;
  P02-02 stays IN_PROGRESS and no real account/session exists.
Next:
  Add exhaustive service/security/repository tests, correct any atomicity or
  denial-path defects, implement the non-public bootstrap command, then assess
  HTTP registration only after the concrete boundary is safe.

2026-09-03 23:48 PKT (UTC+05:00) | P02A-003 | P02-02 | LOCAL AUTH SLICE OFFLINE-VERIFIED
Work:
  Completed the real local-auth code slice while honoring the user's decision to
  defer live database setup. Added LocalAuthenticationService, normalized generic
  login denial with unknown-account dummy Argon2 verification, bounded known-
  account lockout, parameter rehash, hashed session/CSRF issuance, absolute-expiry
  rotation, logout/revocation, and current-session adapter. Added atomic SQLAlchemy
  operations for credential state + session + audit, one-time advisory-lock owner
  bootstrap, typed wiring/settings, interactive no-password-argument CLI, and a
  separately testable login/logout/rotate cookie adapter. It is not registered in
  main.py, so the running application still exposes no auth or market routes.
  Updated Alembic 0004, data/architecture/auth/development docs, Phase 2 checklist,
  README, environment example, root command, dependency manifest, and uv lock.
Verification:
  New auth-focused coverage: 48 PASS across maintained Argon2id hashing, random
  opaque tokens, service denials/races/rehash/lockout/expiry/rotation/logout/CSRF,
  repository tenant/hash queries, bootstrap CLI, secure cookie/error mapping, and
  frozen schema/offline migration contracts. Expanded non-integration Python suite:
  182 PASS, 24 live cases deselected, one existing upstream TestClient warning.
  Real integration collection: 24 cases, including 3 new auth persistence cases.
  Ruff/format PASS (131 files); strict mypy PASS (67 source files); architecture
  boundaries/import cycles PASS. Earlier focused identity/API/schema run: 90 PASS.
  One import-order and several formatting findings were mechanically corrected.
Limitations/blockers:
  Alembic 0004, the 3 auth and 21 earlier real cases, empty-database bootstrap,
  cookie behavior through the running app, persistence/restart, and security-event
  inspection are NOT RUN until D10. Auth router registration and market routes are
  intentionally blocked by that acceptance. Audited membership/role mutation is
  not implemented. Counts remain 16/151 and P02-02 stays IN_PROGRESS. No database,
  account, external provider, paid service, deployment, or market research ran.
Next:
  Implement audited, recent-auth-protected membership/role administration behind
  the existing identity ports. Then prepare P02-03 tenant-scoped market use cases
  without registering them until live authentication/database acceptance passes.

2026-09-03 23:51 PKT (UTC+05:00) | P02A-004 | P02-02 | FINAL OFFLINE REGRESSION PASS
Work:
  Completed the P02-02 offline implementation checkpoint and synchronized active
  documentation/tracker. Removed one unused import found by the final static gate;
  no behavior changed. Auth routes remain intentionally outside main.py.
Verification:
  Focused identity/API/auth/Phase 2 regression: 112 PASS with the existing
  upstream TestClient deprecation warning. Web suite rerun: 11 PASS. Ruff and
  format PASS across 131 files; strict mypy PASS across 67 source files;
  architecture and OpenAPI snapshot PASS. Generated TypeScript schema check,
  uv 48-package lock check, tracker consistency, and narrow secret guard PASS.
  Full non-integration Python result immediately before the final import cleanup
  remains 182 PASS/24 deselected; removing an unused import cannot affect runtime.
Limitations/blockers:
  The 24 live cases and all database/bootstrap/browser/operator acceptance remain
  NOT RUN by the user's instruction to finish database work later. This is not a
  completed P02-02 master checkpoint; total completion remains 16/151.
Next:
  Continue actual code with audited recent-auth-protected membership/role changes,
  followed by tenant-scoped P02-03 market services. Keep routes unregistered until
  live PostgreSQL/auth acceptance is available.

2026-09-04 00:30 PKT (UTC+05:00) | P03C-001 | P03-02/P03-03/P03-05/P03-06/P03-08 | IMPLEMENTATION STARTED
Work:
  User directed work away from authentication and database setup toward actual
  scraper code. Selected two lean, reusable test collectors: RSS/Atom feeds and
  explicitly permitted static HTML pages. The slice includes a current-policy
  dispatch gate, common typed pipeline contracts, bounded HTTP behavior,
  deterministic normalization/exact hashes, parser fixtures, and no AI calls.
  Existing local-auth code remains intact and unregistered; no new auth work is
  in scope for this attempt.
Verification:
  Re-read AGENTS.md, API/worker scoped instructions, MODULARITY.md,
  SCRAPING_POLICY.md, COST_CONTROL.md, the supplied guide's Phase 3/collector
  sections, and P03 tracker requirements. Implementation checks NOT RUN yet.
Limitations/blockers:
  No source has an approved registered policy and live collection is not
  authorized, so tests will use local fixtures only. Per the user's sequencing,
  database migrations/repositories and live PostgreSQL acceptance remain later;
  therefore the affected master checkpoints cannot yet be marked complete.
  Counts remain 16/151 complete and 135 remaining.
Next:
  Add the evidence module's pure contracts/policy/normalization layers, network
  adapter, two collectors, orchestration service, and offline tests; then run the
  focused and full static/regression suites and record exact results.

2026-09-04 01:21 PKT (UTC+05:00) | P03C-002 | P03-02/P03-03/P03-05/P03-06/P03-08 | OFFLINE COLLECTOR SLICE VERIFIED
Work:
  Implemented the shared Phase 3 evidence module and two actual reusable parser
  adapters: RSS/Atom and explicitly permitted static HTML. Added immutable source
  policy, collection request, raw/parsed/normalized document, duplicate, batch,
  and persistence contracts with organization/market/definition lineage. Added
  pre-dispatch approval/review/effective-window/tenant/method/collector/auth gates;
  exact-origin path/query URL controls; a TLS-verifying, redirect/cookie/proxy-
  disabled public-address HTTP adapter; bounded sequential pipeline orchestration;
  capped retry/backoff/jitter, Retry-After handling, request/document/byte/time
  limits, cancellation and circuit breaking; raw-response retention on parse
  failure; NFKC v1 normalization; exact hashes, occurrence keys, and duplicate
  links. Added module-scoped modularity instructions and COLLECTORS.md. Existing
  auth code was neither expanded nor removed and remains unregistered.
Files:
  apps/api/src/mask_api/modules/evidence/ (18 implementation/instruction files),
  apps/api/tests/evidence/ (5 test modules and 7 parser fixtures),
  apps/api/tests/conftest.py, docs/COLLECTORS.md, docs/MODULARITY.md,
  docs/ARCHITECTURE.md, README.md, progress.txt. Database changes: none.
Verification:
  Initial evidence run found one overly exact fixture assertion: 33 PASS, 1 FAIL;
  the assertion was corrected without weakening parser behavior. Final focused
  evidence suite: 36 PASS. Full non-integration Python suite: 218 PASS,
  24 integration cases deselected, one existing upstream TestClient warning.
  Web suite: 11 PASS; total current automated tests: 229 PASS.
  Ruff check PASS and format PASS across 155 files; strict mypy PASS across
  83 source files; architecture/import-cycle check PASS; OpenAPI snapshot and
  generated TypeScript schema checks PASS; local secret guard PASS. No package
  or lockfile change was needed. Tests used local fixtures and fake narrow ports;
  no live website, database, model, account, service, or deployment was touched.
Limitations/blockers:
  P03-01 migrations and concrete PostgreSQL source-policy/evidence adapters were
  deliberately deferred by the user. Consequently persist behavior is verified
  only at the typed seam, no source policy is registered, and neither worker nor
  API collection wiring is enabled. Upload failure/migration/live smoke coverage
  required by P03-04/P03-08 remains NOT RUN. The two collectors are reusable core
  adapters, not P04-01 production-source approvals. No master checkpoint closes;
  counts remain 16/151 complete and 135 remaining.
Next:
  While database work stays deferred, implement P03-04 bounded analyst upload and
  manual-capture validation through the same contracts. Before any real scraping,
  approve two named sources and add their current policy versions; later add the
  Alembic evidence schema, transactional provider/writer, durable worker handler,
  tenant-authorized API, and deliberately small live acceptance run.

2026-09-04 19:48 PKT (UTC+05:00) | PUBLISH-001 | REPOSITORY PUBLICATION | IN_PROGRESS
Work:
  User requested creation of a GitHub repository and publication of the current
  project. Scoped publication to this mask-market-intelligence Git root only,
  excluding the parent attachment/work directories. Selected private visibility
  because the canonical product scope is a private Windows MVP.
Verification:
  Existing repository is initialized on main with no commits and no remotes.
  Git 2.53.0 and Git Credential Manager 2.7.3 are installed; configured commit
  identity is ALiShahzainAbbasi / alishahzain123@gmail.com. Local secret guard
  PASS. GitHub CLI is not installed. The in-app GitHub tab reached the official
  sign-in page; no credentials were entered and no repository or push exists yet.
Limitations/blockers:
  GitHub authentication is required before the private remote can be created.
  This publication task does not change the 16/151 product checkpoint count.
Next:
  Prepare and verify the local initial commit, then have the owner complete the
  GitHub sign-in and create/push the private mask-market-intelligence repository.

2026-09-04 20:29 PKT (UTC+05:00) | PUBLISH-002 | REPOSITORY PUBLICATION | COMPLETE
Work:
  Created the private GitHub repository
  https://github.com/ALiShahzainAbbasi/mask-market-intelligence, attached it as
  origin, and pushed the committed main branch. Added the canonical repository
  link to README. The new remote contains the project Git root only; parent
  attachments, local .env, dependencies, caches, and build output remain excluded.
Verification:
  Initial local commit c6384b6 contains 210 files and 23,860 inserted lines.
  GitHub showed the newly created private repository and its verified HTTPS clone
  URL. Push main -> main succeeded, upstream tracking was set, and local HEAD plus
  origin/main both resolved to c6384b6e5ba57676706751aee8ba8bad2d14ef31
  before this publication-record update.
Limitations/blockers:
  Codex's bundled Git 2.53.0 lacked git-remote-https. With owner approval,
  installed standard Git for Windows 2.55.0.3 through winget. Its first push
  rejected the sandbox/user ownership mismatch; the successful retry used a
  one-command safe.directory override scoped to this exact project and did not
  add a global trust exception. Product counts remain 16/151; publication is an
  operational event, not a product checkpoint.
Next:
  Commit and push this final README/tracker publication record, verify remote main
  matches local HEAD, then resume P03-04 when requested.

2026-09-08 23:18 PKT (UTC+05:00) | SUPABASE-001 | D14 / P01-05 / P02-01 / P02-02 | IN_PROGRESS
Work:
  User selected Supabase as the database and asked to finish database,
  authentication, and related foundations before continuing feature code. Recorded
  D14: Supabase provides managed PostgreSQL/pgvector while the API, web, worker,
  Alembic, queue, and application-owned authentication remain Windows-native and
  modular. Supabase Auth and direct browser database access are outside this choice.
Verification:
  Existing ignored .env key names were inspected without printing values. Both
  MASK_DATABASE_URL and MASK_MIGRATION_DATABASE_URL still target the previous
  local 127.0.0.1:5433 service and do not explicitly request TLS. No Supabase
  credential is configured, no database connection or migration was attempted,
  and counts remain 16/151 complete.
Limitations/blockers:
  Live acceptance requires private Supabase application and migration URLs. The
  existing destructive database-cycle harness must remain blocked from a shared
  hosted database; this work will add a safe Supabase-compatible acceptance path
  rather than weakening that protection. No real account will be bootstrapped
  until the target database and migration result are verified.
Next:
  Add a Supabase deployment ADR, TLS/connection-mode validation, safe hosted
  migration and integration profiles, documentation/tests, and then run the
  complete offline quality suite. Execute live migration/auth acceptance only
  after the owner privately configures the approved Supabase URLs.

2026-09-08 23:48 PKT (UTC+05:00) | SUPABASE-002 | D14 / P01-05 / P02-01 / P02-02 | IN_PROGRESS / PLAN HANDOFF
Work:
  Implemented the offline Supabase/auth foundation: explicit local/Supabase
  target validation, TLS and safe connection-mode requirements, bounded runtime
  pooling, migration isolation, a private mask schema, least-privilege role
  provisioning, hosted-test guards, and migrations 0005/0006. Added an explicit
  auth-route toggle, current-session endpoint, persisted CSRF validation, and a
  tenant-derived administrator role-update flow with last-admin protection and
  transactional audit data. Paused further expansion to present an evidence-based
  execution plan as requested.
Files:
  apps/api/src/mask_api/config.py, apps/api/src/mask_api/database.py,
  apps/api/src/mask_api/persistence/targets.py,
  apps/api/src/mask_api/modules/identity/, apps/api/src/mask_api/persistence/,
  apps/api/migrations/versions/0005_private_application_schema.py,
  apps/api/migrations/versions/0006_membership_role_audit.py,
  apps/api/tests/, scripts/check_services.py,
  scripts/provision_supabase_role.py, package.json, progress.txt.
Verification:
  Installed uv 0.12.9 and created the frozen environment with the available
  bundled Python 3.12.14. Focused affected suite PASS: 163 tests; changed-scope
  Ruff PASS. Initial focused runs exposed two implementation defects and two
  stale assertion patterns; each was corrected before the final passing run.
  No Supabase connection, migration, bootstrap, or live authentication flow was
  run. Full Python/web regression, mypy, OpenAPI/schema generation, architecture,
  and final secret checks remain NOT RUN for this partial slice.
Limitations/blockers:
  No Supabase project URLs are configured; ignored .env still points to the old
  local target. The deployment ADR, environment examples, and operational docs
  are not synchronized, and the live role-store integration case is still
  missing. The verified interpreter is one patch newer than the pinned 3.12.13.
  Changes are uncommitted and unpushed. No master checkpoint closes, so counts
  remain 16/151 complete and 135 remaining.
Next:
  Review the phased plan with the owner, finish and fully verify the offline
  Supabase/auth slice, then configure a dedicated Supabase project privately and
  execute safe migration, permission, authentication, and role-audit acceptance.

2026-09-09 00:40 PKT (UTC+05:00) | SUPABASE-003 | D14 / P01-05 / P02-01 | REVIEW_PENDING / CONNECTION PAUSED
Work:
  Began the owner-requested live Supabase connection. Re-read the project and
  Windows/modularity constraints, inspected only redacted environment status,
  confirmed both configured database URLs still target 127.0.0.1:5433, opened
  the official Supabase dashboard, and selected GitHub sign-in. Stopped before
  GitHub's Supabase OAuth authorization action and preserved the browser page.
Files:
  progress.txt only. No secret, environment, database, account, or source-code
  value was changed during this connection attempt.
Verification:
  Official Supabase sign-in redirected to GitHub's authorization page for the
  signed-in ALiShahzainAbbasi account. The visible request grants Supabase
  read-only access to GitHub email addresses and redirects through
  auth.supabase.io. No authorization was granted, no project was selected or
  created, no credential was retrieved, and no database connection was run.
Limitations/blockers:
  Computer-control confirmation policy requires explicit owner confirmation
  immediately before sharing the GitHub email data and granting this OAuth
  access. The application cannot receive a Supabase connection URL until that
  login completes and an existing dedicated development project is identified.
  Counts remain 16/151 complete and 135 remaining.
Next:
  With explicit owner confirmation, authorize Supabase on GitHub, inspect the
  available projects, and continue the least-privilege connection without
  printing or committing credentials.

2026-09-09 01:09 PKT (UTC+05:00) | SUPABASE-004 | D14 / P01-05 / P02-01 | REVIEW_PENDING / PROJECT CREATION PAUSED
Work:
  Continued after the owner's explicit GitHub OAuth approval. Completed the
  Supabase dashboard sign-in, inspected the existing Free-plan organization and
  project list, and opened the new-project form after no usable project appeared.
  Reviewed current Supabase connection, Data API, and breaking-change guidance:
  this persistent Windows backend should use direct IPv6 where available or
  session-mode port 5432 on IPv4, require TLS, and keep the Data API disabled.
Files:
  progress.txt only. The ignored .env, database, cloud project state, and source
  code remain unchanged.
Verification:
  Dashboard session is authenticated for the owner's GitHub-backed account. The
  organization reports a Free plan; its project page displayed no project card.
  The new-project form is open and exposes project name, generated database
  password, region, Data API, automatic table exposure, automatic RLS, and the
  final Create new project action. No project or database credential was created.
Limitations/blockers:
  Creating the project is an external cloud side effect that generates persistent
  database access credentials. Computer-control policy requires explicit owner
  confirmation at this final action stage. A region and generated password have
  not yet been selected, and the application cannot be connected before creation
  completes. Counts remain 16/151 complete and 135 remaining.
Next:
  With explicit owner confirmation, create a Free-plan project named MASK Market
  Intelligence in the nearest available South Asia region, disable the Data API
  and automatic exposure, retain the credential privately, then configure .env,
  provision mask_app, migrate, and run safe live acceptance.

2026-09-09 19:55 PKT (UTC+05:00) | SUPABASE-005 | D14 / P01-05 / P02-01 | REVIEW_PENDING / PASSWORD HANDOFF
Work:
  After explicit owner approval, created the dedicated Free-plan MASK Market
  Intelligence Supabase project in South Asia (Mumbai), disabled the Data API and
  automatic table exposure, and waited for Healthy status. Verified this Windows
  host can reach the project's direct IPv6 endpoint, selected direct port 5432,
  and staged separate postgres migration and mask_app runtime credentials in the
  ignored local .env with explicit TLS. The initial generated database password
  was not recoverable after project creation, so a replacement was generated via
  a one-shot loopback-only bridge and filled into Supabase's reset dialog.
Files:
  Ignored .env updated privately; progress.txt updated. A temporary untracked
  helper exists outside the project at work/supabase-secret-bridge.py until the
  connection handoff completes. Source code and committed configuration: unchanged.
Verification:
  Supabase dashboard shows project mtcmciyipxxiwfidrtxh Healthy on NANO compute
  in ap-south-1. Data API and automatic exposure were visibly disabled at project
  creation. Direct DNS resolved to IPv6 and TCP port 5432 PASS; session-pooler TCP
  also PASS but is not selected. Redacted Settings validation PASS: target
  supabase, official matching direct hosts, runtime role mask_app, migration role
  postgres, sslmode=require for both, hosted safe tests enabled, auth routes off.
  Git confirms .env is ignored. No secret was printed. No live SQL, role creation,
  migration, integration test, or authentication bootstrap has run yet.
Limitations/blockers:
  The computer-control safety policy forbids automating the final submit action
  of a password change. The owner must click the visible Reset password button;
  until then the staged migration credential intentionally cannot authenticate.
  The asynchronously loaded organization was later found to contain the unrelated
  Nexa Fund backend project; it was not changed. Counts remain 16/151 complete
  and 135 remaining.
Next:
  Owner clicks Reset password and reports completion. Then verify postgres login,
  provision and verify mask_app, migrate through 0006, and run non-destructive
  Supabase schema/permission/TLS/integration acceptance.

2026-09-09 20:31 PKT (UTC+05:00) | SUPABASE-006 | P01-05 / P02-01 | COMPLETE
Work:
  Connected the Windows application to the dedicated Supabase project, provisioned
  the restricted mask_app login, applied Alembic 0001-0006, and switched the
  runtime to the IPv4 session pooler after one transient direct-IPv6 timeout.
  Corrected ORM flush ordering so a new server session exists before its audit
  event, made TLS readiness inspect the client-to-endpoint connection, normalized
  PostgreSQL Enum CHECK reflection acceptance, and made role provisioning support
  safe Supabase-compatible runtime-password rotation.
Files:
  Ignored .env; apps/api/src/mask_api/database.py, health.py, identity/auth_repository.py;
  scripts/provision_supabase_role.py; integration/database-target tests; migrations
  0005/0006 formatting; progress.txt and progress checker.
Verification:
  PostgreSQL 17.6, TLS, pgvector 0.8.2, Alembic 0006, 12 private mask tables,
  restricted mask_app flags, no runtime CREATE privileges, and no mask schema
  usage for anon/authenticated/service_role verified. Live non-destructive suite:
  18 PASS. Offline suite: 248 Python + 11 web PASS. Ruff/format PASS; mypy PASS
  over 88 files; architecture, OpenAPI, generated TypeScript schema, and local
  secret guard PASS. The dashboard password dialog was closed.
Limitations/blockers:
  A failed low-level connection traceback exposed the generated mask_app-only
  runtime credential; it was immediately replaced and the new credential verified.
  The Supabase admin password was not exposed by the traceback. Windows Gitleaks
  ran and reported three redacted findings that still require file/type triage,
  so P01-08 remains open. API/worker/browser auth bootstrap was not run, and
  P02-02 remains in progress. Counts become 18/151 before the new A01-A20 scope.
Next:
  Implement A01 from the new autonomous build specification; separately triage
  Gitleaks without printing secrets and continue P02-02 only when useful.

2026-09-09 20:31 PKT (UTC+05:00) | AUTONOMOUS-001 | A01-A20 | A01 IN_PROGRESS
Work:
  Accepted the owner-supplied Autonomous Market Intelligence Engine Build
  Specification v1 as the functional formula/procedure source. Added the 20
  source-defined A01-A20 checkpoints without removing or renumbering the original
  151 checkpoints. Started A01: versioned formulas, strict market configuration,
  source profiles, shared deterministic math, documentation, and tests.
Files:
  progress.txt and scripts/check-progress.mjs. Source DOCX remains read-only.
Verification:
  Extracted all 228 paragraphs and 25 tables with bundled document tooling.
  The specification contains exact M1-M10, confidence, gate, veto, source, and
  acceptance rules. DOCX raster rendering is unavailable because the resolved
  workspace dependency bundle contains no LibreOffice executable; content-level
  extraction succeeded and no DOCX output is being authored.
Limitations/blockers:
  A01 implementation and tests are active. Paid APIs, LLM calls, collection,
  outreach, and campaign spending remain disabled. Overall tracker denominator
  changes from 151 to 171; 18 are complete and 153 remain.
Next:
  Finish A01, run focused/full static tests, update docs and tracker, then begin
  A02 LocalArtifactStore through explicit CLI persistence wiring.

2026-09-09 20:57 PKT (UTC+05:00) | AUTONOMOUS-002 | A01 | DONE
Work:
  Implemented the first functional checkpoint from the owner-supplied build
  specification. Added strict frozen market, formula, source-profile, confidence,
  gate, veto, completeness, and sample contracts; canonical validated-content
  SHA-256 loaders; exact v1 M1-M10 configuration; an opt-in US public-source
  profile; a Windows-ready HVAC sample market; generated market JSON Schema;
  and Decimal implementations of clamp10, linear, reverse_linear, log_scale,
  ratio_score, decay, weighted_mean, and safe_rate. UNKNOWN remains distinct
  from zero, invalid domains fail explicitly, and all network/paid sources are
  disabled by default. Documented the CLI/module contract and reconciled the
  earlier provisional scoring text with the newly approved v1 rules.
Files:
  AGENTS.md; README.md; apps/api/pyproject.toml;
  apps/api/src/mask_api/modules/evidence/AGENTS.md;
  apps/api/src/mask_api/modules/scoring/{__init__.py,math.py};
  apps/api/src/mask_api/research_runner/{__init__.py,configuration.py,contracts.py};
  apps/api/tests/{research_runner,scoring}/; configs/formulas/v1.yaml;
  configs/markets/us_hvac_10_99.yaml;
  configs/sources/default_us_public.yaml;
  configs/schemas/market.schema.json; docs/AUTONOMOUS_RESEARCH_MODE.md;
  docs/MODULARITY.md; docs/SCORING.md; package.json;
  scripts/export-research-schemas.py; progress.txt; uv.lock.
Verification:
  `uv sync --frozen` PASS (47 packages checked). Focused A01 suite PASS: 28.
  Full offline Python suite PASS: 276 passed, 25 integration tests deselected;
  web suite PASS: 11. Ruff lint/format PASS over 176 files; web ESLint/Prettier
  PASS; strict mypy PASS over 93 source files; web type generation/TypeScript
  PASS; architecture/import-cycle check PASS; OpenAPI/TypeScript schema drift
  PASS; research JSON Schema drift PASS; local secret guard PASS. The only test
  warning specific to the environment is denied pytest cache writing; test
  execution/results are unaffected.
Limitations/blockers:
  No database schema or runtime wiring changed, so the 18 previously passing
  Supabase live tests were not rerun for A01. Windows Gitleaks was not rerun;
  its prior three redacted findings remain an explicit P01-08 triage item. A01
  defines/validates formulas but does not yet calculate whole methods; those
  implementations remain A11-A16. No network, LLM, outreach, campaign, or paid
  research ran.
Next:
  Implement A02 LocalArtifactStore behind an explicit artifact persistence port,
  with atomic bounded writes, safe run paths, lineage, and offline tests. Then
  implement A03 ResearchRunner CLI against that port.

2026-09-09 20:58 PKT (UTC+05:00) | AUTONOMOUS-003 | A02 | IN_PROGRESS
Work:
  Started the explicit local artifact persistence checkpoint after A01 passed.
  Scope is a typed port plus filesystem adapter for bounded atomic raw,
  normalized, evidence, metric, report, manifest, lineage, and log artifacts.
  It must reject path traversal and never activate as a database fallback.
Files:
  progress.txt. The temporary workspace-only Supabase secret bridge helper was
  deleted after connection setup; it was outside the repository.
Verification:
  Predecessor A01 and tracker checks PASS (19/171 complete).
Limitations/blockers:
  A02 code and acceptance tests are active. No database, network, or paid service
  is required or authorized for this adapter.
Next:
  Define the artifact contracts/port, implement atomic local writes, add hostile
  path/limit/idempotency tests, and rerun the full static suite.

2026-09-09 21:09 PKT (UTC+05:00) | AUTONOMOUS-004 | A02 | DONE
Work:
  Implemented a typed `ArtifactStore` port and explicit `LocalArtifactStore`
  adapter. The adapter writes exact bytes atomically into fixed raw, normalized,
  evidence, metrics, reports, manifests, lineage, and logs namespaces. It returns
  content SHA-256 receipts, makes identical writes idempotent, rejects immutable
  conflicts, emits deterministic JSON, blocks absolute/traversal/backslash/drive/
  Windows-device/ambiguous paths, and enforces positive file/count/run byte hard
  limits. Run outputs are ignored by Git. No API/worker fallback wiring was added.
Files:
  .gitignore; README.md;
  apps/api/src/mask_api/research_runner/{__init__.py,artifacts.py,ports.py,local_artifacts.py};
  apps/api/tests/research_runner/test_local_artifacts.py;
  docs/AUTONOMOUS_RESEARCH_MODE.md; progress.txt.
Verification:
  Focused research-runner suite PASS: 38. Full offline Python suite PASS:
  305 passed, 25 integration tests deselected; web suite PASS: 11. Ruff
  lint/format PASS over 180 files; strict mypy PASS over 96 source files;
  web ESLint/Prettier/type generation/TypeScript PASS; architecture/import-cycle
  PASS; OpenAPI/TypeScript and research-schema drift checks PASS. Tests cover all
  eight artifact kinds, exact binary preservation, deterministic JSON, listing,
  idempotency, conflict, hostile paths, Windows reserved names, byte/count limits,
  missing artifacts, invalid roots/limits, and failed atomic-write cleanup.
Limitations/blockers:
  A02 is a local filesystem adapter only; A03 must compose it explicitly. It does
  not replace Supabase or evidence database persistence. The environmental pytest
  cache permission warning remains non-blocking. Database integration was not
  rerun because A02 has no SQL/runtime wiring. No network or paid operation ran.
Next:
  Implement A03 command parsing and ResearchRunner orchestration with config hash
  manifest, selected-method validation, resume behavior, hard budgets, explicit
  LocalArtifactStore composition, and fail-soft partial run summaries.

2026-09-09 21:25 PKT (UTC+05:00) | AUTONOMOUS-005 | A03 | IN_PROGRESS
Work:
  Started the one-command ResearchRunner slice. The runner will load/hash the A01
  contracts, compose A02 local persistence explicitly, validate method selection,
  write append-only run/method manifests, enforce a shared hard budget ledger,
  resume only compatible output directories, and isolate method failures so
  honest partial results survive.
Files:
  progress.txt.
Verification:
  A01-A02 predecessor checks PASS; 20/171 checkpoints complete.
Limitations/blockers:
  Source executors remain separate A04-A10 work. An uninstalled or disabled
  method must stay UNKNOWN rather than generate placeholder evidence or scores.
Next:
  Add runner/budget/result contracts, CLI parsing/composition, fake-executor and
  subprocess acceptance tests, then execute the documented command locally.

2026-09-09 21:39 PKT (UTC+05:00) | AUTONOMOUS-006 | A03 | DONE
Work:
  Implemented `python -m mask_api.research_runner run <market> --methods <all|list>
  --output <run-dir>`. The application validates matching market/formula/source
  profiles, canonicalizes method order, writes immutable configuration lineage
  and append-only checkpoints, resumes compatible interrupted/terminal runs,
  rejects incompatible/non-manifest output, isolates executor failures, and keeps
  M8/M10 UNKNOWN when live validation is disabled. Added a shared atomic budget
  ledger for requests, documents, bytes, duration, and paid USD cost; an attempted
  overage is rejected before usage changes and stops later dispatch. No raw
  exception text is persisted.
Files:
  README.md; apps/api/src/mask_api/research_runner/{__init__.py,__main__.py,
  budgets.py,contracts.py,execution.py,runner.py};
  apps/api/tests/research_runner/{test_budgets.py,test_runner.py};
  configs/markets/us_hvac_10_99.yaml; configs/schemas/market.schema.json;
  docs/AUTONOMOUS_RESEARCH_MODE.md; progress.txt.
Verification:
  Focused research-runner suite PASS: 57. Full offline Python suite PASS:
  324 passed, 25 integration tests deselected; web suite PASS: 11. Ruff
  lint/format PASS over 186 files; strict mypy PASS over 100 source files;
  web ESLint/Prettier/type generation/TypeScript PASS; architecture/import-cycle,
  OpenAPI/TypeScript drift, research-schema drift, frozen dependency sync, and
  local secret guard PASS. Executed the exact documented CLI with all methods:
  it created checkpoint 12 as `partial` because no source executors are installed;
  the second execution safely resumed the same checkpoint with exit code 2.
Limitations/blockers:
  A03 deliberately does not pretend to scrape: uninstalled executors produce
  `method.executor_unavailable` and UNKNOWN results. Actual permitted discovery
  and source adapters begin in A04-A08. No database integration rerun was needed;
  A03 is local orchestration only. No live network, paid API, LLM, outreach, or
  campaign ran. Environmental pytest cache and Starlette/httpx deprecation
  warnings remain non-blocking.
Next:
  Build A04 deterministic query generation and an optional Brave Search adapter
  that is disabled without explicit source enablement, credential, current policy,
  and positive cost budget; retain discovery provenance for A05-A10 collectors.

2026-09-09 21:40 PKT (UTC+05:00) | AUTONOMOUS-007 | A04 | IN_PROGRESS
Work:
  Started deterministic source discovery and the optional Brave Search edge
  adapter. Discovery queries will derive only from validated market terms,
  problems, buyers, geography, and method-specific intent templates. The network
  adapter will remain separate, bounded, credential-injected, and provenance-rich.
Files:
  progress.txt.
Verification:
  A03 completed with the documented CLI smoke/resume behavior.
Limitations/blockers:
  Live Brave execution requires a user-provided credential plus explicit paid
  source/budget enablement; neither is assumed. Official API behavior is being
  verified before implementing the adapter.
Next:
  Confirm the current official endpoint/header/response contract, implement pure
  query generation plus transport port/parser, and add fixture-only acceptance.

2026-09-09 21:52 PKT (UTC+05:00) | AUTONOMOUS-008 | A04 | DONE
Work:
  Implemented bounded deterministic discovery-query plans for M1-M7/M9 from
  validated market terms/problems/buyers/geography, with stable IDs, canonical
  ordering, exact dedupe, source-term lineage, and provider query-size limits.
  M8/M10 intentionally generate no public-search queries. Added the optional
  Brave Web Search adapter behind injected transport, source/profile checks,
  explicit enable/policy/key gates, positive paid budget/request cost, worst-case
  preflight capacity, strict safe search, fixed origin/API version, bounded pages,
  no ambient proxy/cookies/redirects, raw response preservation/hash, URL safety,
  rank/query/page/time provenance, exact usage accounting, and safe error classes.
Files:
  README.md; apps/api/src/mask_api/modules/discovery/{__init__.py,contracts.py,
  query_planner.py,brave.py}; apps/api/src/mask_api/research_runner/budgets.py;
  apps/api/tests/discovery/ including two JSON fixtures; docs/DISCOVERY.md;
  docs/MODULARITY.md; progress.txt.
Verification:
  Official Brave documentation confirmed endpoint, required subscription-token
  header, 600-character/75-word query limits, count max 20, offset max 9,
  `more_results_available`, and optional dated Api-Version header. Focused A04/
  budget suite PASS: 27. Full offline Python suite PASS: 344 passed, 25 integration
  tests deselected; web PASS: 11. Ruff/format PASS over 193 files; strict mypy PASS
  over 104 source files; web lint/format/types, architecture/import-cycle,
  OpenAPI/TypeScript drift, research-schema drift, and local secret guard PASS.
Limitations/blockers:
  The Brave adapter is not composed into a method executor and cannot run without
  explicit policy approval, injected key, positive cost estimate, and positive
  paid run budget. No subscription/key was created and no live/paid request ran.
  Discovery results remain candidates, not evidence, until a registered downstream
  source policy permits collection.
Next:
  Implement A05 official government adapters and normalized metric observations;
  keep keyless sources usable and BEA/SAM credential paths disabled without keys.

2026-09-09 21:52 PKT (UTC+05:00) | AUTONOMOUS-009 | A05 | IN_PROGRESS
Work:
  Started fixture-backed official US data adapters for Census CBP, BLS, BEA, SEC
  EDGAR, and SAM.gov. Shared behavior will cover fixed official origins, explicit
  request contracts, credential injection where required, budget reservation,
  raw response/hash provenance, typed observations, and safe parser failures.
Files:
  progress.txt.
Verification:
  A04/full static suite PASS before A05 start.
Limitations/blockers:
  Credential-gated live smoke tests can only run when the user supplies official
  keys and explicitly enables them. Fixture acceptance does not claim live data.
Next:
  Verify official endpoint/field contracts, then implement and test each parser
  without network calls.

2026-09-09 22:29 PKT (UTC+05:00) | AUTONOMOUS-010 | A05 | IN_PROGRESS
Work:
  Implemented modular official-data contracts, validated request builders,
  source-neutral observation/record parsers, a fixed-origin urllib transport,
  and an opt-in adapter for Census CBP, BLS, BEA Regional, SEC EDGAR submissions,
  and SAM.gov opportunities. Dispatch now requires a matching official_api
  profile plus explicit enablement and policy approval; required credentials stay
  in SecretStr fields and stored provenance records only credential field names.
  Added current API bounds including explicit Census NAICS vintage, registered/
  unregistered BLS caps, SEC contact User-Agent, and SAM one-year/page bounds.
  Exact raw bytes/hash/parser version are retained; invalid rows stay issues,
  exact duplicates do not inflate normalized results, and empty valid responses
  do not invent data. The SAM parser excludes point-of-contact data and echoed
  links while retaining bounded procurement and award fields.
Files:
  .env.example; README.md; configs/sources/default_us_public.yaml;
  apps/api/src/mask_api/modules/official_data/{__init__.py,contracts.py,errors.py,
  parsers.py,requests.py,transport.py}; apps/api/tests/official_data/ including
  five JSON fixtures and request/parser/transport/live-smoke tests;
  docs/{AUTONOMOUS_RESEARCH_MODE.md,MODULARITY.md,OFFICIAL_DATA.md}; progress.txt.
Verification:
  Current official Census, BLS, BEA, SEC, and SAM documentation checked for
  endpoint, authentication/contact, parameter, pagination, and query limits.
  Focused A05 offline suite PASS: 32; credential-gated live suite SKIPPED: 5 as
  designed. Full offline Python PASS: 376 passed, 30 integration tests deselected;
  web PASS: 11. Ruff/format PASS over 204 files; strict mypy PASS over 110 source
  files; web ESLint/Prettier/type generation/TypeScript, architecture/import-cycle,
  OpenAPI/TypeScript drift, research-schema drift, git diff whitespace, and local
  secret guard PASS. Windows Gitleaks reran and again reported three redacted
  findings; this remains the pre-existing P01-08 triage item.
Limitations/blockers:
  A05 is not DONE because its acceptance requires a deliberately small live run.
  The environment has no authorized Census/BLS/BEA/SAM keys, source-policy/run
  flags, or official contact email; no live call was attempted. Adapters are not
  yet composed into method executors because exact source-to-formula transforms
  are A11-A12, and API/worker persistence remains Phase 3. Fixtures are not market
  evidence. No paid API/model, broad crawl, outreach, or campaign ran.
Next:
  Owner: locally provide MASK_OFFICIAL_CONTACT_EMAIL and applicable official API
  keys, then explicitly set MASK_OFFICIAL_POLICY_APPROVED=1 and
  MASK_RUN_LIVE_OFFICIAL_SMOKE=1 for the five-call harness. After it passes,
  mark A05 DONE and begin A06 permitted discussion/web source adapters.

2026-09-09 22:55 PKT (UTC+05:00) | AUTONOMOUS-011 | A05 / A06 | REVIEW_PENDING / IN_PROGRESS
Work:
  Owner directed that APIs which are not freely available be put on hold and not
  block forward coding. Recorded A05 live acceptance as REVIEW_PENDING rather
  than falsely complete, and started A06 on the zero-paid-cost permitted paths.
  The source profile will record operational availability separately from policy
  and cost so disabled paid/credential sources cannot be activated accidentally.
Files:
  progress.txt.
Verification:
  A05 offline implementation remains at 32 passing tests; full offline suites and
  static gates from AUTONOMOUS-010 remain PASS. No source/API call was made.
Limitations/blockers:
  A05's live government smoke awaits optional credentials. Google Places and
  Brave are paid and held. YouTube and approved Reddit need account credentials/
  approval and are held until available. SEC, RSS/Atom, static HTML, and bounded
  keyless BLS capability remain usable under an approved current source policy.
Next:
  Add executable source availability states, source-family lineage/dedupe, and
  conservative declared-persona normalization; reuse the tested RSS/Atom and
  static-HTML collectors without creating duplicate network/parser stacks.

2026-09-09 23:07 PKT (UTC+05:00) | AUTONOMOUS-012 | A06 | IN_PROGRESS
Work:
  Added executable source operational states: available, credential_pending,
  approval_pending, and paid_hold. The default lean profile now keeps Google
  Places, Brave, firmographics, and validation accounts on paid hold; keeps
  account-dependent APIs pending; and leaves SEC, bounded keyless BLS, RSS/Atom,
  and registered static HTML available but still disabled/policy-gated. Brave and
  official adapters reject held sources before network access. Extended the
  common evidence contract with source-family lineage, duplicate-family links,
  distinct-family metrics, and the approved seven-value persona taxonomy. Added
  deterministic declared-persona-v1 normalization: only an explicit unambiguous
  public role hint is classified; names, absent hints, and mixed roles stay
  UNKNOWN. Reused the existing bounded RSS/Atom and static-HTML collectors.
Files:
  README.md; configs/sources/default_us_public.yaml and generated source schema;
  apps/api/src/mask_api/modules/{discovery/brave.py,evidence/contracts.py,
  evidence/domain.py,evidence/normalization.py,evidence/services.py,
  official_data/transport.py}; apps/api/src/mask_api/research_runner/contracts.py;
  apps/api/tests/{conftest.py,discovery/test_brave.py,evidence/test_normalization.py,
  evidence/test_service.py,official_data/test_live_official_smoke.py,
  official_data/test_transport.py,research_runner/test_configuration.py};
  docs/{AUTONOMOUS_RESEARCH_MODE.md,COLLECTORS.md,MODULARITY.md,
  SOURCE_AVAILABILITY.md}; progress.txt.
Verification:
  A06 evidence/config focused suite PASS: 46. Combined collector/discovery/
  official/runner suite PASS: 148 with 5 live tests deselected. Full offline
  Python PASS: 379 passed, 30 integration tests deselected; web PASS: 11.
  Ruff/format PASS over 205 files; strict mypy PASS over 110 source files; web
  ESLint/Prettier/type generation/TypeScript, architecture/import-cycle,
  OpenAPI/TypeScript drift, and regenerated research-schema drift PASS.
Limitations/blockers:
  A06 is not complete because YouTube and Reddit adapters require unavailable
  credentials/provider approval and Google Places is paid; owner explicitly put
  these on hold. RSS/Atom and static HTML require actual registered, approved,
  current source-policy records before live use. No crawler, API/worker route,
  source request, paid service, model call, outreach, or campaign was activated.
Next:
  Continue the no-paid path with A07 intent-taxonomy and cache contracts while
  holding Google Ads Keyword Planner network access, or proceed to A09/A10 local
  extraction validation if the taxonomy alone cannot satisfy M6 metrics.

10. HISTORY ENTRY TEMPLATE
------------------------------------------------------------------------
2026-09-11 19:25 PKT (UTC+05:00) | AUTONOMOUS-031 | A17.5 | IN_PROGRESS
Work:
  Added the fifth A17.5 live-capable source: a targeted Common Crawl
  retriever (mask_api.modules.common_crawl_data) for historical/fallback
  evidence on an already source-policy-approved URL. Verified the real
  mechanics before writing code rather than guessing: fetched
  index.commoncrawl.org's real collinfo.json and CDX JSON response shape
  (confirmed newline-delimited JSON, and that a miss returns HTTP 404 with
  a `{"message": "No Captures found..."}` body, not an empty 200), then
  fetched one real WARC record from data.commoncrawl.org with an HTTP
  Range header and confirmed `206 Partial Content` with a byte-exact body.
  Built `cdx.py` (request validation, NDJSON parsing), `warc.py` (parses
  the decompressed WARC record: WARC headers, then -- for
  WARC-Type: response -- the embedded HTTP status/headers/body), and
  `transport.py`/`retriever.py` (CDX lookup then exactly one ranged WARC
  fetch, both budget-gated, disabled by default). Found and fixed a real
  parsing bug while verifying warc.py against the actual downloaded
  record: without truncating the payload to the WARC record's own
  Content-Length before splitting out the HTTP body, the mandatory
  trailing WARC record terminator (CRLF CRLF) leaked into the extracted
  body (563 bytes returned instead of the correct 559); fixed and
  re-verified byte-exact against the same real record.
  Per the owner's explicit instruction to extend the existing static-HTML
  crawler rather than build a new scraper, `retriever.fetch_resource()`
  deliberately produces a plain `evidence.contracts.FetchedResource` (the
  same contract a live fetch produces) instead of parsed text, and
  `integration.py` calls the *existing*
  `evidence.collectors.static_html.StaticHtmlCollector.parse()` and
  `evidence.normalization.normalize_document()` completely unchanged to
  do the HTML extraction and lineage/dedup work -- zero duplicated
  parsing logic. Added `CollectorKind.COMMON_CRAWL` to
  `evidence.domain` (a small additive enum value) so normalized documents
  can carry accurate lineage. `find_capture()` reuses
  `evidence.policy.require_allowed_fetch_url()` with the caller's
  `SourcePolicy`, so this module can only retrieve a URL that policy would
  also approve for a live fetch -- never a bypass path. Every retrieval
  carries a `CommonCrawlProvenance` record (collection ID, capture
  timestamp, WARC filename/offset/length, retrieval timestamp) so a caller
  can apply the recency penalty the owner's directive requires rather than
  presenting archived content as current. Added `docs/COMMON_CRAWL.md`.
Files:
  apps/api/src/mask_api/modules/common_crawl_data/{AGENTS.md,__init__.py,
  contracts.py,errors.py,cdx.py,warc.py,transport.py,retriever.py,
  integration.py};
  apps/api/src/mask_api/modules/evidence/{domain.py,AGENTS.md} (added
  CollectorKind.COMMON_CRAWL and a note pointing to the new module);
  apps/api/tests/common_crawl_data/{conftest.py,test_cdx.py,test_warc.py,
  test_transport.py,test_retriever.py,test_integration.py,
  test_live_common_crawl_smoke.py};
  docs/{COMMON_CRAWL.md,AUTONOMOUS_RESEARCH_MODE.md,SOURCE_AVAILABILITY.md};
  README.md; progress.txt.
Verification:
  New focused suite PASS: 41 tests (CDX request validation and NDJSON
  parsing including the real 404 "no captures" shape and malformed-row
  rejection; WARC parsing including the terminator-truncation fix, non-
  response rejection, missing-header rejection, and malformed-status-line
  rejection; transport URL/Range-header construction and unsafe-filename
  rejection; the retriever's 404-as-None handling, most-recent-capture
  selection, source-policy-scope enforcement before any network call,
  enabled/policy-approval gates, short-range/non-206/disallowed-content-
  type/non-2xx-archived-status rejection, and record-size-cap enforcement
  before any fetch; and an integration suite proving
  to_parsed_document/to_normalized_document genuinely reuse the existing
  static-HTML/normalize pipeline and correctly stamp Common Crawl
  provenance into metadata without discarding the parser's own metadata).
  A gated live smoke test
  (test_live_common_crawl_find_capture_and_fetch_resource) ran a real CDX
  lookup and ranged WARC fetch for https://example.com/ against
  CC-MAIN-2026-34 and PASSED in 8 seconds, including the full reuse path
  into a parsed document with the correct extracted title. Full offline
  suite PASS: 696 (was 655 after O*NET's fix, see the next entry below),
  33 integration tests deselected (32 plus this new live smoke, which ran
  successfully outside the deselected count) -- confirmed via the
  project's actual root-scoped `pnpm run test:unit`. Ruff check/format
  PASS over 349 files; strict mypy PASS over 187 source files
  (`apps/api/src` and `workers`). Architecture/import-cycle check PASS
  (the new `CollectorKind` member introduced no exhaustiveness break --
  grepped for every `CollectorKind.` usage first). `research:schemas:check`
  PASS with no drift. Local secret guard and pinned Gitleaks source scan
  both PASS. Node.js/pnpm confirm the full web suite (11 tests), web
  lint, and web typecheck all PASS unaffected. `pnpm check:progress` PASS
  at 31/172 (unchanged -- A17.5 remains one in-progress checkpoint).
Limitations/blockers:
  This module is not yet wired into `evidence.services.CollectionService`
  or any `MethodExecutor` -- it produces correct, reusable pipeline
  output (proven by the integration tests and the live smoke), but full
  orchestration (retry/circuit-breaker policy, persistence dispatch) is
  later work, matching A12/A13's "calculator built, not yet wired"
  precedent. `find_capture()` currently requires the caller to name an
  exact `collection_id` (e.g. "CC-MAIN-2026-34"); automatically resolving
  "the current/latest collection" from collinfo.json was not built in
  this pass to keep scope to what the owner's directive named.
Next:
  Build the provider-independent source/evidence fallback registry
  (EvidenceRequirement mapped to Gemini/YouTube/USAspending/O*NET/Common
  Crawl/RSS/static-HTML with cost/credential/quota/quality/freshness/
  fallback-priority metadata, Tier A-E source-quality classes where Tier E
  never directly scores), then the first real `MethodExecutor`
  implementations wiring these sources into M1-M7/M9 -- toward one real,
  source-backed calibration market report at $0 paid-data cost.

2026-09-11 18:58 PKT (UTC+05:00) | AUTONOMOUS-030 | A17.5 | IN_PROGRESS
Work:
  Added the fourth A17.5 live-capable source: an automated O*NET
  downloadable-database bootstrap/cache/import pipeline
  (mask_api.modules.onet_data) for M3 occupation/task mapping, using the
  official downloadable CSV database release (NOT the separate, unapproved
  O*NET Web Services API), per the owner's explicit instruction to do this
  setup automatically rather than ask the owner to download anything
  manually. Verified the real dataset before writing code: fetched
  onetcenter.org's real database page to get the current release (31.0)
  and its exact downloadable file list, downloaded the real ~16.2MB
  db_31_0_csv.zip to inspect it, and confirmed the exact column headers
  and blank-value behavior of the two tables this module imports
  (occupation_data.csv: O*NET-SOC Code/Title/Description;
  task_statements.csv: O*NET-SOC Code/Task ID/Task/Task Type/Incumbents
  Responding/Date/Domain Source -- Task Type and Incumbents Responding are
  sometimes blank in the real data and must stay None, never a guessed
  default). Built `contracts.py`, `parsers.py` (CSV parsing against the
  verified real headers), `transport.py` (HEAD + bounded download,
  disabled by default), `cache.py` (a small dedicated local filesystem
  cache -- deliberately not the per-run ArtifactStore, since this dataset
  is shared across every run, not scoped to one), and `bootstrap.py`
  (`ensure_dataset()`: HEAD-check first, skip the ~16MB download when a
  cached descriptor's Content-Length/ETag still match; `import_batch()`:
  extract and parse only the two needed CSV members from the ZIP, not all
  47 files). Since O*NET publishes no independent checksum for a release,
  the descriptor's SHA-256 is explicitly documented as a self-computed
  local integrity check, not verification against a publisher-supplied
  value. O*NET has no `configs/sources/default_us_public.yaml` entry --
  it is a shared cross-run reference dataset, not a per-request source,
  so it does not fit that registry's shape. Added `outputs/reference_data/`
  to .gitignore for the local cache location. Added `docs/ONET_DATA.md`.

  Attempted the live download twice against the real server (owner
  explicitly authorized doing this setup live, at zero cost, keyless).
  First attempt (~17 minutes) completed the download but `import_batch()`
  then failed with `zipfile.BadZipFile`: the cached archive was 14.68MB
  against a declared 16.24MB Content-Length. Root cause: the transport's
  single `response.read(n)` call had silently returned fewer bytes than
  promised over a slow/unstable connection, and nothing checked for that
  before caching. This was a real bug, found and fixed: (a)
  `UrllibOnetTransport.download()` now loop-reads in bounded 64KB chunks
  to true EOF instead of trusting one `.read(n)` call, catching
  `http.client.HTTPException`/`IncompleteRead` as a clean retryable error
  instead of letting it crash uncaught; (b) `_download_and_cache()` now
  explicitly compares the received byte count against the HEAD response's
  Content-Length and raises a retryable `onet.download_incomplete` error
  rather than ever writing a short read to cache. Deleted the corrupted
  cached archive and retried: the second attempt (~17 minutes) did not
  silently truncate again -- the connection genuinely dropped mid-
  transfer, which the fix now correctly surfaces as a caught
  `onet.network_error` instead of crashing or corrupting the cache,
  proving the fix works under the exact real adverse condition that
  caused the original bug. Neither attempt completed a full end-to-end
  pass; the observed effective throughput to onetcenter.org from this
  environment was roughly 16KB/s. This is recorded as an honest,
  documented environment/connection limitation, not a known code defect --
  the offline suite fully exercises both the success path and this exact
  incomplete-download detection path with a fake transport.
Files:
  apps/api/src/mask_api/modules/onet_data/{AGENTS.md,__init__.py,
  contracts.py,errors.py,parsers.py,cache.py,transport.py,bootstrap.py};
  apps/api/tests/onet_data/{test_parsers.py,test_cache.py,
  test_transport.py,test_bootstrap.py,test_live_onet_smoke.py,
  fixtures/{occupation_data.csv,task_statements.csv}};
  .gitignore; docs/{ONET_DATA.md,AUTONOMOUS_RESEARCH_MODE.md,
  SOURCE_AVAILABILITY.md}; README.md; progress.txt.
Verification:
  New focused suite PASS: 27 tests (parser extraction against the real-
  verified headers plus malformed-header/blank-value/duplicate-row
  handling, local cache round-trip/overwrite/corruption-detection/path-
  traversal-rejection, and the bootstrapper's download-once/skip-when-
  cache-matches/redownload-when-remote-changed/disabled-policy-not-
  approved/end-to-end-archive-extraction/cached-archive-checksum-
  mismatch/incomplete-download-rejection behavior -- the last of these
  added specifically to cover the bug found during live verification).
  Full offline suite PASS: 655 (was 628), 32 integration tests deselected
  (was 31 -- the new live smoke test is correctly gated and skipped by
  default) -- confirmed via the project's actual root-scoped `pnpm run
  test:unit`. Ruff check/format PASS; strict mypy PASS over 179 source
  files (`apps/api/src` and `workers`). Architecture/import-cycle check
  PASS. `research:schemas:check` PASS with no drift. Local secret guard
  and pinned Gitleaks source scan both PASS. Node.js/pnpm confirm the
  full web suite (11 tests), web lint, and web typecheck all PASS
  unaffected. `pnpm check:progress` PASS at 31/172 (unchanged -- A17.5
  remains one in-progress checkpoint).
Limitations/blockers:
  No live O*NET download has completed successfully end to end in this
  environment (see Work above for the full honest account); the offline-
  verified bootstrap/import logic is otherwise complete and correct. Not
  yet wired into any composition root or `MethodExecutor`. `task_ratings
  .csv` (39MB, importance/frequency ratings) and `work_activities.csv`
  (12MB) are intentionally not imported in this pass -- a documented,
  deferred follow-up, not a silent gap.
Next:
  Retry the live O*NET download when a more reliable connection is
  available (the code is ready; no further changes expected to be
  needed), or accept the offline verification as sufficient and proceed.
  Continue A17.5 with the targeted Common Crawl retriever (see the entry
  above -- built immediately after this one, in the same session) and
  then the provider-independent source/evidence fallback registry and the
  first real MethodExecutor wiring.

2026-09-10 21:55 PKT (UTC+05:00) | AUTONOMOUS-029 | A17.5 | IN_PROGRESS
Work:
  Added the third A17.5 live-capable source: a keyless USAspending.gov
  award search adapter for M4/M5 awarded-federal-contract-spend and
  incumbent evidence. Unlike Gemini and YouTube, USAspending is a genuine
  US government source, so it was added as a sixth entry inside the
  existing `mask_api.modules.official_data` module rather than a new one
  -- extended `OfficialSourceId` with `USASPENDING`, added
  `usaspending_award_search_request()` (POST to
  `api.usaspending.gov/api/v2/search/spending_by_award/`, fixed to
  procurement contract award types A/B/C/D, 1-10 bounded NAICS codes, an
  ordered date range no more than five years, and a fixed `fields` list;
  the exact request/response field names -- `NAICS` not `NAICS Code`,
  `generated_internal_id`, `Award Amount` -- were verified against the
  project's own published API contract doc, not guessed from memory,
  since an incorrect field name would silently produce an empty or wrong
  result against the real API later), `parse_usaspending()` (drops a
  result missing its award ID or recipient name as a parse issue rather
  than inventing an identity), and wired both into `transport.py`'s
  existing per-source endpoint-validation and parser-dispatch tables next
  to the other five sources. No API key is required at all -- the adapter
  needs no `SecretStr` credential, only the same enabled/policy_approved/
  operational_status gates every other official_data source has. A
  zero-result response is a normal empty batch; this adapter does not and
  should not decide `NOT_RELEVANT` for a market itself -- that judgment
  belongs to the later method-input mapping layer, per the owner's
  explicit "NOT_RELEVANT, never a penalty" instruction. Added the
  `usaspending` entry to `configs/sources/default_us_public.yaml`
  (`operational_status: available`, `credential_required: false`) and
  added it as a fallback source on the `firm_counts_and_economics` and
  `competitors_and_pricing` signal routes, matching its declared M4/M5
  methods. Added a gated live smoke test
  (`test_live_usaspending_single_page_query`) following the exact same
  `MASK_RUN_LIVE_OFFICIAL_SMOKE`/`MASK_OFFICIAL_POLICY_APPROVED` opt-in
  pattern as the other five sources; it was not run in this session.
Files:
  apps/api/src/mask_api/modules/official_data/{contracts.py,requests.py,
  parsers.py,transport.py,__init__.py};
  apps/api/tests/official_data/{test_requests.py,test_parsers.py,
  test_transport.py,test_live_official_smoke.py,
  fixtures/usaspending.json};
  configs/sources/default_us_public.yaml;
  docs/{OFFICIAL_DATA.md,SOURCE_AVAILABILITY.md,
  AUTONOMOUS_RESEARCH_MODE.md}; README.md; progress.txt.
Verification:
  New focused suite PASS: 9 tests (keyless request-builder shape and
  credential-field emptiness, 6 new invalid-input parametrize cases
  covering empty/malformed NAICS, malformed/reversed/overlong date
  ranges, and an invalid limit; parser extraction of a valid award plus
  missing-identity issue recording; and an end-to-end adapter test
  proving the new source dispatches its own parser through the shared
  transport with zero credential fields). Full offline suite PASS: 628
  (was 619), 31 integration tests deselected (was 30 -- the new live
  smoke test is correctly gated and skipped) -- confirmed via the
  project's actual root-scoped `pnpm run test:unit`. Ruff check/format
  PASS over 318 files; strict mypy PASS over 172 source files
  (`apps/api/src` and `workers`). Architecture/import-cycle check PASS.
  `research:schemas:check` PASS with no drift (USAspending reuses the
  existing `OfficialRecord` contract, no new schema). Local secret guard
  (`check:secrets`) and pinned Gitleaks source scan
  (`scan-secrets.ps1`, "no leaks found") both PASS. Node.js/pnpm confirm
  the full web suite (11 tests), web lint, and web typecheck all PASS
  unaffected. `pnpm check:progress` PASS at 31/172 (unchanged -- A17.5
  remains one in-progress checkpoint).
Limitations/blockers:
  No live USAspending network call has been made; only fixtures were
  used. The adapter is not yet wired into any composition root or
  `MethodExecutor`, and no concrete query-planning logic exists yet for
  how a market definition's NAICS codes/date range become an actual
  USAspending query -- that belongs to the later fallback-registry/
  MethodExecutor wiring. The distinction between "queried and found
  nothing" (candidate for `NOT_RELEVANT`) and "query itself was
  inadequate" is intentionally left to that later layer, not decided
  here.
Next:
  Build the O*NET downloadable-dataset bootstrap/importer (automated
  download, checksum/version verification, local cache, normalized-table
  import, skip-if-already-cached) for M3 occupation/task/workflow
  mapping; then the targeted Common Crawl retriever (CDX index lookup +
  single ranged WARC-record fetch only, never a full archive download);
  then the provider-independent source/evidence fallback registry with
  Tier A-E quality classes; then the first real `MethodExecutor`
  implementations -- toward one real, source-backed calibration market
  report at $0 paid-data cost. Building the concrete `urllib` transports
  and composition-root wiring for Gemini/YouTube (both already
  implemented) and running the USAspending/YouTube live smoke tests
  remain open, lower-priority sub-items of the same checkpoint.

2026-09-10 21:35 PKT (UTC+05:00) | AUTONOMOUS-028 | A17.5 | IN_PROGRESS
Work:
  Added the second A17.5 live-capable source: `mask_api.modules.
  youtube_data`, a fixed-scope adapter for two YouTube Data API v3
  endpoints (`search.list` for M2/M3/M5 video discovery,
  `commentThreads.list` for public audience pain/workaround discussion on
  a named video). Deliberately built as its own module rather than an
  extension of `official_data`, since `official_data` is explicitly scoped
  to US government statutory sources (see its own docs) and YouTube has a
  different auth shape (query-param API key against a commercial platform)
  and, more importantly, a different cost model: YouTube meters requests
  in quota units per endpoint (search=100, commentThreads=1, against a
  10,000-unit default daily free tier), which the shared `BudgetLedger`
  (requests/documents/bytes/currency) cannot express. Added a small,
  separate `YouTubeQuotaLedger` that reserves the exact per-endpoint unit
  cost before every call, alongside (not instead of) the existing shared
  budget preflight. A provider-reported `quotaExceeded` response (HTTP 403
  with that specific error reason, detected from the parsed error body)
  raises the distinct `YouTubeQuotaExceededTransportError` rather than a
  generic HTTP failure, so a future caller can record
  `SOURCE_UNAVAILABLE_QUOTA` and continue the run with other providers
  instead of failing it -- directly implementing the owner's "never fail
  the whole run on quota exhaustion" instruction. Contracts, quota
  accounting, request builders, parsers, and network transport are in
  separate files, matching `official_data`'s split; parsers open no
  sockets, read no credentials, and write no storage. Search/comment
  results are raw evidence only -- this module performs no relevance
  classification, persona assignment, or scoring; that stays the
  grounding pipeline's job. Flipped the `youtube` entry in
  `configs/sources/default_us_public.yaml` from `operational_status:
  credential_pending` to `available`, matching how `census_cbp`/`sam_gov`
  are `available` despite also requiring a credential (the field records
  whether the adapter/pipeline can accept a credential through normal
  configuration, not whether a specific local `.env` currently has one
  set) -- now that both the owner-supplied `MASK_youtube_API_KEY` and the
  adapter itself exist. Added `docs/YOUTUBE_DATA.md`.
Files:
  apps/api/src/mask_api/modules/youtube_data/{AGENTS.md,__init__.py,
  contracts.py,quota.py,requests.py,parsers.py,transport.py,errors.py};
  apps/api/tests/youtube_data/{test_requests.py,test_parsers.py,
  test_quota.py,test_transport.py,fixtures/{search.json,
  comment_threads.json,quota_exceeded.json}};
  apps/api/tests/research_runner/test_configuration.py (updated the
  youtube operational_status assertion to match the config change above);
  configs/sources/default_us_public.yaml;
  docs/{YOUTUBE_DATA.md,AUTONOMOUS_RESEARCH_MODE.md,SOURCE_AVAILABILITY.md};
  README.md; progress.txt.
Verification:
  New focused suite PASS: 39 tests (search/commentThreads request-builder
  validation limits, both parsers' extraction plus missing-title/empty-
  text issue recording and duplicate-comment dedupe, quota-ledger exact-
  cost charging/rejection-without-partial-charge/a cheaper endpoint still
  fitting after a more expensive one is exhausted, and the transport/
  adapter's provenance-and-quota-cost preservation, disabled/unapproved/
  credential-missing/operational-hold short-circuits before any network
  call, shared-budget and quota-ledger preflight rejection, the distinct
  provider quotaExceeded error path versus a generic 403 with no quota
  reason, generic HTTP-failure retry metadata, content-type rejection,
  and endpoint-origin enforcement). Full offline suite PASS: 619 (was
  580), 30 integration tests deselected -- confirmed via the project's
  actual root-scoped `pnpm run test:unit`. Ruff check/format PASS over
  318 files; strict mypy PASS over 172 source files (`apps/api/src` and
  `workers`). Architecture/import-cycle check PASS.
  `research:schemas:check` PASS with no drift (neither provider changes a
  persisted contract shape). Local secret guard (`check:secrets`) and
  pinned Gitleaks source scan (`scan-secrets.ps1`, "no leaks found") both
  PASS. Node.js/pnpm confirm the full web suite (11 tests), web lint, and
  web typecheck all PASS unaffected. `pnpm check:progress` PASS at
  31/172 (unchanged -- A17.5 remains one in-progress checkpoint).
Limitations/blockers:
  No live YouTube network call has been made; only a fake transport was
  exercised, matching the Gemini provider's current state. The adapter is
  not yet wired into any composition root or `MethodExecutor`, and no
  concrete `search.list` query-planning logic exists yet for how a market
  definition becomes an actual search query -- that belongs to the later
  fallback-registry/MethodExecutor wiring, not this adapter. `videos.list`
  (view/like/comment counts for a discovered video) was intentionally not
  built in this pass to keep scope to the two endpoints the owner's
  evidence list named (pain/complaints, workflow evidence, competitor
  discussion); it can be added later at 1 quota unit per call if a metric
  needs it.
Next:
  Build the concrete stdlib `urllib` transports and wire both `GeminiAdapter`
  and `YouTubeApiAdapter` through a composition root under explicit policy
  approval; then continue A17.5 with the keyless USAspending adapter
  (M4/M5), the O*NET dataset bootstrap/importer (M3), the targeted Common
  Crawl retriever, the provider-independent source/evidence fallback
  registry with Tier A-E quality classes, and the first real
  `MethodExecutor` implementations -- toward one real, source-backed
  calibration market report at $0 paid-data cost.

2026-09-10 21:15 PKT (UTC+05:00) | AUTONOMOUS-027 | A17.5 | IN_PROGRESS
Work:
  Started A17.5 with its first live-capable sub-piece: a Gemini structured-
  analysis provider mirroring openai_responses.py's exact architecture
  (Settings/TransportResponse/Transport Protocol/build-payload/parse-
  result/Adapter), reusing the existing provider-neutral `AnalysisProvider`
  port, `AnalysisRequest`/`AnalysisResult` contracts, and
  `ArtifactAnalysisCache` unchanged. `build_generate_content_payload`
  builds a stateless, tool-free `generateContent` request: the untrusted
  source text is embedded only inside a JSON data blob in the user turn,
  never inside the `systemInstruction`, and the extraction-boundary
  instructions ("never calculate market scores... gates, or rankings")
  stay fixed regardless of source content -- verified with an explicit
  prompt-injection-shaped source-text test. `to_gemini_schema` inlines
  Pydantic's `$ref`/`$defs` and drops `additionalProperties`/`$defs`/
  `title`, since Gemini's `responseSchema` dialect is a restricted OpenAPI-
  3.0-like subset that does not support them; this is a documented,
  accepted provider difference (Gemini cannot itself reject an unexpected
  extra property the way OpenAI's strict mode does) -- required field and
  type/enum validation is unaffected. `parse_generate_content_result`
  defensively parses the `candidates`/`usageMetadata`/`promptFeedback`
  envelope, maps `finishReason` to the same COMPLETED/REFUSED/INCOMPLETE
  states OpenAI's adapter uses, validates structured output against the
  same versioned schemas, and computes cost via the same per-request
  pricing fields. `GeminiAdapter` is cache-first, budget-gated through the
  shared `BudgetLedger`, and requires `enabled` + `policy_approved` +
  `MASK_gemini_API_KEY` (the exact existing env var name, not renamed)
  before any transport call -- identical disabled-by-default posture to
  the OpenAI adapter. Gemini still performs extraction/classification only
  here; no code path lets it calculate a score, confidence, gate, or veto.
  No live network call was made; the adapter was exercised only against a
  fake transport and fixture response bodies.
Files:
  apps/api/src/mask_api/modules/analysis/gemini.py;
  apps/api/tests/analysis/{test_gemini.py,
  fixtures/gemini_relevance_completed.json}; progress.txt.
Verification:
  New focused suite PASS: 12 tests (schema-dialect $ref inlining, payload
  statelessness/tool-freedom, prompt-injection-in-source-text stays inert
  in the system instruction, parser usage/cost/schema validation against
  the fixture, null-optional-field preservation for CommercialPainRecord,
  disabled-adapter short-circuit before any transport call, cache reuse
  while disabled, safety-blocked-prompt and non-STOP-finish-reason
  handling remain explicit and uncached, and four malformed/missing-
  candidate envelope rejection cases). Full offline suite PASS: 580
  (was 568), 30 integration tests deselected -- confirmed both from a
  plain apps/api-scoped run and the project's actual root-scoped
  `pnpm run test:unit` invocation, which is authoritative. Ruff
  check/format PASS over 305 files; strict mypy PASS over 165 source
  files (`apps/api/src` and `workers` -- the project's own `typecheck`
  scope; mypy is not run over `tests/`, matching the pre-existing
  `test_openai_responses.py` precedent exactly). Architecture/import-
  cycle check PASS. `research:schemas:check` PASS with no drift (no new
  schema needed; the Gemini provider reuses existing analysis contracts).
  Local secret guard (`check:secrets`) and pinned Gitleaks source scan
  (`scan-secrets.ps1`, "no leaks found") both PASS. Node.js/pnpm confirm
  the full web suite (11 tests), web lint, and web typecheck all PASS
  unaffected. `pnpm check:progress` PASS at 31/172 (unchanged -- A17.5
  remains one in-progress checkpoint, not yet DONE).
Limitations/blockers:
  No live Gemini network call has been made; only a fake transport was
  exercised. The adapter is not yet wired into any composition root or
  `MethodExecutor`, and no real `GeminiTransport` implementation (the
  actual `urllib`-based HTTPS edge) exists yet -- that live transport,
  plus explicit `policy_approved=True` enablement, is required before any
  real request can be sent. Gemini Search-grounded discovery (a separate
  concern from this structured-extraction adapter) is not yet built.
Next:
  Build the concrete stdlib `urllib` `GeminiTransport` implementation and
  wire `GeminiAdapter` through a composition root under explicit policy
  approval; then continue A17.5 with Gemini Search-grounded discovery, the
  live YouTube Data API collector (M2/M3/M5), the keyless USAspending
  adapter (M4/M5), the O*NET dataset bootstrap/importer (M3), the targeted
  Common Crawl retriever, the provider-independent source/evidence
  fallback registry with Tier A-E quality classes, and the first real
  `MethodExecutor` implementations -- toward one real, source-backed
  calibration market report at $0 paid-data cost.

2026-09-10 06:35 PKT (UTC+05:00) | AUTONOMOUS-026 | A17 | DONE
Work:
  Implemented A17 as `mask_api.modules.reporting`, a pure presentation layer
  over one `market_scoring.MarketScoreSnapshot` with no calculation of its
  own. `render_json_report` produces the machine-readable report.json (full
  `ReportPackage` contract). `render_html_report` produces a self-contained
  static HTML page (no template engine, no external asset) with every
  interpolated value passed through `html.escape` without exception, since
  A17.5 will feed it real web-derived evidence text that must never be
  trusted as safe markup; verified this explicitly with an XSS-shaped
  fixture. `render_source_manifest_csv` joins the registered source
  inventory against what a specific run actually attempted
  (`SourceAttemptOutcome`); a source never tried stays entirely absent
  rather than being backfilled with an invented row. `executive_summary`
  and `next_recommended_action` are template-generated from already-
  computed fields only, following SCORING.md section 7's exact priority
  order (confirmed veto, then suspected veto, then the nearest gate-
  blocking missing method, then remaining completeness) -- no free-form or
  model-generated prose anywhere in this checkpoint. `MethodReportEntry.
  detail` carries each method's own already-validated result (dumped to
  JSON by the caller) without reshaping it. PDF rendering is explicitly
  deferred and documented as a permitted limitation: it needs a new,
  unapproved rendering dependency (a weasyprint/reportlab-class library);
  HTML is print-to-PDF ready in any browser today. Added docs/REPORTING.md.
Files:
  apps/api/src/mask_api/modules/reporting/{AGENTS.md,__init__.py,
  contracts.py,builder.py,json_report.py,html_report.py,csv_manifest.py};
  apps/api/tests/reporting/{test_builder.py,test_json_report.py,
  test_html_report.py,test_csv_manifest.py};
  configs/schemas/reporting/report-v1.schema.json;
  scripts/export-research-schemas.py;
  docs/{AUTONOMOUS_RESEARCH_MODE.md,MODULARITY.md,REPORTING.md}; README.md;
  progress.txt.
Verification:
  A17 focused suite PASS: 13 tests (golden report composition with
  risk-adjusted-score cross-check, mismatched-market-id/naive-timestamp
  rejection, next-action priority ordering, JSON round-trip through the
  validated contract and determinism, HTML section presence and explicit
  script-tag-escaping proof, and CSV manifest join with untried-source-
  stays-blank). Full offline Python suite PASS: 568 passed (was 555), 30
  integration tests deselected. Ruff lint/format PASS over 303 files;
  strict mypy PASS over 164 source files; architecture/import-cycle check
  PASS; `research:schemas:check` and `export-openapi.py --check` PASS with
  no unexpected drift; local secret guard and pinned Gitleaks source scan
  PASS. Node.js/pnpm confirm the full web suite (11 tests), web lint, web
  format:check, web typecheck, `pnpm schemas:check`, and `pnpm
  check:progress` (31/172 completed, 141 remaining, matching this entry's
  hand-recalculated totals) all PASS.
Limitations/blockers:
  PDF output is not implemented (documented permitted limitation above).
  Every report built today is necessarily populated from test/fixture
  data, since no real MethodExecutor wiring exists yet -- a report is a
  template proof, not market evidence, until A17.5 lands. `source_attempts`
  and per-method `detail` are designed to be populated by A17.5's real run
  telemetry; callers may omit them today and the report renders honestly
  with what it has.
Next:
  Implement A17.5 (Free-Source Real Market Integration): a live Gemini
  analysis provider (mirroring openai_responses.py's exact shape, no SDK
  dependency), Gemini Search-grounded discovery (discovery-only until the
  original source is fetched/verified), a live YouTube Data API collector
  for M2/M3/M5, a keyless USAspending official_data adapter for M4/M5 (or
  NOT_RELEVANT), an O*NET downloadable-dataset bootstrap/importer for M3,
  a targeted Common Crawl retriever (indexed-record range fetch only),
  extending the existing RSS/Atom/static-HTML crawler under the same
  source-policy gate, a provider-independent source/evidence fallback
  registry with Tier A-E source-quality classes, M6 free-mode evidence,
  and the first real MethodExecutor implementations -- toward one real,
  source-backed calibration market report at $0 paid-data cost.

2026-09-10 06:10 PKT (UTC+05:00) | AUTONOMOUS-025 | A16 | DONE
Work:
  Owner supplied local Gemini and YouTube Data API credentials
  (MASK_gemini_API_KEY, MASK_youtube_API_KEY in .env.official.local; exact
  existing names reused, not renamed/duplicated) and directed a free-first
  real-data expansion: complete A16/A17, then wire real free sources into
  the existing calculators for one real, source-backed market report at $0
  paid-data cost. Re-confirmed all 5 official-data live smokes still PASS
  before starting. Added checkpoint A17.5 (Free-Source Real Market
  Integration) to the roadmap per explicit owner request; extended
  `scripts/check-progress.mjs` to recognize the `A\d{2}.\d` dotted-ID format
  so it is tracked by the same tooling as every other checkpoint (171 -> 172
  total; A track 20 -> 21). Implemented A16 as `mask_api.modules.
  market_scoring`, the top of the calculation stack, composing `confidence`
  and the method calculators' public results without recomputing any of
  them. `AutomatedResearchScore` is the raw (never renormalized) sum of
  available weighted method contributions -- explicitly not the approved
  score, matching SCORING.md's "provisional contribution" language exactly.
  `FinalValidatedScore` stays `not_ready` in every run, honestly, because no
  reviewed-score human-approval workflow exists yet (P12). `aggregate_
  confidence` implements the one renormalization SCORING.md approves
  (weighted mean across a required-method subset, only for confidence,
  never to conceal a missing score) and now backs both gate confidence and
  the full ten-method overall confidence. `evaluate_gate` implements the
  exact five-state result taxonomy (not_ready/blocked/eligible_to_advance/
  does_not_meet_gate/founder_review_required) with a confirmed critical veto
  always checked first, computing gate_score before the confidence check so
  a not_ready-for-confidence result still carries useful numbers.
  `build_market_score_snapshot` composes everything into one immutable,
  reproducible `MarketScoreSnapshot` (inputs_sha256 verifies bit-for-bit
  reproducibility independent of the per-run snapshot_id/generated_at);
  `snapshots_are_comparable` is the only approved way to decide two
  snapshots may be ranked together. Snapshots persist through the existing
  CLI `LocalArtifactStore`, not new production DB tables -- DATA_MODEL.md's
  `market_score_snapshots` table remains unimplemented (P02/P03/P13 scope)
  and this checkpoint does not build it ahead of schedule. Also extracted
  `label_for_confidence`/`clamp_100` out of A15's confidence.py as small
  shared public functions so this module's cross-method aggregation reuses
  the exact same Low/Medium/High boundary rule instead of duplicating it.
Files:
  apps/api/src/mask_api/modules/confidence/{confidence.py,__init__.py}
  (label_for_confidence/clamp_100 extracted and exported);
  apps/api/src/mask_api/modules/market_scoring/{AGENTS.md,__init__.py,
  contracts.py,scoring.py,gates.py,snapshot.py};
  apps/api/tests/market_scoring/{test_scoring.py,test_gates.py,
  test_snapshot.py};
  configs/schemas/market_scoring/market-score-snapshot-v1.schema.json;
  scripts/{export-research-schemas.py,check-progress.mjs};
  docs/{AUTONOMOUS_RESEARCH_MODE.md,MODULARITY.md}; README.md; progress.txt.
Verification:
  A16 focused suite PASS: 22 tests (golden automated-research-score with a
  constant-score reproducibility trick, no-renormalization proof,
  confidence-aggregation golden/missing-required-method cases, risk-
  adjusted-score formula and UNKNOWN propagation, all five gate result
  states including blocked-overrides-perfect-score and confidence-below-
  minimum-still-reports-gate-score, and snapshot golden/reproducibility/
  naive-timestamp-rejection/mismatched-veto-market cases). Confidence
  regression suite PASS unchanged after the extraction refactor (25 tests).
  Full offline Python suite PASS: 555 passed (was 533), 30 integration
  tests deselected. Ruff lint/format PASS over 291 files; strict mypy PASS
  over 158 source files; architecture/import-cycle check PASS;
  `research:schemas:check` and `export-openapi.py --check` PASS with no
  unexpected drift; local secret guard and pinned Gitleaks source scan PASS
  (confirmed the new local credential variable names are not leaked). Node.js/
  pnpm confirm the full web suite (11 tests), web lint, web format:check,
  web typecheck, `pnpm schemas:check`, and `pnpm check:progress` (30/172
  completed, 142 remaining, matching this entry's hand-recalculated totals)
  all PASS. All 5 credentialed official-data live smokes re-confirmed PASS
  at session start.
Limitations/blockers:
  `MarketScoreSnapshot` is not yet wired into the CLI (no MethodExecutor
  implementation exists for any method anywhere in the codebase yet --
  confirmed by direct inspection of research_runner/{execution,runner,
  __main__}.py); that wiring is explicit A17.5 scope, not an A16 blocker.
  Gemini/YouTube/USAspending/O*NET/Common Crawl adapters do not exist yet;
  credentials are configured but unused until A17.5. No live model, quota,
  or paid-data cost has been incurred.
Next:
  Implement A17 (machine-readable JSON report + human-readable report),
  then proceed immediately into A17.5: Gemini provider (mirroring
  openai_responses.py's Settings/Transport/build_payload/parse_result/
  Adapter shape, no SDK dependency), YouTube Data API collector
  (official_data-style, matching its already-registered `access:
  official_api` source entry), a keyless USAspending official_data adapter,
  an O*NET downloadable-dataset importer, a targeted Common Crawl retriever,
  a provider-independent source-fallback registry, source-quality tiers,
  and the first real MethodExecutor implementations wiring official_data/
  search_intent/the new adapters into method_metrics/pain_intelligence/
  workflow_intelligence -- toward one real, source-backed calibration
  market report at $0 paid-data cost.

2026-09-10 05:15 PKT (UTC+05:00) | AUTONOMOUS-024 | A15 | DONE
Work:
  Before starting A15, re-ran and confirmed the owner-supplied official-data
  credentials still work: all 5 live smokes (Census CBP, BLS, BEA Regional,
  SEC EDGAR, SAM.gov) PASS. Implemented A15 as a new `mask_api.modules.
  confidence` module sitting above the method calculators, consuming their
  public result contracts (`method_metrics.MethodMetricResult`) and never
  computing a method score itself. `calculate_confidence` implements the
  exact v1 weighted-aggregation expression (10*(0.25*SD+0.25*SA+0.20*EQ+
  0.10*R+0.20*A)) and Low/Medium/High labeling from five already-normalized
  0-10 dimension inputs; any missing dimension keeps the result UNKNOWN,
  never a partial/renormalized weighting. Deliberately does NOT derive
  source_diversity/evidence_quality/recency/cross_source_agreement from raw
  evidence: SCORING.md section 6 says those normalizers are versioned
  alongside each method rubric, and none exist in v1.yaml yet (open decision
  D04, still pending an owner/reviewer) -- inventing one would itself be an
  unapproved methodology change. `sample_adequacy_score` is the one
  dimension with an approved generic derivation (v1.yaml's
  `confidence.sample_targets` gives an exact target per method, matching
  that method's own `sample.full_target`), including the M10 multi-metric
  "any of" target via a best-of-ratios rule. `classify_completeness` maps a
  method's own status to Proven/Weak/Missing/Not-applicable. `evaluate_
  automatic_vetoes` implements the three v1 vetoes with a purely numeric
  trigger (no_meaningful_budget, no_reachable_economic_buyer,
  highly_bespoke_delivery): the numeric condition alone marks a finding
  suspected, and the exact confidence label the trigger names (HIGH, or
  MEDIUM-or-HIGH for highly_bespoke_delivery) is required for confirmed. The
  other three v1 vetoes are always reported in `not_evaluated` rather than
  guessed: two need an explicit human-verified evidence contract that does
  not exist yet, and one needs M8/M10 data (A18/A19). Every v1 veto is
  treated as critical severity; resolved/accepted_exception remain P18 human
  review-workflow states this module never assigns. Exported JSON Schemas
  for `MarketMethodSummary` and `VetoAssessmentResult`.
Files:
  apps/api/src/mask_api/modules/confidence/{AGENTS.md,__init__.py,
  contracts.py,confidence.py,completeness.py,vetoes.py};
  apps/api/tests/confidence/{test_confidence.py,test_completeness.py,
  test_vetoes.py};
  configs/schemas/confidence/{market-method-summary-v1.schema.json,
  veto-assessment-result-v1.schema.json};
  scripts/export-research-schemas.py;
  docs/{CONFIDENCE_AND_VETOES.md,AUTONOMOUS_RESEARCH_MODE.md,MODULARITY.md};
  README.md; progress.txt.
Verification:
  A15 focused suite PASS: 25 tests (confidence golden aggregation and Low/
  Medium/High boundary cases including the exact 50 and 75 thresholds,
  any-missing-dimension UNKNOWN, single- and multi-metric sample adequacy,
  no-approved-target/unknown-actual UNKNOWN cases, completeness mapping and
  not_applicable override, and per-veto confirmed/suspected/no-finding
  cases plus trigger-text-drift and incomplete-taxonomy guards). Full
  offline Python suite PASS: 533 passed (was 508), 30 integration tests
  deselected. Ruff lint/format PASS over 282 files; strict mypy PASS over
  153 source files; architecture/import-cycle check PASS;
  `research:schemas:check` and `export-openapi.py --check` PASS with no
  unexpected drift; local secret guard and pinned Gitleaks source scan
  PASS. Node.js/pnpm confirm the full web suite (11 tests), web lint,
  web format:check, web typecheck, `pnpm schemas:check`, and
  `pnpm check:progress` (29/171 completed, 142 remaining, matching this
  entry's hand-recalculated totals) all PASS. All 5 credentialed
  official-data live smokes re-confirmed PASS at session start.
Limitations/blockers:
  Confidence cannot become numerically known for any method until an owner/
  reviewer approves the source_diversity/evidence_quality/recency/
  cross_source_agreement normalizers (D04); this module is the aggregation
  engine those normalizers will plug into, not a substitute for approving
  them. Three of six vetoes stay permanently not_evaluated until their
  evidence contracts or M8/M10 exist. Gate-score calculation, the Gate 1-4
  readiness engine, and the human veto-investigation/resolution workflow
  are A16 and P18 respectively, not A15 blockers.
Next:
  Implement A16: approved overall weighting, fixed gate profiles, immutable
  score snapshots, deterministic recommendations, and compatibility checks,
  per docs/SCORING.md section 8 and the `gates` block already validated in
  configs/formulas/v1.yaml.

2026-09-10 04:50 PKT (UTC+05:00) | AUTONOMOUS-023 | A14 | DONE
Work:
  Implemented A14 by extending `mask_api.modules.method_metrics` (M9 uses the
  same v1 `weighted_sum` aggregation as M1/M4/M6/M7, not M2/M3's clustering
  pattern, so it belongs in the existing weighted-sum calculator module
  rather than a new one). Added `M9Inputs` plus three new composite input
  contracts: `CapabilityRequirement` (a capability the target market needs,
  with an importance weight, from the registry) and `CapabilityCoverage` (a
  technical/business reviewer's covered/not-covered judgment) together drive
  `technical_fit` via a new `weighted_coverage_score` transform helper
  (10 * weighted mean of covered flags by required_weight); `IntegrationRecord`
  (a target-market platform's discovered prevalence and whether MASK AI
  supports it) drives `integration_fit` as a summed supported-platform-
  prevalence ratio through the existing MULTIPLY transform. The other five
  components (`standardization`, `recurring_revenue_potential`,
  `proof_potential`, `delivery_simplicity`, `expansion_potential`) reuse the
  existing `SourcedMetric`/`simple_component_breakdown` pattern unchanged.
  Every declared `CapabilityRequirement` must have a matching
  `CapabilityCoverage` record or `technical_fit`--and therefore the whole
  method, since it is a required component--stays UNKNOWN, matching v1's
  "any required capability ... lacks source-grounded registry evidence"
  rule exactly; coverage is never inferred by this module, consistent with
  RESEARCH_METHODOLOGY.md's "AI may organize evidence only" for M9.
  Capability/platform ids must be unique and integration prevalence cannot
  exceed the total target market (100%). M9 has no v1 sample/partial-
  completeness rule, so a known result is always `complete`.
Files:
  apps/api/src/mask_api/modules/method_metrics/{__init__.py,AGENTS.md,
  contracts.py,transforms.py,m9.py};
  apps/api/tests/method_metrics/{test_m9_calculation.py,test_transforms.py};
  configs/schemas/method_metrics/method-metric-result-v1.schema.json
  (regenerated: only its class-docstring description text changed, now
  "M1/M4/M5/M6/M7/M9");
  docs/{AUTONOMOUS_RESEARCH_MODE.md,MODULARITY.md}; README.md; progress.txt.
Verification:
  A14 focused suite PASS: 13 tests (10 M9 calculation: golden weighted-sum,
  partial-capability-coverage UNKNOWN, uncovered-capability lowering
  technical_fit without dropping it, no-requirements/no-integrations
  UNKNOWN, log_scale zero floor, duplicate capability/platform id rejection,
  integration-prevalence-over-100%-rejection, and reproducibility; plus 3
  weighted_coverage_score transform cases). Full offline Python suite PASS:
  508 passed (was 495), 30 integration tests deselected. Ruff lint/format
  PASS over 272 files; strict mypy PASS over 148 source files; architecture/
  import-cycle check PASS; `research:schemas:check` and `export-openapi.py
  --check` PASS with no unexpected drift in any existing schema; local
  secret guard and pinned Gitleaks source scan PASS. Node.js/pnpm (installed
  during A12) confirm the full web suite (11 tests), web lint,
  web format:check, web typecheck, `pnpm schemas:check`, and
  `pnpm check:progress` (28/171 completed, 143 remaining, matching this
  entry's hand-recalculated totals) all PASS.
Limitations/blockers:
  M9's calculator is not yet wired into the CLI's MethodExecutor protocol,
  matching A11/A12/A13's precedent; a capability registry, integration
  discovery adapter, and leadership/technical-reviewer capture UI remain
  later work (P12), not an A14 blocker. No live model call, provider, or
  fixture is claimed as market evidence or an approved productization
  assessment.
Next:
  Implement A15: confidence/completeness, minimum samples, method statuses,
  UNKNOWN rules, contradiction effects, and automatic veto detection, per
  `docs/SCORING.md` sections 6-9 and the versioned `confidence`/`gates`/
  `vetoes` blocks already validated in `configs/formulas/v1.yaml`.

2026-09-10 04:35 PKT (UTC+05:00) | AUTONOMOUS-022 | A13 | DONE
Work:
  Implemented the modular offline A13 vertical slice: a ninth versioned
  analysis extraction schema, `workflow-step-v1`
  (`mask_api.modules.analysis.schemas.WorkflowStepRecord`/`WorkflowStepOutput`,
  registered in `AnalysisType`/`AnalysisSchemaId`/`_OUTPUT_MODELS`), and a new
  `mask_api.modules.workflow_intelligence` module. Ingestion accepts only
  A10-accepted `workflow-step-v1` reports and builds lineage-rich
  `WorkflowStepEvidence` records (role, system, input/output, and the six v1
  M3 raw metrics, each independently optional/UNKNOWN). Unlike M2, steps are
  grouped by an exact normalized-step-name hash rather than embeddings/
  clustering, since a reconstructed step has a natural identity; evidence
  within a group is averaged (never summed) so duplicate/repeated evidence
  cannot inflate a step's components, and exact-duplicate evidence
  (identical evidence_id) is rejected outright. The calculator applies the
  exact v1 M3 per-component transforms (log_scale for volume/labor burden,
  linear for failure_rate/manuality, identity for consequence/automation
  potential), keeps the whole result UNKNOWN if any non-empty step is
  missing a component (no renormalization, no silent exclusion), and
  aggregates via the same 60% max + 40% sqrt(evidence-count)-weighted top-
  five pattern A11 uses for M2. M3 has no v1 sample/partial-completeness
  rule, so a known result is always `complete`. Exported the M3 report
  schema and the new workflow-step-v1 analysis schema.
Files:
  apps/api/src/mask_api/modules/analysis/{contracts.py,schemas.py};
  apps/api/src/mask_api/modules/workflow_intelligence/{AGENTS.md,__init__.py,
  contracts.py,ingestion.py,metrics.py,service.py};
  apps/api/tests/workflow_intelligence/{test_metrics.py,test_ingestion.py,
  test_service.py};
  configs/schemas/workflow/m3-result-v1.schema.json;
  configs/schemas/analysis/workflow-step-v1.schema.json;
  scripts/export-research-schemas.py;
  docs/{AUTONOMOUS_RESEARCH_MODE.md,MODULARITY.md,WORKFLOW_INTELLIGENCE.md};
  README.md; progress.txt.
Verification:
  A13 focused suite PASS: 13 tests (12 workflow_intelligence: golden top-5-
  of-6 bottleneck aggregation with a programmatically-derived expected score,
  missing-component UNKNOWN, no-evidence UNKNOWN, log_scale zero-value floor,
  duplicate-evidence averaging/non-inflation, duplicate-evidence-ID
  rejection, contradiction linking, ingestion lineage/quarantine, and
  multi-document service composition; plus 1 new parametrized analysis-
  schema case for workflow-step-v1). Full offline Python suite PASS: 495
  passed (was 482), 30 integration tests deselected. Ruff lint/format PASS
  over 269 files; strict mypy PASS over 147 source files; architecture/
  import-cycle check PASS; `research:schemas:check` and `export-openapi.py
  --check` PASS with no drift in any existing schema; local secret guard and
  pinned Gitleaks source scan PASS; `git diff --check` reported no
  whitespace errors. Node.js/pnpm (installed during A12) confirm the full
  web suite (11 tests), web lint, web format:check, web typecheck,
  `pnpm schemas:check`, and `pnpm check:progress` (27/171 completed, 144
  remaining, matching this entry's hand-recalculated totals) all PASS.
Limitations/blockers:
  `WorkflowIntelligenceService` is not yet wired into the CLI's
  MethodExecutor protocol, matching A11/A12's precedent; that composition
  is later work, not an A13 blocker. "No unvalidated AI-generated workflow
  supports a high-confidence score" (RESEARCH_METHODOLOGY.md) is left to the
  future confidence engine (P13): v1's M3 completeness rule is purely about
  component availability, and this module does not invent an extra blocking
  gate v1 does not define. No live model call, provider, or fixture is
  claimed as market evidence.
Next:
  Implement A14: capability-registry ingestion, integration discovery,
  complexity, and the exact deterministic M9 fit formula, following the
  established module pattern (typed contracts, an ingestion/grouping step
  if evidence-grounded, a formula-driven calculator, golden/UNKNOWN tests).

2026-09-10 04:10 PKT (UTC+05:00) | AUTONOMOUS-021 | A12 | DONE
Work:
  Implemented the modular offline A12 vertical slice: mask_api.modules.
  method_metrics with typed, source-grounded SourcedMetric/MetricProvenance
  contracts (observed/estimated/interview-confirmed states, geography/
  population/period/currency/unit, evidence reference); a shared
  MethodMetricResult/ComponentBreakdown contract reused by all five methods;
  a transform dispatcher implementing linear, reverse_linear, log_scale,
  multiply, complement, the M5 weighted_mean_sqrt top-5 competitor-gap
  aggregation, and the M7 composite meta_fit calculation, all built on
  mask_api.modules.scoring.math primitives; and one calculator per method
  (m1.py-m7.py) that validates the loaded v1 MethodFormula shape, builds a
  complete per-component breakdown (raw input, transformed score, weight,
  weighted contribution, evidence references, status/reason), and applies
  each method's exact v1 completeness/partial-completeness/sample rule. A
  missing required component keeps the whole method UNKNOWN; M1's "at least
  three of four optional components" partial-completeness rule is the only
  place a weighted sum uses fewer than all configured components, and even
  then only at each component's configured, never redistributed, weight. M5
  rejects duplicate competitor-gap findings before they can inflate
  unresolved_gap, and only its top 5 by score (weighted by sqrt(evidence
  count)) are used. M7's two ratio components (buyer_identification,
  contact_coverage) use safe_rate so a zero target_accounts denominator
  stays UNKNOWN rather than dividing by zero. log_scale floors a zero/
  negative raw value to score 0 instead of raising, since a legitimately
  observed zero count is not an invalid input. Exported a JSON Schema for
  the shared result contract.
Files:
  apps/api/src/mask_api/modules/method_metrics/{AGENTS.md,__init__.py,
  contracts.py,provenance.py,transforms.py,m1.py,m4.py,m5.py,m6.py,m7.py};
  apps/api/tests/method_metrics/{test_transforms.py,test_m1_calculation.py,
  test_m4_calculation.py,test_m5_calculation.py,test_m6_calculation.py,
  test_m7_calculation.py};
  configs/schemas/method_metrics/method-metric-result-v1.schema.json;
  scripts/export-research-schemas.py;
  docs/{AUTONOMOUS_RESEARCH_MODE.md,MODULARITY.md}; README.md; progress.txt.
Verification:
  A12 focused suite PASS: 42 tests, including golden weighted-sum results
  for all five methods, UNKNOWN/PROVISIONAL boundary cases, the M1 "at
  least three of four" partial-completeness rule, M5's duplicate-
  competitor-gap rejection and top-5-of-6 ranking, M7's zero-denominator
  and geography-mismatch cases, and log_scale's zero/negative-value floor.
  Full offline Python suite PASS: 482 passed (was 440), 30 integration
  tests deselected. Ruff lint PASS and Ruff format --check PASS over 260
  files; strict mypy --strict PASS over 142 source files; architecture/
  import-cycle check PASS; `research:schemas:check` and `export-openapi.py
  --check` (Python side of schemas:check) PASS with no drift in any
  existing schema; local secret guard and pinned Gitleaks source scan
  PASS; `git diff --check` reported no whitespace errors. Node.js/pnpm
  were initially absent from this execution environment; after installing
  Node.js 24 LTS via winget (pnpm run through `npx pnpm@11.19.0` because
  corepack's global shim install failed on an unelevated write under
  `C:\Program Files\nodejs`), `pnpm --filter @mask/web test` (11 tests),
  `lint`, `format:check`, `typecheck`, `pnpm schemas:check`, and
  `pnpm check:progress` (26/171 completed, 145 remaining, matching this
  entry's hand-recalculated totals) all PASS.
Limitations/blockers:
  These calculators are not yet wired into the CLI's MethodExecutor
  protocol or into official_data/search_intent source adapters; like A11's
  PainIntelligenceService, that composition needs an approved source-to-
  metric mapping per provider and is later work, not an A12 blocker. pnpm
  itself is not durably installed as a global shim in this environment
  (corepack's shim step needs elevation); future sessions must run it via
  `npx pnpm@11.19.0` or complete `corepack enable` with admin rights.
Next:
  Implement A13: evidence-grounded workflow reconstruction, bottleneck
  metrics, and the exact M3 formula calculator, following A12's module
  pattern (typed contracts, a transform dispatcher reusing scoring.math,
  one calculator file, golden/UNKNOWN tests). Re-run the pnpm-based web/
  progress checks on a Node-enabled environment before relying on their
  A12 status.

2026-09-10 02:51 PKT (UTC+05:00) | AUTONOMOUS-020 | A11 | DONE
Work:
  Built the modular offline M2 pain-intelligence vertical slice. Added an A10-only
  ingestion boundary with document/source/persona/analysis lineage; normalized
  pain mentions and financial values; exact document+mention dedupe; cached,
  versioned embedding ports; a zero-network local signed word/bigram hashing
  adapter; high-threshold near dedupe; optimized deterministic complete-linkage
  cosine clustering; stable cluster/membership/input hashes; strict persona
  separation; outlier handling; representative mentions; source/date/persona/
  component sample distributions; linked contradiction reporting; and the exact
  formula-v1 M2 cluster/top-five calculation. Missing components or fewer than
  100 relevant unique documents remain UNKNOWN; below 500 documents or three
  retained source families remains provisional. Exported the M2 report schema.
Files:
  apps/api/src/mask_api/modules/pain_intelligence/{AGENTS.md,__init__.py,
  clustering.py,contracts.py,deduplication.py,embedding_cache.py,embeddings.py,
  ingestion.py,metrics.py,ports.py,service.py}; apps/api/tests/pain_intelligence/;
  configs/schemas/pain/m2-result-v1.schema.json;
  docs/{AUTONOMOUS_RESEARCH_MODE.md,MODULARITY.md,PAIN_INTELLIGENCE.md};
  scripts/export-research-schemas.py; README.md; progress.txt.
Verification:
  A11 focused suite PASS: 10 tests, including 100-record golden calculation,
  cache reuse, exact/near dedupe, deterministic clustering, persona isolation,
  outliers, contradiction reporting, UNKNOWN handling, and service composition.
  Full offline Python suite PASS: 440 passed, 30 integration tests deselected;
  web suite PASS: 11. Ruff lint/format PASS over 244 files; strict mypy PASS over
  133 source files; web ESLint/Prettier/type generation/TypeScript,
  architecture/import-cycle, OpenAPI/TypeScript drift, research-schema drift,
  git diff whitespace, local secret guard, and Gitleaks source scan PASS.
Limitations/blockers:
  The local lexical hashing adapter is intentionally cheap and reproducible but
  is not a claim of production semantic-embedding quality. A later semantic model
  needs an explicit provider/configuration, bounded budget, labelled clustering
  evaluation, and human cluster review. Persistence/UI and the 100-200-record AI
  release dataset remain Phase 4 work. Fixtures are not market evidence. No live
  model, paid API, collection, outreach, or campaign ran.
Next:
  Implement A12 typed metric/provenance inputs and exact formula-driven
  M1/M4/M5/M6/M7 calculators with golden boundary, missing-value, and UNKNOWN
  tests; wire source adapters only after transforms cannot invent values.

2026-09-10 02:14 PKT (UTC+05:00) | AUTONOMOUS-019 | A11 | IN_PROGRESS
Work:
  Started the offline M2 pain-intelligence checkpoint after A10 passed. Reviewed
  the canonical M2 evidence fields, exact v1 formula, 100/500 unique-document
  sample rules, three-source-family provisional rule, persona separation,
  embedding/clustering lineage, outlier, contradiction, and human-review duties.
  The implementation will consume only A10-accepted grounded records and keep
  embeddings behind a versioned injected port/cache so held model access is not
  silently reintroduced.
Files:
  progress.txt.
Verification:
  A10/full quality gates PASS before A11 start. A11 code/tests NOT RUN because
  this entry records the required start checkpoint before implementation.
Limitations/blockers:
  Live/model embeddings remain held. A11 must use deterministic fixture vectors
  for offline acceptance and cannot claim production semantic quality until a
  provider/configuration and the evaluation policy are explicitly approved.
Next:
  Add grounded pain-mention/embedding/cluster contracts, exact and near-duplicate
  handling, deterministic persona-separated clustering, M2 component/aggregation
  calculations, lineage-rich report data, and golden/UNKNOWN tests.

2026-09-10 02:12 PKT (UTC+05:00) | AUTONOMOUS-018 | A10 | DONE
Work:
  Implemented the mandatory offline grounding boundary after structured analysis.
  It revalidates request/schema identity, checks exact case-sensitive spans,
  normalizes and verifies material numeric claims, preserves contradictory records
  and paths, detects bounded prompt-injection signals, and deterministically
  identifies high-impact financial/purchase-intent/severe-pain/pricing/workflow/
  interview claims. Added an injected independent-verifier port and service: a
  missing/ambiguous verification or injection signal quarantines output,
  unsupported claims and deterministic failures reject it, and only accepted
  reports expose a separate scoring_input. No repair, retry, model choice, score,
  provider call, or credential was added.
Files:
  apps/api/src/mask_api/modules/analysis/{__init__.py,AGENTS.md,grounding.py,
  grounding_contracts.py,ports.py}; apps/api/tests/analysis/{test_grounding.py,
  test_schemas.py}; docs/{ANALYSIS_PROVIDER.md,AUTONOMOUS_RESEARCH_MODE.md,
  GROUNDING_VALIDATION.md,MODULARITY.md}; README.md; progress.txt.
Verification:
  A10/A09 focused suite PASS: 27 tests. Full offline Python suite PASS: 430
  passed, 30 integration tests deselected; web suite PASS: 11. Ruff lint/format
  PASS over 229 files; strict mypy PASS over 123 source files; web ESLint,
  Prettier, route type generation, and TypeScript PASS; architecture/import-cycle,
  OpenAPI/TypeScript drift, research-schema drift, git diff whitespace, local
  Git-aware secret guard, and Gitleaks source scan PASS.
Limitations/blockers:
  A10 is an offline safety gate, not production AI approval. A09 live execution,
  provider/model selection, labelled AI release evaluation, and all paid calls
  remain held. The severe-pain 8-10 rule is versioned review prioritization only,
  not methodology scoring. No output from fixtures is market evidence.
Next:
  Begin A11 locally with grounded M2 record ingestion, exact/near dedupe,
  provider-neutral embeddings, deterministic clustering/persona separation, and
  cluster calculation/report contracts; retain UNKNOWN where inputs are absent.

2026-09-10 01:55 PKT (UTC+05:00) | AUTONOMOUS-017 | A09 / A10 | IN_PROGRESS / IN_PROGRESS
Work:
  Completed A09's offline boundary: eight canonical v1 Pydantic/JSON Schemas,
  provider-neutral request/result/cache ports, immutable local cache, and an
  OpenAI Responses mapping/parser with explicit model/cost/token limits,
  untrusted-input separation, no tools, no storage, raw response provenance,
  and refusal/incomplete handling. Corrected A07's intent values to the canonical
  taxonomy. Began A10 after the A09 offline suite passed.
Files:
  apps/api/src/mask_api/modules/analysis/{AGENTS.md,__init__.py,cache.py,
  contracts.py,openai_responses.py,ports.py,schemas.py};
  apps/api/tests/analysis/; configs/schemas/analysis/;
  apps/api/src/mask_api/modules/search_intent/{contracts.py,taxonomy.py};
  apps/api/tests/search_intent/{test_google_ads.py,test_taxonomy.py};
  docs/{ANALYSIS_PROVIDER.md,MODULARITY.md,SEARCH_INTENT.md};
  scripts/export-research-schemas.py; progress.txt.
Verification:
  A09 plus corrected A07 focused suites PASS: 40 tests. Ruff PASS over both
  modules/tests and schema exporter; strict mypy PASS over the analysis module
  and exporter; generated research-schema drift check PASS. Current official
  OpenAI Structured Outputs documentation was checked for the Responses JSON
  Schema shape, closed/required objects, supported schema subset, and explicit
  refusal/incomplete edge cases. No live request ran.
Limitations/blockers:
  A09 remains IN_PROGRESS because the owner placed live model use on hold: no
  model/key is configured, no concrete credential-bearing network transport is
  bundled, and no evaluation release gate has been claimed. Its offline boundary
  is ready for later opt-in wiring. A10 has just started.
Next:
  Implement a pure A10 validation report and independent-verifier port; quarantine
  invalid spans/numbers, preserve contradictions, and require review for injection
  signals or unverified high-impact claims before scoring eligibility.

2026-09-10 01:35 PKT (UTC+05:00) | AUTONOMOUS-016 | A09 | IN_PROGRESS
Work:
  Owner kept held APIs disabled and directed implementation forward. Started the
  offline A09 extraction foundation: provider-neutral analysis contracts, strict
  versioned schemas, deterministic cache/provenance, and a fail-closed OpenAI
  Responses Structured Outputs boundary. Current official OpenAI documentation
  was reviewed; no API key, model call, or paid budget was enabled.
Files:
  progress.txt.
Verification:
  NOT RUN yet; implementation has just started.
Limitations/blockers:
  Live OpenAI acceptance remains intentionally held and no model has been selected
  or authorized. During schema review, the A07 code taxonomy was found to differ
  from the canonical AI_PIPELINE.md search-intent values; this must be reconciled
  before sharing one schema version.
Next:
  Align the offline intent taxonomy, then implement and fixture-test A09 without
  adding an SDK, credential, network call, or business scoring logic.

2026-09-10 01:06 PKT (UTC+05:00) | AUTONOMOUS-014 | A05 | DONE
Work:
  Owner supplied local Census, BEA, and SAM.gov credentials, approved their use,
  and directed YouTube and other unavailable providers to remain skipped. Stored
  the three values only in an ignored local environment file. Ran one bounded
  live query for each supplied API plus keyless BLS and SEC EDGAR. Replaced the
  discontinued BEA CAEMP25N smoke query with one active CAINC4 county query and
  made the parser recognize BEA's current top-level error envelope. Marked the
  three credentialed source states available in source profile v2 and documented
  the exact lean source set and intended research uses without recording secrets.
Files:
  Local ignored .env.official.local (not tracked); .gitleaks.toml;
  configs/sources/default_us_public.yaml and generated source schema;
  apps/api/src/mask_api/modules/official_data/parsers.py;
  apps/api/tests/official_data/{test_live_official_smoke.py,test_parsers.py,
  test_transport.py}; docs/{OFFICIAL_DATA.md,SOURCE_AVAILABILITY.md}; README.md;
  progress.txt.
Verification:
  Authorized live official suite PASS: 5/5 in 40.95 seconds—one Census CBP,
  keyless BLS, BEA Regional, SEC EDGAR, and SAM.gov request. The first BEA
  attempt safely failed because its old table was discontinued; the corrected
  bounded query passed. Local Git contact identity supplied the required
  user-agent contact and was not committed. Local secret guard PASS. Gitleaks
  source scan PASS after excluding ignored local environment files while the Git
  aware guard continues to inspect every tracked/non-ignored source file.
Limitations/blockers:
  Official adapters are not yet composed into API/worker jobs or persisted as
  market evidence; those remain Phase 3/A11-A12 work. Live smoke responses were
  validation only, not retained research evidence. No broad collection ran.
Next:
  Keep these five official sources opt-in and bounded; wire their exact
  source-to-metric transforms under A11-A12 after extraction validation.

2026-09-10 01:06 PKT (UTC+05:00) | AUTONOMOUS-015 | A07 | IN_PROGRESS
Work:
  Implemented source-neutral historical-metrics contracts; exact micros and
  currency-aware CPC/bid amounts; monthly volume and competition normalization;
  `keyword-intent-v1`; Google Ads request mapping and fixture parser; a typed
  cache port with immutable 30-day default local artifact records; short native
  Windows paths with full internal hash validation; and a fail-closed injected
  provider adapter. Added module-scoped modularity instructions. No intent weight
  or M6 score was invented.
Files:
  apps/api/src/mask_api/modules/search_intent/{AGENTS.md,__init__.py,cache.py,
  contracts.py,google_ads.py,taxonomy.py};
  apps/api/tests/search_intent/{fixtures/google_ads_historical.json,
  test_cache.py,test_google_ads.py,test_taxonomy.py};
  apps/api/src/mask_api/research_runner/artifacts.py;
  docs/{MODULARITY.md,SEARCH_INTENT.md}; README.md; progress.txt.
Verification:
  A07 focused suite PASS: 21. Official-parser plus A07 suite PASS: 35. Full
  offline Python PASS: 402 passed, 30 integration tests deselected; web PASS: 11.
  Ruff/format PASS over 215 files; strict mypy PASS over 115 source files;
  architecture/import-cycle, OpenAPI/TypeScript drift, research-schema drift,
  local Git-aware secret guard, and redacted Gitleaks source scan PASS.
Limitations/blockers:
  A07 is not DONE because Google Ads account/developer-token approval and live
  acceptance remain intentionally held. The concrete credential transport is not
  bundled or composed. Fixtures are not market evidence, and M6 formula mapping
  remains A12. YouTube, Reddit, Meta, Google Places, Brave, and firmographics
  also remain skipped/held. No paid or model call ran.
Next:
  Proceed to A09's offline structured extraction/provider port and A10 grounding
  validation while paid/model network use stays disabled.

2026-09-09 23:18 PKT (UTC+05:00) | AUTONOMOUS-013 | A07 | IN_PROGRESS
Work:
  Owner kept all paid and credential-dependent APIs on hold and directed work to
  continue. Started the offline A07 foundation: versioned intent taxonomy,
  historical-metrics contracts/parser/cache, and a provider boundary that must
  reject live Google Ads access while its source status remains held.
Files:
  progress.txt.
Verification:
  NOT RUN yet; checkpoint implementation has just started. Reviewed the current
  Google Ads Keyword Planning and historical-metrics field documentation. No
  provider request was made and no key was read or stored.
Limitations/blockers:
  Google Ads live acceptance remains credential/approval pending by owner
  decision. Offline contracts and fixture-backed behavior can proceed safely.
Next:
  Implement the modular A07 contracts, taxonomy, cache, held adapter, fixture
  parser, tests, and module-boundary documentation; run focused and full gates.

DATE/TIME (timezone) | EVENT ID | CHECKPOINT ID(S) | STATUS
Work:
  Specific completed or partial work.
Files:
  Project-relative changed paths; migration IDs if applicable.
Verification:
  Exact commands/checks and results, including NOT RUN reasons.
Limitations/blockers:
  Remaining issue, owner, and safe next action; approval source if relevant.
Next:
  One resumable checkpoint/action.
