# Shared boundaries

Python runtime services live in apps/api/src/mask_api and are imported by workers. Language-neutral API contracts live in packages/schemas. Do not duplicate scoring rules across languages.

